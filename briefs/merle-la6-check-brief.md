# Brief: L-A6 verify (calibrated lottery / two-shore census) — for a delegated session

**Context required before starting (in order):** `README.md` (binding stopping rules), `AGENTS.md`, `HANDOFF.md` item 1, `briefs/merle-la5-check-findings.md` (the −17 canary, computed exact), `briefs/merle-round8-check-findings.md` (L-A4 descent verification; the (B) margin constants), `briefs/merle-round8-coedit-findings.md` part A (asymptote constants `c_gen = 0.0793186`, `c_strat = 0.2667875`, `α* = 0.3747344`), `cycles.md` 12.6.1 entire (esp. 12.6.1.2 census incl. −17 and the freebies, 12.6.1.3 spent stock, 12.6.1.5 asymptote), `itinerary.md` 14.15.6.7 (signed two-shore characterization — the frame his entry builds on), `aeh.md` 13.6 (the uniform-residue/genericity model this entry calibrates).

## Provenance

L-A6 arrived by shared-repo push (Merle, commits `92a6edb` seed + `fb5e8fc` realizability filter, dated 2026-07-24, on `main` now at `81431c7`) with an accompanying letter (2026-07-25, on file with the author). Seeded DRAFT, one key (his, explicitly **measured/assessed grade**), our key invited. The letter's framing: the negative shore ("south") is a control group — a twin where a paid-lock cycle actually exists (−17) — so the uniform-residue model can be *calibrated* on the shore where it can be tested. Entry claims (verbatim text to be recorded in the findings from the clone):

1. **Census.** In the letter alphabet (classical frame, numerator `B` from letter constants `β_m = 3^m − 2^m`), the complete `C = 1` census at `n ≤ 14`, both shores, is exactly: the Gersonides freebies (`|q| = 1`: `+1`, `−1`, `−5`), the `−17` orbit at `(n, K) = (7, 11)` (primitive, `q = −139`, words `(4,3|1,3)` and `(3,4|3,1)` realizing `−17` and `−41`), and the L-A4-forced powers. Nothing else; frame agreement `q | B ⟺ q | R_0` on every hit; predictions written before measurement.
2. **Necklace budgets.** With the necklace (rotation orbit) as the independent trial (rotation-invariance by L-A1): south `n ≤ 14`: `λ = 1.12`, primitive necklace-hits = 1 (the −17 — "the unique paid-lock cycle in existence"); north: `λ = 2.64`, hits = 0, `P(0) ≈ 7%`.
3. **Tail.** `Σ_{n>14}` decays geometrically at the capacity–demand rate (the (B) constants): south `+0.16`, north `+0.33` through `n = 200`, vanishing beyond; dominant future cells on convergent anchors (`27/17` north, `84/53` south); the north's residual budget beyond the verified range `≈ 5·10⁻³` formal cycles.
4. **Realizability filter closed (REQ-MATH-025).** A formal hit IS a real cycle: all 18 census hits realize exactly (true-map orbit, parity, sigma pattern, return); mechanism the "2-adic ghost" identity — the fixed point `x = B/q` follows the word's itinerary automatically (300/300 random words; parity forced by `v_2(ghost) = 0`). So budgets are direct real-cycle expectations, no correction.
5. **Reading, assessed grade:** the south's winnings equal its budget (calibration), and under the calibrated model the north's remaining expectation is ~0.005. The wall's role unchanged. **Falsifier: any second paid-lock cycle, either shore, breaks the calibration.**
6. **Artifacts:** `experiments/test_REQ-MATH-022_miroir_rive_sud.py` (census), `test_REQ-MATH-023_loterie_calibree.py` (budgets + tails), `test_REQ-MATH-025_*` (realizability), outputs committed alongside, in the shared repo.

**Grade discipline (main-session framing, binding):** this entry mixes three grades. (i) The census and realizability claims are *exact/verifiable* — our key can turn on them if they check. (ii) The budgets and tails are *replicable model computations* — verifiable as computations, conditional on the model. (iii) The calibration *reading* is assessed-grade heuristic and stays so — our key never grades it higher, and the findings keep the three strata visibly separate. He labels this honestly himself; match his care.

**Stopping-rule compliance:** independent re-verification of a correspondent's seeded entry — a *census* over a bounded finite range and a *model calculation*, not a cycle search and not a proof effort on the open condition. The cycles front stays PARKED. The one place this touches the wall is descriptive (the falsifier statement), and it stays descriptive.

## Queue

1. **Shared repo, read-only.** Fresh clone (scratchpad). Record HEAD (expected `81431c7`; if moved, record and continue read-only). Record the L-A6 entry text verbatim (both commits — seed and filter addendum) in the findings. Read the three REQ scripts and their committed outputs for the **operational definitions only** (cell = ? necklace count = ? budget formula = ? what "18 hits" enumerates — words or necklaces, both shores, powers included?); record these definitions explicitly. His scripts are read, never run as verification.

2. **Independent verification, fresh code** (`experiments/merle_la6_check.py`; imports nothing from his repos or prior check scripts; exact integer/rational arithmetic at every pass/fail decision; float only for the final λ/P(0) readouts, stated as such). **Write the predictions block first and print it before any sweep** (his method, and ours): expected census = freebies + −17 orbit + forced powers, expected hit counts per shore.
   - (a) **Census replication, exhaustive.** Enumerate both shores to `n ≤ 14` per the recorded operational definition (all words / all `(n, K)` cells in domain — derive the domain from the definitions, e.g. `K` range making `q` nonzero; both signs of `q`). For every word compute `gcd(q, R_0)` exactly; record every `C = 1` hit. Verify: exactly the predicted census, nothing else; frame agreement `q | B ⟺ q | R_0` on every hit (implement `B` independently from the `β_m` letter constants); the −17 orbit's two stated words and their realized cycles (−17 and −41 — true-map orbit computed exactly); the forced powers are exactly L-A4's (base a cycle ⟺ power a cycle — cross-check a sample against 12.6.1.4's identity).
   - (b) **Budget replication.** Count necklaces per cell (Burnside or direct orbit count — your own implementation), apply the recorded budget formula (expected: uniform-residue probability `~1/|q|` per necklace-trial, or the exact form his script defines — record which), and reproduce south `λ ≈ 1.12`, north `λ ≈ 2.64`, `P(0) = e^{−λ} ≈ 7%` (state the Poisson assumption explicitly as model, not fact). Verify "primitive hits: south 1 / north 0" against (a)'s census with powers and freebies excluded per the definitions.
   - (c) **Tail replication.** Extend the budget sum `n = 15..200` (necklace counts + the model probability; exact where feasible, controlled floats above). Check the geometric decay claim against the (B) constants (round-8 part A: the margin rate — derive the predicted ratio and compare); reproduce south `+0.16`, north `+0.33`; check the `27/17` / `84/53` dominant-anchor claim (identify the max-budget cells in your own sweep); reproduce the `≈ 5·10⁻³` north residual (record his cut — beyond the *verified range*; identify what range that is from his script/output and state it).
   - (d) **Realizability.** For all census hits from (a): construct the fixed point `x = B/q` (exact rational), run the TRUE map (×3±1 per shore convention — use the recorded frame), verify the orbit follows the word's itinerary and returns; that reproduces or refutes 18/18 (reconcile the count 18 against your hit list — words vs necklaces). Then the ghost identity on your own random words (fresh draws, both shores, 300+): does `x = B/q` always follow the word's parity pattern (`v_2` claim)? This is the load-bearing new lemma — if it holds on your draws, ALSO write out the one-paragraph why (his mechanism: parity of `x` forced by `v_2(ghost) = 0`) and check the argument, not just the counts; if the argument is exact, say so; if it is only empirical on your side, grade it so.
   - (e) **The "unique paid-lock cycle in existence" phrase.** This exceeds `n ≤ 14` (it appeals to the known ×3−1 cycle list). Check it against the classical list (1, −1, −5, −17 orbits; cite `cycles.md` 12.6.1.2) — the claim is that among the four known cycles only −17 has `|q| > 1` (paid lock). Verify on the knowns; flag that its universality is exactly the open question on the south shore beyond verified ranges (known verification bounds; do not overclaim what "in existence" is grounded on — record how his entry grounds it).

3. **Record** (branch commits, per-item):
   - `experiments/merle_la6_check.py` + committed output (one commit; predictions block printed first in the output).
   - `briefs/merle-la6-check-findings.md` — verbatim entry; operational definitions as recorded; per-claim adjudication in the three grade strata (exact-confirmed / model-replicated / assessed-heuristic-noted); the ghost-identity argument written out with its grade; discrepancies flagged flatly; the key recommendation (turn / turn-with-offer / hold) with the exact scope our key would carry — expected shape if clean: key turns on census + realizability + replicated computations, with the reading explicitly left at his own assessed grade.
   - `HANDOFF.md` item 1 update — L-A6 verification state per findings; owed next: co-edit/key push (gated on the author's go-ahead), reply draft. Do not touch other items.

## Rules

- Branch **`merle-la6-check`** from your worktree HEAD (verify it contains this brief; state the base SHA in the findings). Per-item commits; do NOT merge — the main session reviews (re-runs the script) and merges.
- Read-only everywhere outside this repo: no pushes, no shared-repo writes, no web actions beyond the GitHub clone/read of the shared repo. Any push impulse = stop and hand back.
- Exact arithmetic at every decision; canaries hand-computed and printed before sweeps (the four known cycles' words are the canaries — compute them by hand first, per his own practice).
- Discrepancies recorded and flagged, never disputed in prose, never silently reconciled. If your census finds a hit his misses (or vice versa), that is the finding of the round — triple-check your own frame conventions against the recorded operational definitions before flagging, then flag.
- No reply paragraphs; no key turns (recommendation only); no co-edit commits; stop after item 3.
