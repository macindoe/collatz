# Brief: round-14 review window — verify PR #4 (L-A10 and the two retractions), close the round-13 carries, prepare the paperwork — for a delegated session

**Context required before starting (in order):** `README.md` (binding stopping rules), `AGENTS.md`, `HANDOFF.md` item 1 (its live-conditions paragraph is STALE — this brief's Provenance is the current state; rewriting it is part of the job), `briefs/merle-round13-review-brief.md` and `briefs/merle-round13-review-findings.md` (the round this one answers: §3 the h5 gap, §5 the peak replication, §6 the §96 reconstruction with its four operational notes and the `p = 3` non-reproduction, §7.1 the posted review — the register model for this round's draft), `briefs/merle-la9-check-findings.md` §6.4 (the offers h1–h5 verbatim), `briefs/merle-la7-rhin-check-findings.md` (the Rhin carriers). Also `briefs/fresh-eyes-assessment-findings.md` §3.3 and `briefs/literature-dictionary-findings.md` §6 — for what the author's next mail will carry alongside this review (the dictionary paragraph); that paragraph is mail, not review, and does not enter the PR.

## Provenance (current state, 2026-09-04, verified read-only via `gh` by the main session)

- **PR #3 is MERGED** — by Merle himself, 2026-09-03 17:00 UTC, merge commit `95e7212`, after his own pre-merge commit **`f336e57` "Round 13, pre-merge: apply offer h5, correct the map's grading sentence."** So the L-A8 `(d-bis)` carry (round-13 findings §3) is applied on his side; verify it (Queue 4).
- **The NOTE-v1 bracket patch is on the shared repo's `main`** at **`93c6ac4`** ("NOTE-v1.md: restate the L-A9 grade sentence to the post-round-13 state, drop the signing bracket", author macindoe). `grep "GRADE AT SIGNING"` on main returns nothing. Shared repo HEAD **`93c6ac4`**; branches `round-14` at `2f7b865`, `round-13` at `f336e57`, `note-v1-draft` at `96ccadf`, `round-12` at `accda4b`.
- **PR #4 is OPEN**, opened 2026-09-03 18:14 UTC by `ericmerle3789`, branch `round-14` at **`2f7b865`**, base `main`, **zero reviews**. Our approving review is the second key per PROTOCOL §13. Three files: `LEDGER.md` (+38: the new entry **L-A10**, seeded at one key), `briefs/merle-breach-campaign-map.md` (+36/−17: the ⊢ §96 item rewritten to the two-line form, a "Withdrawn (2026-09-03)" block kept visible, the cross-side paragraph rewritten), `rounds/R14-merle.md` (+134, the letter). The full diff is 256 lines; fetch it yourself (`gh pr diff 4`), archive it verbatim in the findings.
- **His Lean repo** `ericmerle3789/one-obstruction-three-faces-lean` HEAD **`db0e89d`** (2026-09-03, "Section 96: commit the artifacts, retract the phantom census, keep the result."): `experiments/run_050.py` + output (his exact reprise, "14 checks, 0 failures"), `run_049.py` + output (the standing §96 run), `run_048.py` + output committed **with its canary firing** and a retraction header.
- **His Junction repo** `ericmerle3789/Collatz-Junction-Theorem` `main` at **`8bcee67`**: the Rhin retraction now at the head of its README, "additive, 10 lines, nothing removed" (the round-13 locatability item).

**What L-A10 claims** (his wording, from the diff): for odd `p ≥ 3`, `T` the Terras half map (`x/2` even, `(px+1)/2` odd), every fixed `k ≥ 1` and every `f : ℤ/2^k → ℝ_{>0}`, the altitude `V(x) = x·f(x mod 2^k)` fails to satisfy `V(T(x)) < V(x)` on all positive integers. Proof: `r_p = −(p−2)^{−1} mod 2^{k+1}` is odd; `u_p = r_p mod 2^k`; every positive `x ≡ r_p (mod 2^{k+1})` has `x ≡ T(x) ≡ u_p (mod 2^k)` and `T(x) − x = ((p−2)x+1)/2 > 0`; same residue, same `f`, so `V(T(x)) > V(x)`. Scope, his: closes the family at fixed `k`; excludes no cycle; not in Lean; the `k`-growing escape is prose only; `p = 1` is a negative control. Provenance, his: the loop is §95's fixed point `x = −1/(p−2)`, the legitimacy of the constraint is §96's; this round adds only that the two suffice in two lines.

**The `p = 3` resolution, his:** our round-13 reconstruction measured the accelerated map `x ↦ (px+1)/2^v` on odd residues with one canonical successor per node; his object is the half map on `ℤ/2^k` carrying both lifts from `ℤ/2^{k+1}`; our figures reproduce exactly on our object (`p=3`: none at `k=4..9`, one at `k=10`, one at `k=11`, two at `k=12`, none at `k=13..16`; `p=7,k=8`: three faulty cycles at `(L,K) = (3,4), (4,8), (31,67)`), and the witness edge `u → u` is absent from it. Our four operational gaps: note 1 identified the cause; the other three are "moot for this entry rather than answered".

**Two retractions, his:** (a) the phantom census, including "100 % of the faulty residue cycles are phantoms at `p = 7, k = 8`" (round 13 §6 and the campaign map), withdrawn in full — `run_048.py` never ran (its canary C3 halts it), with the bug fixed it fails its own control P5 (emits `x = −6`), and it never tests `p = 7`; committed as written with a retraction header. (b) "the witness climbs forever inside its class" — false, `511 → 767 → 1151` and `1151 ≡ 127 (mod 256)`; withdrawn. He also disambiguates: our own round-13 "100 % phantom" figure is **ours, on our object**, not reproduced on his side, and must not be read as related to the withdrawn census.

**Also in the letter:** peak replication accepted in our wording (clustering a clean non-finding, spectral marginal reported flat); his `5.62× @ f=124` withdrawn as a recoverable claim; our `θ = 1` strengthening verified and adopted (his numbers: `f'(0) = ln(p/4)`, `+0.2231` at `p=5`, `+0.5596` at `p=7`; drift `−0.1438` at `p=3`, `+0.1116` at `p=5`); h4 provenance recorded on his side; placement of §96 taken as we set it (apparatus section + one-sentence principle in the body's 2-adic paragraph); "No schedule, ever."

**The main session's pre-check (2026-09-04, fresh code, `scratchpad/la10_precheck.py`, not committed):** the two-line claim holds on `4,000` `(p,k)` pairs (`p = 3..201` odd, `k = 1..40`), five witnesses each, `20,000` witnesses, `0` failures; `r_3 = 511`, `u_3 = 255` at `k = 8`; `T(511) = 767`, `T(767) = 1151`, `1151 mod 256 = 127`; the accelerated map sends `255 ↦ 383 ≡ 127`; the half map also sends the lift `255 ↦ 383 ≡ 127` while the lift `511 ↦ 767 ≡ 255`; `p = 1` control: `T(511) − 511 = −255`. **One wording point to carry into the review, kindly:** his sentence "your map sends `255 → 127`, where ours has `255 → 255`" — the half-map relation on `ℤ/256` has *both* edges, `255 → 127` from the lift `255` and `255 → 255` from the lift `511`, exactly as his own "both lifts" description says; the sentence should read "ours also has". Not a defect in the claim.

**The author's decisions, on record for this window (2026-09-04):** (i) the §96 placement (apparatus witness + one-sentence body principle) stands, and he has accepted it — nothing to re-decide; (ii) **the review posting is the author's call after seeing the draft** — this window drafts, turns no key, writes to no remote; (iii) the wiki's new literature dictionary (TOUR.md) is mail business for the author's next letter, not PR business — do not put it in the review draft; (iv) L-A10 may receive a one-sentence pointer in our wiki (Queue 8) only if the key turns.

**Stopping-rule compliance:** verification of a co-edit round (an elementary theorem about finite-quotient altitude functions, two retractions, three closed carries) — not a proof effort on the open condition, not a cycle search, no per-period computation. Cycles front stays PARKED. L-A10 excludes no cycle and the review must not suggest otherwise.

## Queue

1. **State check, read-only.** Fresh scratchpad clones of the shared repo (expect HEAD `93c6ac4`; branch `round-14` at `2f7b865`), his Lean repo (expect `db0e89d`), his Junction repo (expect `main` at `8bcee67`). If anything has moved, record flat and continue read-only. Archive verbatim in the findings: the PR #4 body, the full PR #4 diff, the PR #3 merge commit `95e7212` and pre-merge commit `f336e57` messages, and `f336e57`'s diff of `LEDGER.md`.
2. **L-A10, keyed with fresh code** (`experiments/merle_r14_check.py`, imports nothing of his, exact integers everywhere, canaries printed first, per-item commits, committed output written by Python). (a) The two-line theorem: `p = 3..201` odd, `k = 1..40`, witnesses `x = r_p + m·2^{k+1}` for many `m` including large random `m` (hundreds of bits); check `r_p` odd, `x ≡ T(x) ≡ u_p (mod 2^k)`, `T(x) > x`. (b) The concrete instances above. (c) **The divergence diagnosis, reconstructed:** build both objects on `ℤ/2^k` — the accelerated single-successor graph of round 13 (reuse the definitions from `merle_r13_check.py` by re-implementation, not import) and the half-map relation carrying both lifts from `ℤ/2^{k+1}` — and show that at `p = 3, k = 8` the edge `255 → 255` is present in the relation (via the lift `511`) and absent from the single-successor graph; reproduce our own round-13 `p = 3` and `p = 7` figures on our object as a regression check; state in words which object each of the two sides measured. (d) The `p = 1` negative control. (e) The "for every `f`" quantifier: confirm the argument uses only "same residue ⇒ same `f`" and nothing about `f`'s values. (f) Scope: confirm the argument bears on no cycle and no integer orbit beyond one edge; say so in the findings in one sentence.
3. **His artifacts at `db0e89d`, read and run.** In the Lean-repo clone: run `run_050.py` as committed (note Python version and any environment fixes you had to make; do not edit his file — copy it to the scratchpad if a fix is needed and record the fix); compare its printed checks with his committed output and with your Queue 2 results, P4 in particular. Read `run_048.py`'s retraction header and run it as committed: record whether its canary C3 halts it as he says (expected), and do **not** attempt to fix or rescue it — the retraction is his and the record is that it fails. Read `run_049.py` P1's "20,000 edges, all realised" claim: state in the findings why edge-realisability is automatic for the both-lifts relation (every edge is defined by an integer lift) and whether P1 is therefore a consistency check rather than a finding — kindly.
4. **The h5 application (`f336e57`).** Diff the L-A8 `(d-bis)` block on main against our offered h5 text (`merle-la9-check-findings.md` §6.4) and our round-13 findings §3; confirm the census line is applied in our wording and figures with the superseded drawn control kept visible, as the letter says. The L-A8 ledger line in HANDOFF changes from "accepted but not yet applied" to closed only if this checks out.
5. **The campaign-map edits.** Confirm the "Withdrawn (2026-09-03)" block is additive and accurate; that the rewritten ⊢ item matches L-A10 word for word where it quotes it; that the cross-side paragraph's description of *our* round-13 figures (the 4 residue cycles, 3 faulty, `(L,K)` triples; the bounded-search "100 % phantom" as ours on our object) is accurate against `merle-round13-review-findings.md` §6 — quote our own sentence; and that no sentence presupposes cross-side verification this PR has not yet supplied (the round-13 grading-tension pattern).
6. **The Rhin locatability, at its address.** In the Junction clone: verify `8bcee67` on `main`, that the README head carries the retraction, and that the commit is additive (inspect its diff; count lines added/removed). This closes the round-13 locatability note; say so for HANDOFF.
7. **The smaller letter items, recomputed.** `θ = 1`: `f(θ) = (p/2)^θ + (1/2)^θ − 2`; `f(1) = 0` at `p = 3` exactly; `f'(0) = ln(p/4)` hence `+0.2231` (`p=5`), `+0.5596` (`p=7`) and no positive root there (scan `(0, 50]`); the mean-log-step drift `½(ln(p/2) + ln(1/2))`: `−0.1438` at `p=3`, `+0.1116` at `p=5`. Peak: confirm our round-13 wording is what he adopted (quote both). h4: confirm the three numbers are `merle_la9_check.py` PART 3's own output (grep the committed output).
8. **The wiki pointer (gated on Queue 2).** If L-A10 verifies, add one calibration sentence at `open-problems.md` 11.6 (the finite-state-shadow question) recording that no altitude of the form `x·f(x mod 2^k)` descends everywhere under the half map for any fixed `k` — Merle's L-A10, two keys once the review is posted — with the credit in the wiki's standing form (see cycles.md 12.6.1.1–12.6.1.4 for the verbatim-preservation convention) and a pointer to the shared ledger. Conservative: one sentence, no restatement of the proof, nothing else on the page moves. If it does not verify, no wiki edit.
9. **The round's paperwork, drafted (nothing sent, nothing pushed).**
   - *(The review draft.)* The full text of our approving review of PR #4, in the register of round 13's §7.1: verified-independently facts first (L-A10 keyed, with the counts; his artifacts run; the retractions read and confirmed; h5 confirmed applied; the Rhin fix confirmed at `8bcee67`; the θ and drift numbers recomputed), then the keys the review turns (the second key on L-A10 — in the draft's own words only if Queue 2 supports it; the peak entry closed in our wording; L-A8 closed), then the raised items: the "ours also has" wording point; the P1 realisability note if you judge it worth a sentence; anything Queue 5 finds. If any check fails, the draft flags instead of approving and the main session re-plans.
   - *(HANDOFF item 1.)* Rewrite the live-conditions paragraph, the "Repos, current values" paragraph and the ledger-state list to the current state: PR #3 merged by Merle 2026-09-03 (`95e7212`) with h5 applied in `f336e57`; the bracket patch on main at `93c6ac4` (delete the "pending push" sentence); PR #4 open at `2f7b865`, our key pending the author's posting of the review at `briefs/merle-round14-review-findings.md` §<n>; shared HEAD `93c6ac4`, Lean HEAD `db0e89d`, Junction `main` at `8bcee67`; a new ledger line **L-A10** (one key his; ours turns with the posted review); L-A8 closed; the phantom-census withdrawal recorded with the nuance that our round-13 figure is ours on our object; his spectral figure withdrawn; the round-13 locatability item closed; and, in the standing-conventions paragraph, that the author's next mail carries the dictionary paragraph (`briefs/literature-dictionary-findings.md` §6). Do not restructure anything else in the file. Item 4 of HANDOFF is not yours.

## Record

- `experiments/merle_r14_check.py` + committed output (canaries first; per-item commits; exact arithmetic at every pass/fail decision).
- `briefs/merle-round14-review-findings.md` — the verbatim archives (PR body, diff, the two commit messages, `f336e57`'s LEDGER diff, the run_048 retraction header, the README retraction text at `8bcee67`); every recomputation with its derivation; the divergence diagnosis in words; the artifact runs with their environment notes; the h5/map/Rhin adjudications; the review draft; the HANDOFF rewrite is on the page itself.
- `open-problems.md` 11.6 — one sentence, gated (Queue 8).
- `HANDOFF.md` — item 1 only (Queue 9).

## Rules

- Branch **`merle-round14-review`** in the worktree named in your prompt, cut from `main` at the commit carrying this brief. FIRST verify the worktree contains `briefs/merle-round14-review-brief.md`; if missing, `git merge main --ff-only`, and state your base SHA in the findings.
- Read-only everywhere outside this repo: fresh clones of the three Merle-side repos in the scratchpad; `gh` for PR texts, read-only; **no pushes, no comments, no reviews, no reactions, no fork/issue/star/watch, no contact with anyone.**
- File edits via the Edit/Write tools only — never PowerShell `Get-Content`/`Set-Content`. Committed outputs written by Python (no PowerShell redirection). Run `experiments/encoding_scan.py` before your final commit and record RESULT: CLEAN.
- Long computations run in the foreground; do not end the session until your final commit exists on the branch.
- Discrepancies recorded and flagged, never disputed in prose; every defect found is a finding delivered kindly — the register of his letters and of our round-13 review is the model. His two retractions are his to own; the review acknowledges them in one sentence each and does not dwell.
- No key turns (drafts only), no merges — the main session re-runs the script and merges; the review posting is the author's decision after reading the draft. Every commit message ends with the trailer line `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`. Stop after Record. Final report: branch, HEAD SHA, base SHA, `git log --oneline main..HEAD`, the script's summary lines, the verdict on each Queue item in one line, the encoding-scan result.
