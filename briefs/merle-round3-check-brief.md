# Brief: Merle's third reply — transport recurrence (antecedence + verification), his two corrections, and our key on the AEH cross-verification (for a delegated session)

**Context required before starting (in order):** `README.md` (binding stopping rules — see compliance below), `AGENTS.md`, `HANDOFF.md` item 1 (collaboration state), `cycles.md` 12.5–12.8 (especially Proposition 12.6.1 and the 12.8.6.4 instance record), `reverse.md` 14.15.9 section (a) entire (Lemmas 14.15.9.1–14.15.9.2, Theorem 14.15.9.3, Corollary 14.15.9.4), `aeh.md` §13 (especially the calibration record and the fixed-horizon "decisive test" protocol), `paper/collatz-reduced-v2.tex` section `sec:aeh` (the class-skeleton sentence, currently line ~226) and the Discussion's three directions, `briefs/merle-pincer-check-findings.md` (the prior round this reply answers), `spine.md` §9 (the exact per-step laws you will implement fresh).

## Provenance

Eric Merle's third reply (2026-07-18) was **pasted verbatim by the author** into the main session — no Gmail retrieval was involved, so its numerals are trusted as transcribed (this is the standing convention after the glyph-corruption incident recorded in `briefs/merle-pincer-check-findings.md`). The full text is Appendix A of this brief; it is the primary source — work from it, not from this summary. Contents, sorted by what they demand:

1. **Concessions and closure of round 2** — he accepts the p = 22 outcome (his recovery-threshold claim refuted at his own candidates, his cause confirmed), and answers our generator question: his `n = 25217, 31202` were "heuristic darts" (arithmetic near the scale target; `2×15601`), not generator output. This *confirms* what our findings item 2's "hypothesis to ask him" guessed. Record only; nothing to compute.
2. **The transport recurrence** (his candidate ledger entry L-A1) — the substantive mathematical claim, with an explicit **antecedence question addressed to us**: is it already in our record? Items 1–3 below.
3. **Two corrections to his own prior items** — the "local-global defect" is generic (his auditor proved `v₂(3^m w − 1) = s` is an algebraic identity, hence carries no information), and his distance-statistic KS number was miscomputed (`D = 0.1525`, not `0.136`; ~34 independent instances, not 62, because `c = 1` and `c = 2` give the same `u`). Item 4.
4. **The AEH cross-verification** (his candidate L4) — his stack put both keys on the paper's class skeleton and went one step further (the Discussion's "direction two"): a measured class-level transfer-matrix spectrum. Pending *our* key. Item 5.
5. **Protocol acceptances** (repo, co-editing, his proposed ledger state L1/L2/L3/L-A1/L4). Reply-drafting material for the author; record only.

**His claims, stated exactly (from Appendix A):**

- **Transport recurrence:** `R_{r+1} = (3^{m_r} R_r + (2^{s_r} − 1)·q) / 2^{σ_r}` — "an exact identity for every profile (not only staircases; verified on 200 random ones, 0 failures)". Reduced mod `q` the `(2^{s_r} − 1)q` term vanishes, so `R_{r+1} ≡ (unit)·R_r (mod q)` (2 and 3 both invertible mod `q`). Corollaries he draws: `q | R_0` iff `q | R_r` for all `r`; `gcd(q, R_r)` is rotation-invariant; "the `p` divisibility conditions of a cycle are, exactly, one condition."
- **His reading of our record:** "cycles.md enumerates `q | R_0, R_1, R_2` at p = 3 as three checks, and the survivor searches carry a per-rotation divisibility test" — i.e. he believes we still test the conditions severally, and asks us to confirm the recurrence is new-in-frame or point him to where we already have it.
- **AEH measurements:** independent implementation of `F` (canaries: `F(1,1) = (1,1)`; the block of 7 exits at 13); 3000 real orbits, fixed seed, bottom-regime cut at `2^20` and again at `2^30` as a control. Our exact values reproduce: `(1, odd) → d_next = 1` in 15,515 of 15,515 transitions; `(5, odd) → P(d_next even) = 0.6626 ± 0.0074` against exact `2/3`; `P(s = j)` tracks `2^{−j}` with a small `s = 1` excess "of exactly the small-starting-values type your calibration documents". New: the class-level transfer matrix built from those orbits has `|λ₂| ≤ 0.06` at both cuts — spectral gap ~0.95, "the class chain mixes in a single step". Declared artifact: drift-per-step `−0.33/−0.36` bits against the theoretical `−0.415`, attributed to survivorship bias from the cut itself — "flagged, not sold". His own scope limit: all of it is the generic face; a spectral gap says nothing about exceptional orbits.

**Main-session pre-check (verify, don't trust):** the paper's class-skeleton sentence (`sec:aeh`) does state both exact values he reproduces, and the Discussion's direction two is transfer-operator analysis of the exact window chain — his targets are real. Proposition 12.6.1 does conclude "`q` divides all `p` numbers `R_r`" severally, and the searches do test per-rotation — his reading of our record looks right. The sharp antecedence tension: `reverse.md` Lemma 14.15.9.2 (merged 2026-07-17, essentially simultaneous with his letter) proves the rotated fixed points are **one affine orbit with one shared denominator** — which smells like the mirror-frame face of his recurrence — and Corollary 14.15.9.4(1) proves `v₂(3^{m_i} q*_i − 1) = r_i` exactly, which smells like his auditor's ghost-alignment identity under the frame dictionary. Adjudicating both precisely is the heart of this brief.

**Stopping-rule compliance:** this is verification of a correspondent's claims plus antecedence adjudication against our own record — not a cycle search, not a divisibility-based exclusion attempt, not an equidistribution proof effort, not transfer-operator theory development. The cycle front stays parked; AEH proof effort stays parked. The collapse corollary in particular is an *invitation* to cheap divisibility campaigns — declining that invitation is part of the brief.

## Queue, in order

1. **Verify the transport recurrence, fresh code.** `experiments/merle_round3_check.py`, importing nothing from `staircase_allp.py`, `merle_pincer_check.py`, `periodic_shelf.py`, or any other committed script. Own implementation of 12.6.1's `R_r` (exact integers). First pin the index conventions: his `m_r, s_r, σ_r` against our 12.6.1 (`M_t = Σ_{j>t} m_j`, `S_t = Σ_{j<t} σ_j`, rotation order starting at `r`) — derive the correctly-aligned form of the recurrence under our conventions and state it; if his stated indices are off by one or a rotation under our reading, record the corrected form explicitly rather than harmonizing silently. Then check exactly, over (a) ≥ 500 random profiles `(m_t, s_t)_{t<p}`, assorted `p` up to ~12, **no closure assumption** (his "every profile" claim), and (b) the recorded 12.8.6.4 instances including the p = 22 (`n = 25217`) and p = 7 configurations printed in `briefs/merle-pincer-check-findings.md`: the recurrence itself; `q | R_0` iff `q | R_r` for all `r`; `gcd(q, R_r)` rotation-invariance; and the unit-transport form `2^{σ_r} R_{r+1} ≡ 3^{m_r} R_r (mod q)` with `gcd(q, 6) = 1` stated with its one-line reason (`q` odd as even − odd; `q ≡ (−1)^K mod 3`). Every pass/fail decision exact.

2. **Antecedence adjudication — the item his letter actually asks of us.** Three parts, each with the evidence quoted or computed, none smoothed:
   - (a) **The cycles frame.** Confirm (by reading, with quotes) that `cycles.md` nowhere states the recurrence or the collapse: 12.6.1 derives `q | R_r` for all `r` from closure, severally; 12.5.4/12.7/12.8's searches test rotations independently. Also state what 12.6.1's *proof* already contains implicitly (the one-step unrolled identity is the recurrence's parent) — the honest reading of "implicit vs stated".
   - (b) **The mirror frame.** Establish the exact relationship to Lemma 14.15.9.2. Conjecture to check (numerically and by a short derivation): under the seam dictionary (cycle profile `(m_t, s_t)` ↔ periodic word letters `(m_i, r_i)`), the rotated fixed points satisfy `y*_r = −R_r/(q·⋯)` up to the bookkeeping factor `3^{a_{r−1}}` — i.e. the affine orbit equation `y*_{i+1} = α_i y*_i + β_i` (14.15.9.2(1)), multiplied through by the denominator, **is** the transport recurrence, and 14.15.9.2(2) (one shared reduced denominator `q`) **is** the gcd-invariance corollary in reduced-fraction form. Either exhibit the dictionary explicitly (state it as a displayed identity, verify it exactly on the instance grid of 14.15.9's Setup — the 20 probe words + 3 integer words — and on random profiles) or, if the correspondence fails or covers less than his claim (e.g. his `q = 2^K − 3^n` vs 14.15.9's *reduced* denominator `q | 2^{S_P} − 3^{M_P}` — these differ exactly when the fraction reduces; find and report a profile where they differ, if one exists), report precisely what is and is not covered. The verdict wanted: one of "equivalent to 14.15.9.2 under an explicit dictionary", "strictly contains it / is contained by it, in this precise sense", or "independent content", with proof-level care — this feeds a priority/credit answer in the author's reply.
   - (c) **His auditor's identity.** Same adjudication for `v₂(3^m w − 1) = s` against Corollary 14.15.9.4(1) (`v₂(3^{m_i} q*_i − 1) = r_i`): identify what his `w` and `s` must be for the statement to be true (reconstruct from context: it arose from his p = 7 "local-global defect", our frame's block step), verify the identity fresh on random profiles, and state whether it is 14.15.9.4(1) under the dictionary or something adjacent. Timing fact for the record, stated flatly: 14.15.9 merged 2026-07-17; his letter is dated within a day; the findings file states the mathematical relationship and the dates — credit language is the author's call, not yours.

3. **What the collapse buys in our pipeline, precisely.** Separate the per-rotation tests by kind: divisibility `q | R_r` (collapses to one test if item 1 confirms) versus size `q ≤ R_r` (does **not** collapse — `R_r` varies across rotations; confirm on recorded instances that the worst-margin rotation is not constant, i.e. exhibit an instance where size passes at one rotation and fails at another — the pincer findings' pre-correction tables are full of candidates). State flatly what changes where: 12.5.4's "eleven divisibility tests" (which of the eleven were rotation-duplicates of one condition?), the 12.7/12.8 survivor searches (their binding per-rotation test is size, not divisibility — so the practical speedup claim is bounded; say by how much, measured or counted, no benchmark campaign). His Lean-obligation point is his own side's to develop; note it without comment.

4. **His two corrections, verified where checkable, recorded at his stated weight where not.** (a) The ghost-alignment identity: item 2(c) covers the mathematics; additionally verify "carries no information" in the operational sense he means (the identity holds for *every* profile, so it cannot distinguish cycle-admissible from generic — one sentence once the identity is confirmed). (b) The KS correction: his `D` and instance count are computed from his data, which we do not have — do not attempt to reproduce the number. What we *can* check from our side: his deduplication reason, "`c = 1` and `c = 2` give the same `u`" — on our own instances where both crash depths produced configurations (the pincer findings' calibration tables), compute the distance statistic `u = min(R_r mod q, q − R_r mod q)/q` per rotation for the `cd = 1` and `cd = 2` configurations of the same `n` and report whether the profiles are identical, and why or why not (the two crash depths give different `(m, s)` profiles — if `u` nevertheless coincides, that is a small finding; if it differs, his deduplication claim describes his construction, not ours — report which). Record his corrected values (`D = 0.1525`, ~34 independent) as the ledger-facing numbers on his key, at the weight he assigns.

5. **Turn our key on L4 — the AEH cross-verification.** Fresh code (`experiments/merle_aeh_key_check.py`), importing nothing from the committed AEH scripts (`aeh_*.py` or relatives). (a) Implement `F` independently from `spine.md` §9's laws; confirm his two canaries against direct computation (`F(1,1) = (1,1)`; the block of seed 7 exits at 13). (b) Confirm the two exact skeleton values are theorems of our record (cite the paper's `sec:aeh` sentence and the owning spine/stage law numbers — locate and cite, do not re-prove). (c) Reproduce his protocol at his scale, no bigger: ~3000 orbits from large uniform starts, fixed recorded seed, bottom-regime cut `2^20` with a `2^30` control; measure `(1,odd)` and `(5,odd)` transition frequencies and `P(s = j)` vs `2^{−j}`; then build the class-level transfer matrix from observed class-to-class transitions and compute its spectrum (floats are fine here and only here — this is a measurement, not a proof; label it so). Report our `|λ₂|` at both cuts against his `≤ 0.06`, and the gap. (d) The drift artifact — the one place his letter flags its own number: reproduce drift-per-step under his cut protocol (expect something like his `−0.33/−0.36` against theoretical `−0.415` — state the theoretical value's identity, `log₂ 3 − 2` per block under `π_k`, citing the owning law), then apply **our own calibration methodology** (the fixed-horizon, unweighted, no-stopping-rule protocol of aeh.md's decisive test) to the same orbit population and report whether drift moves to `−0.415` within error. That is a clean test of his survivorship-bias attribution: confirmed, refuted, or partial — his flagged artifact deserves the same two-key treatment as a headline claim. (e) One sentence confirming his scope statement (generic face only; exceptional orbits live in the distributional-to-pointwise gap) is exactly our own framing (aeh.md front matter, README stopping rules) — agreement recorded, no new framing invented.

6. **Record.** Everything into `briefs/merle-round3-check-findings.md`: the recurrence verdict with the convention-aligned statement; the three-part antecedence adjudication with the explicit dictionary (or its precise failure); the collapse-payoff paragraph (divisibility yes, size no, with the instance); the corrections record; the AEH key results (canaries, exact-value confirmations, spectrum at both cuts, the drift-attribution verdict) with seeds, orbit counts, ranges, dates; and drafted-but-NOT-applied proposal lines for any wiki text that would change if the author accepts the outcome (candidates: a remark under 12.6.1 recording the recurrence/collapse with its antecedence status and credit per the author's decision; an aeh.md line recording the external replication and the spectral measurement at measured grade). No wiki page is touched. The findings file is reply-drafting material: write its summary section so the author can lift verdicts directly (register flat, no excitement).

## Success / stop criteria

- **Floor:** items 1–3 (the recurrence verdict and the antecedence adjudication are what his letter blocks on) + item 6's record.
- **Primary:** all six items.
- **Stop:** after item 6, stop. Explicitly forbidden: any divisibility-based cycle-exclusion attempt (the collapse makes them cheap; the front is parked — cheapness is not an idea, and README's stopping rule stands); any new construction or search campaigns; any transfer-operator *theory* (spectral measurement at his scale is the ceiling; no operator analysis, no mixing-time proofs); any Lean work (his side's artifact); any PractRand-scale statistics. Off-brief observations: log in the findings file, stop.

## Rules (non-negotiable, from AGENTS.md)

- Fresh code for every independent check, as named per item; exact integer/Fraction arithmetic for every pass/fail decision; floats only for the spectrum and display columns, labeled.
- Record what was checked, ranges, seeds, dates, inline in the findings file.
- Register norm: flat. His claims are his until both keys turn; disagreements are findings, recorded exactly, not smoothed; agreements are stated with their evidence, not celebrated.
- Work on branch **`merle-round3-check`**, commit per item, do NOT merge — the main session reviews and re-runs before merging; the author drafts and sends the reply (standing convention).
- No scope expansion.

## Definition of done

The recurrence verified (or corrected) with conventions pinned; the antecedence question answered at proof-level precision with the 14.15.9.2/14.15.9.4 dictionary explicit; the collapse-payoff accounting; the crash-depth `u`-coincidence check; our key turned on L4 with canaries, exact values, both-cut spectra, and the drift-attribution verdict; the findings file complete with unapplied proposal lines; clean per-item commits on `merle-round3-check` with both experiment scripts passing.

## Appendix A: the letter, verbatim (pasted by the author, 2026-07-18)

> Dear Ben,
>
> "Each stack broke one of its own claims on the other's data within one exchange." I have read that sentence five times. It happened in the first real round, and I don't think either of us will find a cleaner argument for the ledger than the one the ledger just wrote about itself.
>
> So p=22 is closed — and you closed it by refuting me. My "-3/-4 bit recovery threshold" was never measured; it was a guess extrapolated from p=21 and p=23. Your real algorithm, at honest budgets, recovers -7.86 bits in 13 moves. My cause was right (a genuine availability hole at p=22's scale, the shadow of the partial quotient 23); my threshold was wrong; your generator settles it. Good outcome, taken happily. And on your question: my n were heuristic darts, not generator output (25217 sat next to the scale target L^22 = 25143; 31202 was just 2 x 15601). Your strict grid is the reference; your six are the real objects.
>
> Now the one thing from our side I want to put real weight on — because it survived my own red-team pass when other things did not, and because I think it is yours to confirm or to claim. A transport recurrence:
>
> R_{r+1} = (3^{m_r} R_r + (2^{s_r} - 1) q) / 2^{sigma_r}
>
> an exact identity for every profile (not only staircases; verified on 200 random ones, 0 failures). Reduce it mod q: the (2^{s_r} - 1) q term vanishes, so R_{r+1} = (unit) x R_r (mod q), since 2 and 3 are both invertible mod q. The corollary is sharp: q | R_0 iff q | R_r for all r, and gcd(q, R_r) is rotation-invariant. The p divisibility conditions of a cycle are, exactly, one condition.
>
> I went looking in your record before claiming this, and it reads as if you still test them severally — cycles.md enumerates "q | R_0, R_1, R_2" at p=3 as three checks, and the survivor searches carry a per-rotation divisibility test. If I am not missing a place where you have already collapsed them, this buys something concrete: one check instead of p, at every period, and a much smaller Lean obligation — pure integer arithmetic, the kind of thing the kernel settles without argument. If it IS somewhere in your record, tell me and it stays folklore with your name on it. If it is new-in-this-frame, it is the natural founding lemma of the ledger, and I would like to formalize it first on my side, with the Lean artifact attached — a load-bearing claim that passes the kernel key cleanly.
>
> Two honest corrections to carry alongside it, same rule as always. The "local-global defect" I sent is generic, exactly as you said — my auditor went further and proved the 2-adic ghost alignment v2(3^m w - 1) = s is an algebraic identity, true for every profile, so it is not a test and carries no information; the real content is your uniform-distance framing, which we reached from both sides. And that distance statistic was softer than I dressed it: my KS number was miscomputed (true D = 0.1525, not 0.136) and my "62 instances" were about 34 independent ones (c=1 and c=2 give the same u). The uniform reading survives a correct threshold, but at ~34 points, not 62. The ledger should carry it at that weight.
>
> One more thing, freshly turned: we put both keys on a third section of your paper — the AEH class skeleton. An independent implementation of F (canaries: F(1,1)=(1,1); the block of 7 exits at 13), 3000 real orbits, fixed seed, bottom-regime cut at 2^20 and again at 2^30 as a control. Your two exact values reproduce: (1,odd) -> d_next = 1 in 15,515 of 15,515 transitions; (5,odd) -> P(d_next even) = 0.6626 +/- 0.0074 against your exact 2/3. P(s=j) tracks 2^-j with a small s=1 excess of exactly the small-starting-values type your calibration documents. Then we did the thing your Discussion lists as direction two: built the class-level transfer matrix from those orbits and took its spectrum — |lambda_2| <= 0.06 at both cuts, a spectral gap of ~0.95: the class chain mixes in a single step. One artifact declared rather than smoothed: our drift-per-step lands at -0.33/-0.36 bits against the theoretical -0.415, and we attribute it to survivorship bias from the cut itself — flagged, not sold. And the honest scope, before you say it first: all of this is the generic face. A spectral gap says nothing about exceptional orbits; the cycles live exactly in the distributional-to-pointwise gap. Its value is a measured number where the paper has a computable skeleton — candidate L4, at whatever grade your side's key assigns it.
>
> Your three-faces theorem — reverse.md 14.15.3-6 — is the thing itself. "A bi-infinite block-itinerary is realized by an integer iff its 2-adic and 3-adic limits coincide at a positive integer" is the exact statement my title was reaching for and could not write; "the classical negative cycles reappear as diagonal points of the wrong sign" is the sentence I will be quoting for a year. The note has its theorem, and it is yours.
>
> Yes to the repo, yes to the protocol, yes to co-editing it as exercise one — L1 reading "corrected, both directions" is exactly the founding precedent it should be. I will accept the GitHub invitation on the handle attached to this address. Ledger as I see it: L1 corrected-both-directions; L2 and L3 both keys turned; L-A1 the transport recurrence (pending your antecedence check, then my Lean artifact); L4 the AEH cross-verification and class spectrum (pending your key). Independent stacks, two keys on load-bearing claims, errors kept with their refutation data — we have now each supplied one of those errors, which is the best possible start.
>
> Warmly — and tell your collaborator mine says the same back: your realization-height is the object my delta8 was the shadow of,
>
> Eric
