# Brief: round-15 review window — verify PR #5 (L-A11, the Sturmian word; the F₂[x] fact; Knight 2025; the L-A2 reduction claim), prepare the review — for a delegated session

**Context required before starting (in order):** `README.md` (binding stopping rules), `AGENTS.md`, `HANDOFF.md` item 1 (its live-conditions paragraph is STALE on PR #4 — this brief's Provenance is current; fixing it is Queue 8), `briefs/merle-round14-review-brief.md` and `briefs/merle-round14-review-findings.md` (the previous window: the pattern, the L-A10 objects, how his artifacts are run), `briefs/merle-la8-t1-check-findings.md` (the L-A8 seam chain — L-A11 is a claim about it), the L-A2 findings file (locate with `grep -l "L-A2" briefs/*findings*`; the repeated-word gcd law — the Knight reduction is a claim about it), `briefs/merle-round13-review-findings.md` §6.4 and §9 (the grading register and the review-correction pattern).

## Provenance

PR #4 (round 14) was **merged by Merle 2026-09-12 17:47 UTC**; shared repo `macindoe/one-obstruction-three-faces` HEAD is **`3d07de7`**. One hour later he opened **PR #5 — round 15**, branch `round-15`, base `3d07de7`, head **`b916c1a`** (amended once before any review, per his own PR comment). No review posted; ours is the second key per PROTOCOL §13. Three files, `+175/−4`: `LEDGER.md` (+26, the new entry **L-A11**, one key, ours invited), `briefs/merle-breach-campaign-map.md` (+66/−4), `rounds/R15-merle.md` (+83, the letter — "a delivery, not a reply"). His artifacts, public in his Lean repo `ericmerle3789/one-obstruction-three-faces-lean`: `experiments/run_123.py` at **`fcd35d1`** (30 checks, 0 failures, his figure) and `run_124.py` at **`3b7f438`** (the Christoffel corrections), plus `NAMING-run_050.md` (housekeeping: a local number collision, `run_050.py` unchanged by his account).

**His claims, as the PR body and his comment state them** (record the verbatim texts from your own clone; this list is not the record):

1. **L-A11.** In L-A8's seam chain the whole `√X` window rests on the bound `R := Σ x_min/xᵢ ≤ n`. `R` measures bounded (`≈ 12` on 11,674 real cycles); the **Sturmian word of `log₂3`** refutes any sharpening: admissible at every length, `R ≥ k/2`, `R/k → 1/(2 ln 2)` exactly. Corrected before sending: the word has `⌈k·log₂3⌉ − k ≈ 0.585k` rise-blocks — the most fragmented word, outside every circuit theorem (Hercher's `m ≤ 91` passed at `k = 156`). Negative control: the one-block word has `R ≈ 7`, bounded.
2. **F₂[x].** Collatz over `F₂[x]` is a theorem (Hicks–Mullen–Yucas–Zavislak, AMM 115 (2008); Alon–Behajaina–Paran, arXiv 2401.03210): a rise adds exactly one to the degree, the size face closes for free, the theorem is a finite cycle check. Corrected: maximal stopping times at `d = 14, 16` are `43, 51` exhaustive (his journal's sampled `37, 39` were wrong).
3. **§167:** `corrsum mod d` is Poisson(`C/d`) to 0.1 % at `k ≤ 15`, residue 0 undistinguished. **§169** (scope note under L-A2): the repeated-word law retires `5.1·10⁻⁸` of admissible words at `k = 24`, none at half the lengths; exact Möbius counts `5,005` and `792` at `k = 20, 24`. **§175:** the second-witness criterion, added to the map as its reading key.
4. **The comment (head `b916c1a`).** Knight, *Discrete Math.* 349(3) (2025) 114812, "Collatz high cycles do not exist": the cycle whose parity vector is the upper Christoffel word is never integral, by the reversal symmetry of Christoffel words, no Baker input; he re-verified on all 518 coprime `(S,k)`, `k ≤ 60`. The Sturmian word is Knight's word circularly for `k = 2,3,4,5,8,10,13,15,17,22,27,29` (of `k ≤ 40`) and `200`, not for `7,11,14,100,156,1000`; `R ≥ k/4` exactly on Knight's word, `≥ k/2` measured to `k = 300`; and **"your L-A2 reduces every non-coprime `(S,k)` to his case"**, so the balanced cycle is excluded at every length. Two of his canaries fired and are corrected in `run_124.py`: the exact constant `1/(2 ln 2)` holds for the irrational-slope word only; on the Christoffel word `R/k` depends on `ε = S − k·log₂3`. Map additions: Cobham–Semënov in logic (a Jan-2026 preprint whose witness is the L-A10 residue `2^k − 1 → 3^k − 1`), Stérin–Woods's base converter, López–Stoll's Sturmian-to-2-adic bridge, and Furstenberg's *intersection* conjecture proved (Shmerkin; Wu; *Annals* 2019) with no bridge to a cycle — the zero-entropy measure rigidity still open.

**Standing policies for this window, decided in advance — do not re-open them:**
- **Tooling disclosure (the author's policy, 2026-09-16):** his PR body ends with a Claude Code footer. It is not recorded anywhere on our side — not in the findings, not in HANDOFF, not in the review. Credit lines are a name and a date.
- **Calibration:** a correspondent's result is checked against the founding premise of the page it lands on, not against a grep. Nothing is called "significant", "publishable" or "major"; grades are the ledger's grades. His four self-corrections are *claims* and are re-verified like everything else, not accepted as fixes.
- **A claim about one of our entries** (L-A11 about L-A8's seam chain; the reduction about L-A2) is adjudicated by reading our own record first and stating exactly what our entry does and does not rely on. If his premise about our entry is wrong, that is a finding delivered kindly, with the sentence of ours that shows it.
- **No scope expansion.** If an item cannot be verified (paywall, missing artifact), record the reason and continue to the next item. Do not re-plan the queue, do not add items, do not chase a lead past its queue entry. When the queue is done, stop.
- **Stopping rules:** verification of a negative-results delivery and one seeded entry — not a proof effort on the open condition, not a cycle search. Cycles front stays PARKED.

## Queue (linear — complete each item, record it, move to the next)

1. **State check, read-only.** Fresh scratchpad clones of the shared repo (expect HEAD `3d07de7`; `round-15` at `b916c1a`) and his Lean repo (record HEAD; confirm `fcd35d1` and `3b7f438` exist and what each contains). Archive verbatim in the findings: the PR #5 body, his PR comment, and — from `gh pr view 4` — the PR #4 merge facts (merged 2026-09-12 17:47 UTC, by whom). If anything has moved, record and continue read-only.
2. **The diff, verbatim.** Record the full text of the L-A11 entry, the map diff, and the letter (by patch reference is acceptable for the letter; the L-A11 entry is quoted in full). Note any personal content in the letter (expected none — business only; if present, it is the author's to answer and is not drafted here).
3. **L-A11 against L-A8 — the premise first.** Read L-A8 in the merged `LEDGER.md` and `briefs/merle-la8-t1-check-findings.md`. State, with the sentence quoted, whether L-A8's `√X` window does rest on `R ≤ n` as he says, or on something else. Then, with fresh code (`experiments/merle_r15_check.py`, exact integers and controlled precision, imports nothing of his), build the Sturmian word of `log₂3` at lengths up to at least `k = 300`, compute `R` under his definition (state the definition you used and where it came from), and check: `R ≥ k/2` at every length tried; `R/k → 1/(2 ln 2) = 0.7213…`; the rise-block count `⌈k·log₂3⌉ − k`; the one-block negative control (`R ≈ 7`, bounded); and whether Hercher's `m ≤ 91` block bound is exceeded at `k = 156` (define `m` as our la8 record defines it). Then run his `run_123.py` from his clone, as committed (reproduction, not verification): does it exit 0 with 30 checks, and do his printed figures match yours?
4. **F₂[x].** Verify the two citations resolve (`WebFetch` on the arXiv abstract and the AMM DOI or a reliable index; the AMM paper may be paywalled — record if so) and that they state what he says: Collatz over `F₂[x]` is a theorem, with the degree argument. Then with fresh code implement the `F₂[x]` map and compute exhaustive maximal stopping times at `d = 14, 16` — his corrected figures `43, 51`. State the map convention you used (the papers define it; use theirs).
5. **§167 and §169.** Reproduce the Möbius counts (`5,005` at `k = 20`, `792` at `k = 24`) exactly from the repeated-word law's own definition in our L-A2 record, and the fraction `5.1·10⁻⁸`. For §167, reproduce the Poisson(`C/d`) comparison at `k ≤ 15` under the definition of `corrsum` he gives (if the letter does not define it operationally, record that as a gap and do not guess beyond one stated reading). §175 is a reading key, not a claim — record it, check it against the map's own grading header for consistency, no computation.
6. **Knight 2025 and the L-A2 reduction — calibrate.** Verify the citation resolves and the abstract says what he says (Christoffel/balanced parity vector, no Baker input). With fresh code, confirm on all coprime `(S,k)` with `k ≤ 60` that the upper-Christoffel-word cycle is non-integral (count the pairs — his figure 518). Check his circular-equality list (Sturmian vs Christoffel) at the named `k`. Then the reduction claim: read our L-A2 entry and its findings file and state exactly what the repeated-word gcd law says; determine whether "every non-coprime `(S,k)` reduces to the coprime case" is (a) literally L-A2's statement, (b) a corollary of it that L-A2's own proof gives, or (c) a new claim needing its own proof. Write the adjudication out. Run his `run_124.py` as committed and record its output against his description of the two canaries.
7. **The map additions.** For each of Cobham–Semënov (identify the Jan-2026 preprint), Stérin–Woods, López–Stoll, Shmerkin and Wu (*Annals* 2019): verify the citation exists and that his one-line characterization matches its abstract. No deeper reading. Record what could not be resolved.
8. **Paperwork, drafted — nothing sent, nothing pushed.**
   - *The review draft*, in the register of `briefs/merle-round13-review-findings.md` §7.1: verified facts first, then what the review keys, then raised items. **The L-A11 key is a recommendation only** — the draft carries `[THE AUTHOR'S: the L-A11 key decision]` at the key-turn sentence with your recommendation beside it (turn / turn scoped / hold, with the reason). The L-A2 reduction adjudication goes in as a flat paragraph. Anything that failed verification is flagged, not approved.
   - *Wiki-side consequences, listed not applied:* if L-A11 or Knight bears on any wiki sentence (`cycles.md` 12.8.6, the T1 window, `open-problems.md`), list the sentence and the proposed edit in the findings. No wiki mathematics changes in this window.
   - *HANDOFF item 1*: rewrite the live-conditions paragraph and the repos paragraph to the current state (PR #4 merged 2026-09-12 by Merle; HEAD `3d07de7`; PR #5 open at `b916c1a` with this window's verdict and the prepared review; L-A11 seeded one key, our recommendation; his Lean repo HEAD and the two artifact commits). Add the L-A10 and L-A11 lines to the ledger-state list. Nothing else in the file restructured.

## Record

- `experiments/merle_r15_check.py` + committed output, canaries printed first, one documented command that reproduces the committed output exactly (`python experiments/merle_r15_check.py > experiments/merle_r15_check_output.txt` from the repo root — use `cmd /c` redirection or Python's own file write, never PowerShell `>`, which adds a BOM); per-item commits.
- `briefs/merle-round15-review-findings.md` — verbatim archives; every recomputation with its definition and derivation; the two premise adjudications (L-A8, L-A2) with our sentences quoted; the citation checks; the review draft; the wiki-consequences list; the HANDOFF summary.
- `HANDOFF.md` — item 1 only.

## Rules

- Branch **`merle-round15-review`** from your worktree HEAD. FIRST verify the worktree contains this brief (`briefs/merle-round15-review-brief.md`); if missing, `git merge main --ff-only` before anything is read, and state your base SHA in the findings.
- Read-only everywhere outside this repo: fresh clones only; `gh` reads only; no push, no comment, no reaction, no fork/issue/star/watch, no contact with anyone; web only for the citation checks in Queue 4, 6, 7.
- His scripts are run as committed, from his clone, and their output recorded — that is reproduction. Verification is your own fresh code only.
- File edits via the Edit/Write tools only — never PowerShell `Get-Content`/`Set-Content`. Run `experiments/encoding_scan.py` before your final commit and record RESULT: CLEAN.
- Long computations run in the foreground; do not end the session until your final commit exists on the branch.
- Discrepancies recorded and flagged, never disputed in prose; every defect found is a finding delivered kindly.
- No key turns, no merges, nothing sent anywhere. The main session reviews (re-runs the script) and merges; the review post is the author's. Stop after Record.
