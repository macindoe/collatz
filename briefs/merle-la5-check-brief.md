# Brief: L-A5 verify (content invariant + separation lemma) — for a delegated session

**Context required before starting (in order):** `README.md` (binding stopping rules — compliance note below), `AGENTS.md`, `HANDOFF.md` item 1 (through the key-turns-pushed state, shared HEAD `740edb3`), `briefs/merle-round8-check-findings.md` (the round-8 evidence base and conventions), `cycles.md` 12.6.1 entire (esp. 12.6.1.1 transport recurrence, 12.6.1.2 census incl. `−17`, 12.6.1.4 descent), `itinerary.md` 14.15.9–14.15.10 (the affine/fixed-point frame and letter constant his entry credits).

## Provenance

L-A5 arrived by shared-repo push (Merle, commit `9f2dbcc`, 2026-07-24), not by letter — seeded DRAFT, one key (his: Lean kernel + scripts), our key invited. No letter accompanies it yet; verification proceeds on the entry text and artifacts alone. The entry (verbatim in the shared repo's `LEDGER.md` at `740edb3`, section L-A5) claims:

1. **The content invariant** `C(P) = log gcd(q, R_0) / log |q| ∈ [0,1]`, rotation-invariant (by L-A1's corollary), `C = 1 ⟺ cycle`; credited framing: the fixed-point-denominator frame and letter constant are ours (`cycles.md`, `index.md`, `itinerary.md` `β`/`G(y)`), the normalization and landscape his; anteriority sweep of our repo on record in the entry.
2. **Measured landscape (REQ-MATH-018):** aperiodic words at chance level (max `C` 0.11–0.50 at sampled depth, tracking `q`'s factorization); repeated words `B^j` climb `C → 1` per the L-A2 law; a single-letter change at fixed `q` collapses `C` to background.
3. **Separation lemma T1/T2 (Lean, claimed kernel-verified):** for adjacent one-unit transfers at fixed `q` in `W0 = 2^{m_0} R_0` coordinates — s-transfer difference `2^{mssum(pre)}·3^{msum(suf)}·2^{m_1+s_1}·(3^{m_2} − 2^{m_2})`; m-transfer difference `−2^{mssum(pre)}·3^{m_2+msum(suf)}·2^{m_1}·(2^{s_1} − 1)`; valid at every position, boundary regular in `W0`. **Corollary:** a divisor of `q` shared by adjacent words divides the letter-scale seam `3^{m_2} − 2^{m_2}` resp. `2^{s_1} − 1`; at `m = 1` shared content is 1 — total isolation.
4. **The gloss:** "content towers have no shoulders: the only road to `C = 1` is exact repetition, and repetition is sterile [L-A4]… no *aperiodic* word reaches `C = 1`."
5. **Artifacts:** `ericmerle3789/one-obstruction-three-faces-lean` — `OneObstruction/ContentSeparation.lean` at commit `e297d9d` (T1, T2, separation_T1/T2, q_divisor_coprime; claimed kernel-3, 0 sorry, no user axioms, no `native_decide`); `REQ-MATH-018` and `REQ-MATH-019` at `9932f3f` (identities 713/713, 604/604; corollary 560/560); `REQ-MATH-020` at `e297d9d` (Lean↔Python bridge, 300/300).

**The main-session flag this brief must adjudicate (stated as a hypothesis to test, not a conclusion to confirm):** the gloss (4) appears to overreach the lemma (3). Two candidate repairs, both to be tested: (i) **scope** — the untuned `−17` cycle has a *primitive* word (`cycles.md` 12.6.1.2) with, presumably, `C = 1` (`gcd(q, R_r) = 139 = |q|`): compute it; if so, the unscoped gloss has a live counterexample pattern and any correct version needs the tuned-regime scope. (ii) **modality** — T1/T2 prove *adjacency isolation* (no high-content neighbors; no gradual road up), not *non-existence* of aperiodic `C = 1` words; in the tuned regime, "no aperiodic word reaches `C = 1`" restated exactly is the open cycle question itself (`C = 1 ⟺ q | R_0`), which the ledger's own NOTE §6 names as the residual gap. If your derivation finds the gloss *does* follow from T1/T2, that is a major finding — write out the implication with full care and flag it loudly; do not assume the flag is right.

**Stopping-rule compliance:** independent re-verification of a correspondent's seeded entry plus adjudication of its stated scope — not a cycle search, not an exclusion attempt, not an equidistribution proof effort. The cycles front stays PARKED. Registered language only; the adjudication distinguishes what is proved from what is glossed, flatly, as material for co-edit and reply — his prose is never disputed in prose, only recorded against the checks.

## Queue

1. **Shared repo + artifacts, read-only.** Fetch the shared repo (scratchpad clone or fresh); record HEAD (expected `740edb3`, ours; flag if moved again — do not prepare anything on a moved HEAD, record and continue read-only). Record the L-A5 entry text verbatim in the findings. Clone/fetch his Lean repo read-only; verify commits `e297d9d` and `9932f3f` exist; record what each contains (note both post-date `017288f` — record the commit graph order). Read `ContentSeparation.lean` and the three REQ scripts: for the Lean file, record the exact theorem statements and match them clause-by-clause against the entry's (3) — statement-level review; check the file's own axiom/scope claims (`#print axioms` output if present in his committed output, the scope header, absence of `sorry`/`native_decide` by read). Attempt `lake build` ONLY if a toolchain + Mathlib cache is already available and the build completes inside ~15 minutes; otherwise record "read, not built" honestly (the L-A1 precedent records an honest scope header — follow that practice). His scripts are read, never run as verification.

2. **Independent verification, fresh code** (`experiments/merle_la5_check.py`; imports nothing from his repos or prior check scripts; exact integer arithmetic at every pass/fail decision; canaries hand-computed and printed before sweeps):
   - (a) **The invariant.** From 12.6.1.1's conventions: verify `gcd(q, R_r)` rotation-invariance directly on random profiles (both signs, tuned and not) — the entry's "by L-A1's corollary"; verify `C = 1 ⟺ q | R_0` on knowns: trivial word (`C = 1`), the `(−5)`-shore word, **the `−17` cycle's primitive word — compute `C` exactly and record it** (this is the scope canary; expected `C = 1` with `|q| = 139`), the `p = 7` staircase seed (`C = log 7 / log q`, near 0), random words (low `C`).
   - (b) **The landscape, reduced scale, labeled spot checks:** max `C` over sampled aperiodic tuned words at 2–3 depths (his 0.11–0.50 band); `B^j` climb — compute `C(B^j)` exactly via 12.6.1.4's identity (`gcd(q_P, R_0(P)) = G_j·gcd(q_B, R_0(B))`, so `C(B^j) → 1` iff… derive the exact rate and check it against his "per the L-A2 law"); single-letter-change collapse at fixed `(n, K)` on a handful of instances.
   - (c) **T1/T2 clean-room.** Derive the two adjacent-transfer difference formulas yourself from the `R_0` definition in `W0 = 2^{m_0} R_0` coordinates — define "adjacent one-unit s-transfer/m-transfer" exactly as his artifacts do (read REQ-MATH-019 for the operational definition; record it) — and verify your derived formulas symbolically-by-evaluation: exact equality of `W0(word′) − W0(word)` against the closed forms at every position over exhaustive small grids plus random draws (target his 713/604/560 counts or better). Verify the corollary (shared divisor of `q` divides the letter seam) and the `m = 1` isolation (`3^1 − 2^1 = 1`).
   - (d) **The gloss adjudication.** With (a)'s `−17` result and (c)'s derivation in hand, write out precisely: what T1/T2 + corollary prove (adjacency separation of content); what the gloss says; whether the first implies the second (per the flag: expected no — the lemma bounds *shared* content between neighbors, saying nothing about isolated aperiodic peaks; and unscoped, `−17` is the counterexample pattern); and the exact corrected statement you would offer at co-edit (expected shape: "in the tuned regime, no aperiodic word is *connected to* `C = 1` by one-unit transfers — `C = 1` has no shoulders; existence of an isolated aperiodic peak is exactly the open condition `q | R_0` and is untouched"). If the implication DOES go through, flag per the Provenance paragraph.

3. **Record** (branch commits, per-item):
   - `experiments/merle_la5_check.py` + committed output (one commit).
   - `briefs/merle-la5-check-findings.md` — verbatim entry text; artifact review (Lean statements vs entry, built-or-read status); per-claim adjudication (confirmed / consistent-at-reduced-scale / flagged); the T1/T2 derivation written out; the gloss adjudication with the exact proposed co-edit language; flags separated from confirmations.
   - `HANDOFF.md` item 1 update — L-A5 verified per findings; owed: L-A5 co-edit + our key turn (**push-gated on the author's explicit go-ahead**, and gated further on the gloss resolution — the key must not turn on the gloss as written; the co-edit offers the corrected statement, his acceptance is his call), then the reply draft (carries: key turns pushed, solitary-7 refinement, gloss precision, L-A5 verification state; sending stays with the author).

## Rules

- Branch **`merle-la5-check`** from your worktree HEAD (verify `d74f430` or a descendant; state the base in the findings). Per-item commits; do NOT merge — the main session reviews (re-runs the script) and merges.
- Read-only everywhere outside this repo: no pushes, no shared-repo writes, no edits to his repos, no web actions beyond GitHub clones/reads of the two named repos. Any push impulse = stop and hand back.
- Discrepancies are recorded and flagged, never disputed in prose and never silently reconciled. Obstructions recorded, not forced — if the Lean file's statements don't match the entry, or your T1/T2 derivation disagrees, that is a finding.
- No reply paragraphs; no key turns; no co-edit commits (this round adjudicates; the co-edit is a separate gated round); stop after item 3.
