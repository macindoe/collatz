# Brief: the p = 22 pincer hypothesis — answering Merle's falsification question from our own stack (for a delegated session)

**Context required before starting (in order):** `README.md` (binding stopping rules — see the compliance note below), `AGENTS.md`, `cycles.md` 12.8.6 entire, `briefs/staircase-allp-brief.md` and `briefs/staircase-allp-findings.md` (the p = 22 record this brief extends), `experiments/staircase_allp.py` (the committed construction/verification script), `HANDOFF.md` item 2 (the Merle collaboration state).

## Provenance

Eric Merle's reply (correspondence, 2026-07-16, author's mailbox — not in the repo) accepts the two-key/shared-ledger collaboration protocol and contributes four items, three of which touch our ledger. The main session verified what is checkable without new computation:

- **His p = 7 re-verification matches our published record exactly** (`m = (4,7,9,15,23,35,1)`, `γ = 6.7438` vs our `6.74`, size 7/7, divisibility 0/7 — cycles.md 12.8.3). First two-key entry, both keys turned. Nothing for you to do here.
- **His local-global observation on p = 7** (the closure equation `ω·q = R_r` solvable over `R`, over `Z₂`, over `Z₃`, never over `Z`): the local parts are structurally trivial for the *entire* family — `q = 2^K − 3^n` is odd (even − odd) and `≢ 0 (mod 3)` (`q ≡ (−1)^K mod 3`) for every configuration, so `Z₂`- and `Z₃`-solvability are automatic. The genuinely new data is his **distance-to-integrality measurement**: `min(R_r mod q, q − R_r mod q)/q` essentially uniform across rotations (his range: 0.05–0.48). Item 3 below re-runs that independently (his ledger seed #3).
- **His p = 22 "Diophantine pincer" hypothesis** (his ledger seed #1) is the substantive item, and it *disagrees with our findings file* — which is the point (p = 22 is the agreed first object our stacks disagree about). His claim: the scale target `n ~ L^22 ≈ 25,143` (`L = log₂3`) falls inside the largest multiplicative hole of the "good n" grid, `(15601, 31202)`, the shadow of the giant partial quotient `23` in the continued fraction of `log₂3`; hence every in-scale candidate has `δ` 10–20× too coarse (his measurement: 9 failing rotations pre-correction, worst `−7.86` bits) while the nearest off-scale candidate (`n = 31202`, 24% above scale) pays the deformation cost (6 failing, worst `−4.80` bits) — both beyond the correction algorithm's empirical recovery threshold (`≈ −3/−4` bits). Our `briefs/staircase-allp-findings.md` item 1 says instead "the Diophantine choice of `n` was never the binding constraint at any period tested." **Both could be right** (his cause, our symptom: the hole forces bad pre-correction margins, which then *look* combinatorial), and his question is exactly the falsification test: *"which six n did your script try, and what were its worst margins?"* Our findings file does not record that data. Producing it, auditably, is this brief.
- **Transmission note:** his email arrived with mangled glyphs on some numerals. Reconstructed by the main session from scale arithmetic (flag any that your data contradicts): `p = 21, n = 15601` (3 failing, worst `−2.27`); `p = 23, n ≈ 39468` (4 failing, worst `−3.77`); `p = 22` in-scale `n ≈ 25217` (9 failing, worst `−7.86`); `p = 22` off-scale `n = 31202` (6 failing, worst `−4.80`).

**Stopping-rule compliance (same footing as `briefs/staircase-allp-brief.md`):** this is diagnostic reporting on an existing floor-grade construction plus independent re-verification of a correspondent's measurements — not a per-period cycle search, not a divisibility-based exclusion attempt, not an equidistribution proof effort. The cycle front stays parked throughout.

## Queue, in order

1. **Answer the falsification question, from the committed script's own logic.** Instrument (do not alter the committed copy of) `experiments/staircase_allp.py` to report, for `p = 22`: every candidate `n` its Diophantine chain supplies within the committed search window, and for each — `n`, the scale ratio `n / L^22`, the pre-correction count of failing rotations under the base construction (12.8.6.2), and the worst margin in **bits**, using the corrected diagnostic of findings item 2 (`bit_length(R_r)` vs `bit_length(q)`, not `bit_length(q − R_r)`). Also reconstruct, as nearly as the findings record allows, the *supplementary* wider candidate set of findings item 3 (the 60-move/no-time-limit runs) and report the same columns. This is reporting what our stack did — instrumentation of the committed code is correct here, and is the one place this brief permits running it.

2. **Test the pincer hypothesis against that table, flatly.** State, with the numbers: (a) whether every in-scale candidate's pre-correction worst margin falls materially beyond the `−3/−4` bit recovery threshold (his prediction); (b) whether any candidate combined near-threshold margins with in-scale `δ` (which would refute the pincer as the complete explanation); (c) whether the committed window even contained candidates inside `(15601, 31202)` other than near-hole edges. Also cross-check his two calibration rows with **fresh code** (not the committed script): `p = 21, n = 15601` and `p = 23, n ≈ 39468` — base-construction failing-rotation counts and worst margins, compared against his `(3, −2.27)` and `(4, −3.77)`. Agreement or disagreement, stated exactly; a disagreement is a finding, not a failure.

3. **Independent re-run of his ledger seed #3 on our p = 7 instance** (fresh code): the trivial local solvabilities stated with their one-line structural reasons (`q` odd, `3 ∤ q` — for every configuration, not just p = 7); `Z`-solvability 0/7 (this is 12.8.3's own divisibility record — cite it); and the distance-to-integrality profile `min(R_r mod q, q − R_r mod q)/q` for all seven rotations, compared against his reported range `[0.05, 0.48]`.

4. **The continued-fraction hole claim** (fresh code): compute the continued fraction of `log₂3` far enough to confirm the partial quotient `23` and the convergent denominator `15601`; enumerate the "good n" grid as our own lemma 12.8.6.1 defines it (correctly-signed semiconvergent runs) in `[10^4, 10^5]`, and report the largest multiplicative gap — is it `(15601, 31202)` as he states, under our definition? (Note `31202 = 2·15601`; whether that value is in *our* grid depends on our run/step conventions — report what our definition gives, and if it differs from his endpoints, record the difference precisely rather than harmonizing.)

5. **Record.** All of it goes in `briefs/merle-pincer-check-findings.md`: the p = 22 candidate table, the pincer verdict (supported / refuted / partially — with the exact sense), the two calibration cross-checks, the seed-#3 re-run, the CF-hole check, and one paragraph reconciling (or precisely opposing) findings item 1's "never the binding constraint" sentence with the outcome — if the pincer holds, that sentence needs a scoped correction (the *choice among available* `n` was not binding; the *availability of in-scale* `n` was), which you draft as a proposed erratum line for cycles.md 12.8.6 but do NOT apply (main-session decision). No other wiki page is touched.

## Success / stop criteria

- **Floor:** items 1–2 (the falsification answer and the verdict) + item 5's record.
- **Primary:** all five items.
- **Stop:** after item 5, stop. Explicitly forbidden: any attempt to *close* p = 22 (new construction variants, bigger searches beyond reproducing the recorded budgets — the front is parked and the untried variants of findings item 5 stay untried); any cycle-exclusion work; any equidistribution proof attempt; any exploration of his "R mod q stays far from 0" proposal beyond item 3's measurement (that proposal is note-scoped future work, not ours to develop unilaterally). Off-brief findings to the same findings file; log and stop.

## Rules (non-negotiable, from AGENTS.md)

- Fresh code for every independent check: `experiments/merle_pincer_check.py`, importing nothing from `staircase_allp.py` (item 1's instrumented run is the sole, clearly-labeled exception, and the instrumented copy lives inside your script or a scratch file, never overwriting the committed one). Exact integer arithmetic for every pass/fail decision; margins in bits may use bit_length comparisons per findings item 2.
- Record what was checked, ranges, seeds, dates, inline in the findings file.
- Register norm: flat. His hypothesis is his until both keys turn; our findings sentence is ours until the data says otherwise; disagreements are recorded, not smoothed.
- Work on branch **`merle-pincer-check`**, commit per item, do NOT merge — the main session reviews and re-runs before merging, then drafts the reply (sending stays with the author, standing convention).
- No scope expansion.

## Definition of done

The p = 22 candidate/margin table exactly answering "which n, what worst margins"; the pincer verdict with its evidential basis; both calibration cross-checks; the seed-#3 independent re-run with the distance profile; the CF-hole check under our own grid definition; the findings file complete with the proposed (unapplied) erratum line if warranted; clean per-item commits on `merle-pincer-check` with `experiments/merle_pincer_check.py` passing.
