# Brief: L-A7 μ-source check + replication (torsion ruler) — for a delegated session

**Context required before starting (in order):** `README.md` (binding stopping rules), `AGENTS.md`, `HANDOFF.md` item 1, `briefs/merle-round8-coedit-findings.md` part A (the (B) constants: `c_gen = 0.0793186`, `c_strat = 0.2667875`, `α* = 0.3747344` — derivation and meaning), `cycles.md` 12.6.1.5 (asymptote remark), `briefs/merle-la6-check-brief.md` Provenance (the sibling entry this one caps — read for context; its verification is a separate session, not yours).

## Provenance

L-A7 arrived by shared-repo push (Merle, commit `81431c7`, now HEAD, dated 2026-07-24), seeded DRAFT, one key (his; self-graded "theorem-grade modulo two published ingredients"), our key invited. **He explicitly flags the load-bearing external ingredient for re-check:** "effective irrationality measure of `log₂3` (exponent `μ = 5.125`, Salikhov as documented in the Merle v2 corpus §5 — *primary source to be re-checked before any publication*)." That re-check is this session's main job; the algebra around it is elementary and gets replicated fresh. Entry claims (verbatim text to be recorded from the clone):

1. **Ingredients.** The (B) counting constant `c_gen = 0.0793186` (finite places) fused with an effective irrationality measure of `log₂3`, exponent `μ = 5.125` attributed to Salikhov via his own v2 corpus §5.
2. **Statement (verified `n ≤ 2000`, canary-anchored):** for the best north cell at scale `n`: `R(n) = log₂(#words) − log₂ q ≤ −c_gen·n + (μ−1)·log₂ n + C₀`, with `C₀ = −5.77` exhibited (max at `n = 2`); ingredient-inequality slack `min_n ε_n·n^{μ−1} ≈ 14.5`. Canaries: anchors `(5,8) → q = 13`, `(7,12) → q = 1909`; `n ≤ 14` word-budgets reproduce the census budgets (`6.17` north / `3.41` south).
3. **Consequence:** total ticket mass beyond `n = 600` provably `< 5.2·10⁻⁴` (both shores; word units upper-bound necklace units); below `n = 600` exact finite computation. "The kiosk provably closes."
4. **Honest scope (his):** bounds the *expectation*, not the truth — the model→certainty step remains the ×2×3 gap; bound slack at small `n`; crosses below one ticket near `n ≈ 550`.
5. **Artifacts:** `experiments/test_REQ-MATH-029_regle_de_torsion.py` + committed output, predictions written before measurement, in the shared repo.

**Stopping-rule compliance:** a literature verification plus replication of an elementary bound — not a proof effort on the open condition, not a cycle search. Cycles front stays PARKED. The entry itself concerns the *model's* tail mass; keep that framing exact.

## Queue

1. **Shared repo, read-only.** Fresh clone (scratchpad). Record HEAD (expected `81431c7`; if moved, record and continue read-only). Record the L-A7 entry verbatim in the findings. Read `test_REQ-MATH-029_*.py` + committed output for operational definitions ONLY (what is `#words` per cell, what is "best north cell", what exactly is `ε_n`, where μ enters, what "word units" sums); record them. His script is read, never run as verification.

2. **The μ source (the main item; web access is granted for THIS item, literature only).** Establish, with primary or best-available sources, the state of effective irrationality measures for `log₂ 3 = log 3 / log 2`:
   - (a) What Salikhov actually proved, for which constant, in which paper (his celebrated effective measures are for `log 2` and for `π`; whether he — or anyone — published an effective measure for `log 3/log 2` or equivalently for the pair `(log 2, log 3)`, and with what exponent). Chase the actual attribution: candidate literature includes Rhin, Rhin–Viola-style methods, Bugeaud, Wu–Wang, linear forms in two logarithms (Laurent–Mignotte–Nesterenko / Gouillon), and the Collatz-specific applications (Simons–de Weger's cycle work uses exactly such bounds — their source is the standard reference chain for this constant). Record: the best published *effective* exponent for `log₂3` you can source, its paper (author, year, journal/arXiv, theorem number), and its exact statement including any constant and validity range (`|log₂3 − p/q| > c/q^μ` for `q ≥ q₀` — the `c` and `q₀` matter for an *effective* tail bound, not just the exponent).
   - (b) Adjudicate `μ = 5.125`: supported by a primary source (which?), unsupported (nearest real value and source), or supported-but-misattributed. Note: the natural appearance of an exponent near 5.125 elsewhere in the literature (e.g. measures for other logarithms) should be checked — a transplanted exponent is a classic citation slip. Also record what the weaker-but-certain fallback is (an effective bound from linear forms in two logarithms is guaranteed to exist — with what exponent/constant), and what μ that fallback implies for item 3's sensitivity table.
   - (c) "Merle v2 corpus §5" is his own document, not in our repos; do NOT request it — adjudicate from the primary literature. If the attribution can't be pinned, the verdict is "unsourced pending his §5 reference," stated flatly.
   - Web use is bounded: searches and paper/abstract fetches on this question only; no other web actions; no contact with anyone.

3. **Replication, fresh code** (`experiments/merle_la7_check.py`; imports nothing from his repos or prior checks; exact integer arithmetic for all word counts and `q` values; controlled precision (e.g. `mpmath` or interval-safe floats with stated guard digits) for logs — every pass/fail decision must be robust to rounding, state how). Treat **μ as a parameter**:
   - (a) Derive the chain yourself before computing: how an irrationality measure `|log₂3 − K/n| ≥ c/n^μ` lower-bounds `log₂|q|` for `q = 2^K − 3^n` at the best cell (write the two-line derivation out in the findings: `|q|/3^n = |2^{K − n·log₂3·…}| …` — make it exact, including where the `(μ−1)` vs `μ` exponent lands and where `c` enters `C₀`), and how `log₂(#words)` compares against `c_gen·n` (tie to the round-8 (B) derivation). Then verify `R(n) ≤ −c_gen·n + (μ−1)·log₂n + C₀` on `n ≤ 2000` at μ = 5.125: reproduce `C₀ = −5.77` (max at `n = 2`), the slack `≈ 14.5`, and the canaries `(5,8) → q = 13`, `(7,12) → q = 1909` (hand-compute both canaries first and print them).
   - (b) Reproduce the consequence: tail beyond `n = 600` `< 5.2·10⁻⁴` in word units, both shores; the crossing near `n ≈ 550`; the `n ≤ 14` word-budget cross-check (`6.17` north / `3.41` south — reconcile these with the L-A6 necklace budgets' word-unit counterparts; they are different units, record the ratio).
   - (c) **Sensitivity table (the deliverable that makes item 2 actionable):** recompute the tail-mass bound and crossing scale for the μ values item 2 actually sourced (e.g. the true best-published exponent, the linear-forms fallback, and 5.125 for comparison), including the effect of the source's constant `c` and validity floor `q₀` on `C₀`. State plainly whether the entry's headline (`< 5.2·10⁻⁴` beyond `n = 600`) survives under the *sourced* values, and what the corrected headline is if not.

4. **Record** (branch commits, per-item):
   - `experiments/merle_la7_check.py` + committed output (one commit; canaries printed first).
   - `briefs/merle-la7-mu-check-findings.md` — verbatim entry; operational definitions; the μ-source adjudication with full citations (this section is the round's citable record — precise enough for the reply and for any later publication footnote); the derivation written out; replication results; the sensitivity table; the key recommendation (expected shape: the *algebra* confirmable, the *grade* conditional on the sourced μ — "theorem-grade modulo ingredients" adjudicated to exactly which ingredients at which sourced values; co-edit offer text if the attribution needs correcting).
   - `HANDOFF.md` item 1 update — L-A7 verification state per findings; owed next per the standing pattern. Do not touch other items (coordinate note: `merle-la5-closure` and `merle-la6-check` sessions run in parallel and also touch item 1 — keep your edit scoped to the L-A7 lines to keep the merge clean).

## Rules

- Branch **`merle-la7-mu-check`** from your worktree HEAD (verify it contains this brief; state the base SHA in the findings). Per-item commits; do NOT merge — the main session reviews (re-runs the script) and merges.
- Read-only everywhere outside this repo: no pushes, no shared-repo writes; web access ONLY per item 2's bounds. Any push impulse = stop and hand back.
- Discrepancies recorded and flagged, never disputed in prose. If μ = 5.125 is unsupported, that is a *finding delivered kindly in the record* — he asked for this check himself; the findings state the sourced truth and the sensitivity consequences, nothing more.
- No reply paragraphs; no key turns (recommendation only); no co-edit commits; stop after item 4.
