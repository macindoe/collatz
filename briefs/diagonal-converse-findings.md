# Off-brief findings: diagonal-converse (branch `diagonal-converse`, 2026-07-16)

One off-brief finding, logged per the brief's explicit permission ("if the negative case suggests a clean signed statement, record it as an off-brief finding, one paragraph, and stop") and not pursued further.

**A natural signed generalization, not attempted.** The admissibility-class lemma's proof (reverse.md `14.15.5.1`) uses positivity of the "present door" `z` exactly once, and only by inheritance: the base case cites `14.15.4.1`, whose own proof concludes the predecessor `y` is positive from `q=(2^r z+1)/3^m` being "a quotient of two positive numbers," which needs `z>0`. Nothing else in `14.15.5.1`'s induction step uses positivity — the affine algebra (`z=A_k y_{-k}+B_k`, the base-case congruence `z≡-2^{-r} mod 3^m`) is sign-independent, exactly as `14.15.5`(c) already found for `stratum`/`G` themselves. Dropping the positivity hypothesis and letting `z` range over all nonzero odd integers would very likely give the same lemma — one residue class mod `3^{M_n}` — among *all* nonzero odd `z`, not only positive ones (not verified here; the brief's stop line forbids extending the apparatus beyond the one reconciliation sentence, so this is recorded as a plausible direction, not a checked claim). If so, a "signed" diagonal locus — `W` integrally realized by *some* nonzero (possibly negative) odd integer iff `y₂(W)=y₃(W)` at a single such integer — would subsume both this section's positive locus and the classical negative `T`-cycles (`{-1}`, `{-5,-7}`, `{-17,-25,-37,-55,-41,-61,-91}`) as the periodic-word case, folding `14.15.5`(c)'s reconciliation into the theorem itself rather than leaving it as a boundary exception. What this would change: exactly where the sign boundary sits (a hypothesis in `14.15.4.1`'s proof, not an intrinsic obstruction elsewhere in the machinery); it would not touch the Bridge itself (deciding `y₂=y₃` for a word family is unchanged either way). Whether it is worth restating the `14.15.3`–`14.15.5` apparatus over `Z\{0}` is a scope decision for the main session, not exercised on this branch.
