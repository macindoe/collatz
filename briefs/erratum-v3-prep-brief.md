# Brief: the v2 note's erratum, packaged as a Zenodo v3 — prepared for the author's upload — for a delegated session

**Context required before starting (in order):** `README.md` (binding stopping rules), `AGENTS.md`, `HANDOFF.md` item 1 (round-12 paragraph — the author's decision 4: the published record is amended BEFORE round 12 is answered) and items 8–9 (the two discharged gates), `briefs/staircase-status-apply-findings.md` §5 (the drafted erratum — your starting text) and §§1–2, `briefs/staircase-gamma-upper-findings.md` (the constants the draft cites), `cycles.md` §12.8.6 as merged, `publication.md` (DOIs and version history), `briefs/record-defects-repair-findings.md` (the σ clause — optional item 5), HANDOFF "Known infrastructure quirks" (LaTeX builds in a sandbox temp dir; `\wnext`/`\dnext`; the PowerShell encoding rule).

## Provenance

The v2 note (paper 1, DOI 10.5281/zenodo.21421120) contains exactly one wrong published sentence: its identification of the remaining gap, superseded on both halves by the all-`p` staircase closure (merged: availability `12.8.6.1`, Construction B `12.8.6.2`, `γ` bracketed `3.683012 ≤ γ ≤ 5.140212`). An erratum correcting only that sentence is drafted at `briefs/staircase-status-apply-findings.md` §5; its stated precondition (§12.8.6 reviewed and merged) is met, and the two prerequisites HANDOFF item 6 once attached to a v3 (P1: `γ = O(1)` proved; P2: the author's same-family adjudication) are both discharged. The author has decided: **amend the published record now, before the round-12 reply.** The upload itself is the author's; this window prepares everything up to it.

The mechanism recommended to the author (he can override at upload): a **Zenodo new version (v3)** on the same concept record — one canonical object, version DOIs preserved, changelog in metadata — rather than a standalone erratum file. Prepare for that.

**Stopping-rule compliance:** record repair of one published sentence; the closure it reports passed three verification windows and review; nothing reopens the cycle front, and the correction text itself says so.

## Queue

1. **Verify the drafted correction paragraph against the merged record, number by number, before touching LaTeX.** The draft cites: `8 − 5log₂3 = 0.0751874964…` positive and smaller than the target arc; 66 consecutive integers; the arc `[0.0415, 0.1169390665…]`; the additive offset `1/(log₂3 − 1) = 1.70951` per block; `p ≥ 16` unconditional / `3 ≤ p ≤ 15` finite check / `p ∈ {2,4}` by exhibition; `γ` between `3.683012` and `5.140212`; "with candidates drawn from the whole window the same construction closes every period tested"; and the pointer "the current record is `cycles.md` §12.8.6". Check each against `cycles.md` §12.8.6 as merged and the gamma-upper findings; where the draft and the merged page differ in wording or precision (the status-apply session recorded eleven deviations from earlier drafts for exactly this reason), the merged page wins — record every deviation you make from the §5 draft with its reason. Pin "every period tested" to the exact verified ranges the record supports (end-to-end `p ∈ {3,…,26}`; construction verified through `p = 32`) rather than leaving it unquantified.
2. **Verify the published sentence verbatim.** The draft quotes the v2 note's gap sentence ("third-from-last sentence"). Confirm the quotation character-for-character against `paper/collatz-reduced-v2.tex` (and the frozen v1 in `sources/paper/` for lineage). Also confirm the note's repository pointer as published (the draft says it pins `…/blob/72ec88e/cycles.md`) — quote the sentence that carries it.
3. **Prepare the v3 LaTeX** following the repo's own v1→v2 pattern (inspect how v2 relates to v1 and where each lives; follow it — expected shape: a new `paper/collatz-reduced-v3.tex`, `paper/` conventions for any built artifacts, `sources/` untouched and immutable). Content rules:
   - The original gap sentence **stays visible**, immediately followed by the correction paragraph marked `(Correction, [DATE — the author sets this at upload].)` — failures are recorded, not deleted; this is the project's own norm applied to its published record.
   - The correction is the §5 draft as adjusted by item 1. It remains **silent on the hedge** (`thm:staircase`'s "not proved here" stays a true statement about the paper and is not touched).
   - Update the repository pointer to a pin that resolves to the settled §12.8.6: propose the current `main` SHA at your base, and mark it **CHECK AT UPLOAD** in the findings — the pinned commit must be public on wiki `main` before the record goes live (the round-9/10/11 co-edit pattern exactly; the author pushes wiki `main` first).
   - Nothing else in the note changes under this item. No new results are imported; the all-`p` closure is *reported* by the correction and *lives* in the wiki record it points at.
4. **Build the PDF** in a sandbox temp dir (the mount locks aux files — HANDOFF quirk), copy artifacts per `paper/` conventions, and verify the built PDF carries the correction where intended (page/position noted in the findings).
5. **The σ clause — a separate commit, clearly optional, the author decides at upload.** `paper/collatz-reduced-v2.tex` line ~178 carries the same σ-undefined-at-use gap repaired on the wiki (`briefs/record-defects-repair-findings.md` item (i); the correct convention `σ_j = s_j + m_(j+1)`, one clause, and the tex has a `Definition~\ref{def:reduced}` pointer the wiki lacked — check whether that pointer already covers it before proposing text). Prepare the one-clause repair as its own commit on the same branch, clearly labeled OPTIONAL, and state in the findings the honest trade-off: we told Merle the erratum corrects only the gap sentence; including a definitional clause is defensible if the changelog declares it, but it is the author's call, not yours and not ours.
6. **The upload package for the author** (in the findings, one section, checklist form): the Zenodo steps (new version on the existing concept record; which files; keep v1/v2 depositions untouched); the proposed metadata changelog text (one factual paragraph, no excitement); the canonical-citation line for the round-12 reply (v3 version DOI once minted + the concept DOI, and the flat note that the ccchallenge `Macindoe2026` entry is catalogued from v1 — the reply states the canonical DOI rather than chasing the register); and the proposed `publication.md` update block (drafted, NOT applied — `publication.md` moves after the DOI exists).

## Record

- The v3 LaTeX (+ built artifact per `paper/` conventions) in per-item commits; the OPTIONAL σ commit separate and labeled.
- `briefs/erratum-v3-prep-findings.md` — item-1 verification table (every number, its source in the merged record, any deviation from the §5 draft with reason); the verbatim published sentence and pointer; the placement decision; the CHECK-AT-UPLOAD pin; the σ trade-off stated; the upload package.
- Do NOT edit `publication.md`, `cycles.md`, `sources/`, or `HANDOFF.md`. Five windows run in parallel this round.

## Rules

- Branch **`erratum-v3-prep`** from your worktree HEAD. FIRST verify the worktree contains this brief; if missing, merge/rebase onto local `main` first; state your base SHA in the findings.
- `sources/` is immutable. `paper/` gains the v3 files only. No pushes anywhere; the Zenodo upload and the wiki-`main` push are the author's alone.
- File edits via Edit/Write tools only — never PowerShell `Get-Content`/`Set-Content` (the tex is full of characters the quirk destroys). Run `experiments/encoding_scan.py` before your final commit and record RESULT: CLEAN.
- Register norm applies doubly to a published correction: flat, calibrated, no excitement inflation; the correction states what closed, at what scope, and where the record is — nothing more.
- No reply paragraphs; do NOT merge — the main session reviews (rebuilds the PDF, re-checks item 1) and merges. Stop after Record.
