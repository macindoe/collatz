# Brief: L-A9 check (δ8 impossibility) + round-12 offers-discharge verify — for a delegated session

**Context required before starting (in order):** `README.md` (binding stopping rules), `AGENTS.md`, `HANDOFF.md` item 1 (the round-12 paragraph and the round-10/11 L-A7/L-A8 paragraphs), `briefs/merle-round12-letter.md` (the whole cover email; Part 2 intro, §5, §7, §12 calibration paragraph), `briefs/merle-la8-t1-check-findings.md` (the Hercher adjudication — the 704/1536/2048 units and the exact-vs-integral window), `briefs/merle-la7-mu-check-findings.md` §2 (the μ-source citations), `briefs/merle-r11-hygiene-check-findings.md` (Half 2 item H — the chi-squared question this round answers).

## Provenance

Merle seeded **L-A9** at shared-repo commit `78f80f0` (one hash fix on top: HEAD `7c05458`), one key (his), our key invited. **He asks, twice — once in the email, once in the entry — that the grade line be the sentence we attack hardest.** The same commit carries an offers-discharge block (round-11 offers e/f/g) and one volunteered defect nobody raised: his `0.00103` statistic was computed on a sequence that was not `log₂3` past its 385th partial quotient. Entry claims (record the verbatim text from your own clone; the summary below is not the record):

1. **The claim.** The Product-Bound chain cannot be closed by any improvement in effective Diophantine constants: the required effective irrationality exponent to close the current window (Barina `2⁷¹`; Hercher `1.375·10¹¹`) is `c* ≈ 0.9617` (`0.9622` at "the exact Barina bound `2075·2⁶⁰`"), and by Dirichlet no irrational has an effective exponent below 2 — so the route is shut for every constant, not merely `log₂3`.
2. **Deficits, for scale:** at `c = 2`, `k_max = 1.9205·10⁷` against Hercher's `1.375·10¹¹` (missing factor `7159.5`); closing at `c = 2` would need `X₀ = 2^109.4` against Barina's `2^71` (missing `2^38.4`); at Salikhov's `c = 5.125`, `k_max = 3693` (short ~37 million).
3. **The scissors:** lower jaw `k_max(X₀) = (3X₀)^{1/(c+1)} ≤ X₀^{1/3}` even at `c = 2`; upper jaw (Crandall on the real convergents) `k_min(X₀) ~ X₀^α` with **α measured 0.482–0.511 over `[2⁷¹, 2⁴⁰⁰]`** — so the open window widens as verification advances (ratio 1817 at `2⁷¹`, `9·10¹⁹` at `2⁴⁰⁰`); raw Crandall at `2⁷¹` gives `3.49·10¹⁰`, factor 3.9 below Hercher.
4. **Artifacts:** `experiments/test_REQ-MATH-001_scissors_cstar.py` and `test_REQ-MATH-004_scissors_race.py` in his Lean repo, re-run 2026-07-29, canaries passing.
5. **Honest scope (his):** exact Python with written-ahead canaries; nothing formalized; nothing excludes a cycle; it closes one route. `α` is measured, not proved; the argument needs only `α > 1/3`. The Dirichlet half is the load-bearing half and is unconditional on its own.

**Stopping-rule compliance:** verification of a route-impossibility claim about *instruments* — not a proof effort on the open condition, not a cycle search. Cycles front stays PARKED.

## Queue

1. **Shared repo, read-only.** Fresh clone (scratchpad). Record HEAD (expected `7c05458`; if moved, record and continue read-only). Record verbatim: the L-A9 entry and the whole offers-discharge block. Read his two scripts for operational definitions ONLY (what exactly is the Product-Bound chain as he computes it; what is `k_max`, `k_min`, where Crandall enters, what the canaries pin); his scripts are read, never run as verification. Small side item while the clone is open: his §12 claims NOTE.md has had five commits (three his, 23–24 July; two ours, 24th), net +2/−2 lines since the 19 July skeleton, our §4 text verbatim at HEAD — verify from the log and record flat.
2. **Clean-room derivation of `c*`** (fresh code, exact/interval arithmetic; imports nothing of his). Derive the chain yourself before computing: what effective exponent `c` in `|log₂3 − p/q| > κ/q^c` would close the window through the Product-Bound route, written out step by step in the findings. Verify `0.9617`, the `0.9622` variant, and **adjudicate the Barina figure**: the entry says "exact Barina bound `2075·2⁶⁰`" where our la8 record instantiates `2048·2⁶⁰ = 2⁷¹` — establish which number is Barina's actual verified bound (his paper's stated threshold), with citation, and whether the entry's parenthetical is right.
3. **The Dirichlet half — the grade-line attack he asked for.** State and verify the floor argument in your own words: Dirichlet gives every irrational infinitely many `|ξ − p/q| < 1/q²`, so no *effective lower bound* of the form `|ξ − p/q| > κ/q^c` with `c < 2` can hold — confirm the logical direction as the entry uses it (the chain *needs* `c* ≈ 0.96 < 2`, which no constant has, hence impossibility for every constant). Then attack: is this half genuinely unconditional and theorem-grade? Is the reduction "closing the window ⟺ exponent `c*`" itself exact or does it carry model assumptions? Negative controls in the regime-column spirit: verify the scissors claims in a second regime chosen against them (e.g. extend the `α` measurement beyond `2⁴⁰⁰`, and check `α > 1/3` is not an artifact of the fitted range).
4. **Replication, fresh code** (`experiments/merle_la9_check.py`; exact integers for all `q`, controlled precision for logs, every pass/fail robust to rounding): the deficits (7159.5, `2^38.4`, ~37 million), the scissors ratios (1817, `9·10¹⁹`), the raw-Crandall 3.49·10¹⁰/factor-3.9 sanity check, and your own re-measurement of `α` over `[2⁷¹, 2⁴⁰⁰]` plus the extended regime.
5. **The offers-discharge block, item by item:**
   - *(offer e, `7f20348`.)* Line-level verify in his Lean repo at that commit: line 188 subscripts now `q₂₂`/`q₂₃`, line 186's `δ` carries the REQ-054 factor 2, the header's `4.955e10` named as withdrawn. His recompile claim is read-not-built — state the trust boundary.
   - *(offer f.)* The ledger now records Cor. 29's `X₀ ≥ 3·2⁶⁹` with the `704 < 1536 < 2048` units — verify the wording against `briefs/merle-la8-t1-check-findings.md` and the external premise findings; flag any drift.
   - *(offer g, 052bis at `7f20348`.)* Run `test_REQ-MATH-052bis_ostrowski_grille.py` yourself from a clone of his Lean repo (this is HIS artifact and running it is reproduction, not our verification — the verification is your own reimplementation of the load-bearing rows in `merle_la9_check.py`): the ten rows, the four figures the ledger cites (`14936 = [(665,22),(306,1)]`, `15601`, `31202`, `46803`), eps-small median `15601`; the re-specified control declared as re-specified; the 200-draw control (median 2, max 53, 0/200 reaching 306).
   - *(REQ-067, `4f4bb2e` — the volunteered defect.)* Independently: compute the CF of `log₂3` at `dps = 400` and at two higher precisions with your own code, locate the divergence index (his claim: 385, with 1615 of 2000 terms noise); verify `0.00078` on the correct sequence under the confirmed convention (classes `{1},…,{B−1},{≥B}`, `dof = B−1` — the main session confirmed this is exactly our `merle_r11_hygiene_check.py` convention); verify his six-number table (our `0.008425` / `0.001214` / max-over-all-`B∈[3,40]` `0.5666` on the correct sequence; `0.004496` / `0.001249` / `1.0504` on his wrong one). Note for the findings: our own committed run tried 13 binnings, not all 38 — his exhaustive max is the stronger form of our printed `< 0.567`; say whether it holds.
6. **The grade-line adjudication (the deliverable).** Weigh at minimum: (a) the entry as-is, one key, our key turned scoped; (b) a split grade inside one entry — the Dirichlet half two-keyed as unconditional once verified, the scissors half explicitly measured-not-proved and left at the weaker grade; (c) any defect found = turn-with-offer per the standing pattern. Draft the co-edit offer text (wording offers only; his prose untouched; the key turn itself is NOT made — main session reviews, and the push is gated on the author).

## Record

- `experiments/merle_la9_check.py` + committed output (canaries printed first; per-item commits).
- `briefs/merle-la9-check-findings.md` — verbatim entry and offers block; operational definitions; the `c*` derivation written out; the Barina adjudication with citation; the Dirichlet-half attack and verdict; replication results with the second-regime controls; the REQ-067 verification; the grade recommendation and drafted co-edit offers.
- Do NOT edit `HANDOFF.md` (the main session records at merge — five windows run in parallel this round and HANDOFF races were the one overflow we have had).

## Rules

- Branch **`merle-la9-check`** from your worktree HEAD. FIRST verify the worktree contains this brief (`briefs/merle-la9-check-brief.md`); worktrees are sometimes cut from a stale HEAD — if it is missing, merge or rebase onto local `main` before starting, and state your base SHA in the findings.
- Read-only everywhere outside this repo: fresh clones of the shared repo and his Lean repo in the scratchpad; no pushes, no shared-repo writes, no web beyond the two clones and (for the Barina citation) literature lookup; no contact with anyone; no interaction with his repositories (no fork, issue, star, watch).
- File edits via the Edit/Write tools only — never PowerShell `Get-Content`/`Set-Content` (see HANDOFF quirks; the repo's `—`/`≤`/`ε` are destroyed silently). Run `experiments/encoding_scan.py` before your final commit and record RESULT: CLEAN.
- Discrepancies recorded and flagged, never disputed in prose; a defect found is a finding delivered kindly — he asked for this attack himself.
- No reply paragraphs; no key turns (recommendation only); no co-edit commits; do NOT merge — the main session reviews (re-runs the script) and merges. Stop after Record.
