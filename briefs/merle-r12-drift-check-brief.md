# Brief: round-12 §4 drift hand-backs, §9 rotation facts, the §10 silence, and two small corrections — for a delegated session

**Context required before starting (in order):** `README.md` (binding stopping rules), `AGENTS.md`, `HANDOFF.md` item 1 (round-12 paragraph; the round-11 hygiene and co-edit paragraphs), `briefs/merle-round12-letter.md` Part 2 §§4, 9, 10 and the closing prior-art section, `briefs/merle-r11-hygiene-check-findings.md` (Half 2, item 4 — the drift; this is the record his §4 corrects), `briefs/merle-round11-coedit-findings.md` (the theorem hand-back block as pushed to the shared LEDGER), `briefs/junction-public-recon-findings.md` (the two sentences his closing section corrects), `cycles.md` §12.8.6 as merged and `briefs/staircase-gamma-upper-findings.md` §§ on the maxgap criterion (what §9 refines).

## Provenance

Merle's round-12 letter hands back **three corrections to our record** (§4(d) the displayed drift identity, §4(b) the shared 44-decimals diagnosis, the closing section's §10.2 sentence), confirms one we could not previously settle (his 3,100 bytes vs our printed 3,175 — the main session has already established the mechanism: the blob is 3,100 bytes and 75 lines; our figure was a CRLF-converted working-copy measurement; your job is to re-verify and record it properly), contributes an irrational-rotation reformulation of our availability theorem (§9) that he wants in the marked section of `12.8.6.1` under both names, and flags one genuine silence (§10): his round-11 item 2, "the rhythm of the peaks", got no answer in our round-11 letter.

**Stopping-rule compliance:** verification and record repair; nothing here reopens a front.

## Queue

1. **§4(d) — the display is a truncation, and the correction is ours to own.** Fresh code (`experiments/merle_r12_drift_check.py`, high-precision with stated guard digits, every verdict rounding-robust):
   - Derive the series yourself: with `u = 1/(3x)`, `D(x) = (2/ln 2)·artanh(u)`, so `D(x)/δ_x = 1 + 1/(27x²) + 1/(405x⁴) + 1/(5103x⁶) + …` where `δ_x = 2/(3x·ln 2)`. Verify his coefficients and his relative-error figures (`2.58e-3` at `x = 1`, `3.06e-5` at 3, `2.96e-8` at 17, `7.94e-89` at `2⁷¹`) and the leading gap `6.6432e-45` at `2⁷¹`.
   - Confirm the corrected form: exact is the chain `D(x) > δ_x·(1 + 1/(27x²)) > δ_x`, strict; the display as equality under a "what is exact" heading is wrong.
   - **Locate every place our record carries the display**: the shared LEDGER's round-11 co-edit theorem-hand-back block (fresh read-only clone; quote it verbatim), `briefs/merle-r11-hygiene-check-findings.md`, `briefs/merle-round11-coedit-findings.md`, `briefs/merle-round11-reply-draft.md`, and any HANDOFF lines. Record each site verbatim. Draft the correction block for the co-edit (acceptance stated as ours, the exact chain in place of the equality) — drafted in the findings, NOT pushed.
2. **§4(b) — the diagnosis we both reached is refuted.** Verify his scale-invariance claim: rescale the `−17` cycle shape to `x_min = 17·2^k` for several `k`; the ratio `Σᵢ D(xᵢ) / (n·D(x_min))` should be `0.4755266037564546…` at every scale (his figure), because to leading order it is `(1/n)·Σᵢ(x_min/xᵢ)`, a pure function of shape — verify that identity to leading order too, and check the real cycles with ≥ 2 elements. Locate where our 44-decimals explanation sits in our record (hygiene findings; the sent round-11 reply) and record the refutation flat.
3. **§4(e)/(f) — flat confirms, small numbers.** The two-shore factor-2 reading (`log₂(1+u) − log₂(1−u) = 2u/ln 2 + O(u³)`); the corollary restated with referents: uniqueness of `x* = 7/3` via `3(3x+1) = 4(3x−1)`, `D` strictly decreasing, `D(1) = 1` exact, `D(3) = log₂(5/4) = 0.321928 < s = 2 − log₂3 = 0.415037`, ratio `D(2⁷¹)/s = 9.8145e-22`. Record his credit corrections as he fixed them (factor 2 "jointly arrived at"; corollary his, proof ours) — no action, just the record.
4. **§9 — the rotation reformulation, every claim checked** (this is candidate `cycles.md` material, so nothing passes on his word):
   - `5L = 8 − θ` with `θ = 8 − 5·log₂3 = 0.0751874964…`, so `δ(n) = ⌈nL⌉ − nL` advances by `θ` **mod 1** under `n → n+5` (his correction to his own first formulation); wrap count over `n = 1..2000` is 150, fraction ≈ `θ`.
   - The step sequence `K(n+1) − K(n) ∈ {1,2}` is Sturmian of slope `L − 1 = log₂(3/2) = 0.584963` (not `L`), identity `K(n+1) − K(n) = 1 + (⌈(n+1)(L−1)⌉ − ⌈n(L−1)⌉)`; letter-2 frequency over 2000 steps `0.585`; factor complexity `p(m) = m + 1` for `m = 1..12`.
   - `1/θ = 13.3000838`; for `J ≤ 13` the points `{jθ}` are already sorted, so `maxgap(J) = max(θ, 1 − Jθ)` **exactly** — reproducing `maxgap(12) = 0.0977500433` and `maxgap(13) = θ` with no three-distance machinery; the sweep criterion `J ≥ (1 − l)/θ = 12.2967` with `l = 0.1169390665 − 0.0415 = 0.0754390665`.
   - His new result: maxgap stays exactly `θ` for `J = 13..25` and first falls at `J = 26`, to `14θ − 1 = 0.0526249495`, with the identity `13(14θ − 1) + 14(1 − 13θ) = 1`; margin `l − θ = 2.5157e-4` (0.3335% of arc); and the cost-not-failure claim — losing `θ ≤ l` moves the sweep from 66 to 131 consecutive integers (via `J = 26`) and the unconditional range from `p ≥ 16` to `p ≥ 18` (window sizes `0.05L^p`: 125.7 at `p = 17`, 199.2 at `p = 18`). Verify each number.
   - Deliverable: a proposed text block for `12.8.6.1`'s marked section carrying the reformulation **under both names** (he asked for exactly that), consistent with the merged `12.8.6` wording — proposed in the findings, `cycles.md` NOT edited.
5. **§10 — the silence.** Establish what his round-11 item 2 ("the rhythm of the peaks") actually claimed: search `briefs/` for the round-11 letter record and the hygiene brief's four-negatives list; reconcile with his §10 description (null model already broken and said so; "no memory, no clustering, no spectral line" drawn with a disowned detector). Then lay out the three options he names — retain, retain with a scope clause, strike — with what our record supports for each, and a recommendation consistent with the stopping rules (the expected shape: recorded as unreplicated, his side of the ledger, the way the `+0.22/−0.29/−0.57` triple was recorded; but establish it, do not assume it).
6. **The two small corrections, verified and recorded:**
   - The 3,175: re-verify independently (blob `b55095a` in his Lean repo: `git cat-file -s` = 3,100; line count 75; 3,100 + 75 = 3,175 under CRLF conversion). Draft the one-sentence correction to `briefs/junction-public-recon-findings.md`'s figure — as a dated addendum note in your findings naming the mechanism, original file untouched.
   - §10.2: in his Junction repo, read the file our sentence described (the `PROOF_ASSEMBLY.md` §10.4/§10.5 area — locate the exact object from `briefs/junction-public-recon-findings.md` §4.3): his correction says it prints **fifteen** partial quotients and **cites Jackson–Matthews 2002** for the 10,000. Verify at the pinned commit; record which sentence of ours overstated it and draft the correction the same way.

## Record

- `experiments/merle_r12_drift_check.py` + committed output (per-item commits; canaries/negative controls printed first — in the regime-column spirit, test every asserted equality in a regime chosen against it: small `x` for the series, spread configurations for the sums, and both a rescaled and a real cycle for §4(b)).
- `briefs/merle-r12-drift-check-findings.md` — the sites located verbatim; the drafted correction blocks (co-edit material, marked as such); the §9 verdicts claim by claim with the proposed 12.8.6.1 block; the §10 item-2 options and recommendation; the two small corrections with mechanisms.
- Do NOT edit `HANDOFF.md`, `cycles.md`, or any existing findings file (corrections are drafted in YOUR findings; the main session applies at merge). Five windows run in parallel this round.

## Rules

- Branch **`merle-r12-drift-check`** from your worktree HEAD. FIRST verify the worktree contains this brief; if missing, merge/rebase onto local `main` before starting, and state your base SHA in the findings.
- Read-only outside this repo: fresh scratchpad clones of the shared repo and his two GitHub repos as needed; no pushes; no interaction with his repositories; no web beyond the clones.
- File edits via Edit/Write tools only — never PowerShell `Get-Content`/`Set-Content`. Run `experiments/encoding_scan.py` before your final commit and record RESULT: CLEAN.
- Corrections to our own record are stated as ours, flat, with the mechanism named — the round-10 `35 031 770 966` pattern.
- No reply paragraphs beyond the drafted blocks marked as material; no key turns; no co-edit commits; do NOT merge. Stop after Record.
