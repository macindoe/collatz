# Brief: the family graph's guaranteed neighbours — the two commutation laws of ladder and step, and the cousin search that closes them (ladder.md 15.7) — for a delegated session

**Context required before starting (in order):** `README.md` (strategy and **binding stopping rules**), `AGENTS.md` (house norms: nothing is labeled proved without independently written verification code; no change logs in tracked files; every fact lives in exactly one page; content and structure in separate commits), `HANDOFF.md` (register norm; the delegation pattern; the PowerShell quirk), `ladder.md` in full (15.1.1 is one of the two laws composed here; 15.5 the valuation-level reading), `spine.md` §9.8 (the reduced map `F`, states `(ω, d)`, the numerator `A(ω, d) = 3^d ω − 1 = 2^s e`, the exit `e`, the next state `(Ω, D)` through `e + 1 = 2^m 3^a Ω`, the letter `(s, m₊)`), the first-layer mod-8 table (stage1.md 11.8.4.1 and wherever `3^d ω mod 8` decides `m₊`; find the address), `stage2.md` 11.8.5 (the 3-gain `a = v₃(2^s − 1)` off resonance, `h(s)`), `stage3.md` 11.8.6.3 (targets `1 − 2^s`, entry depth), `stage4.md` 11.8.7.3 and 11.8.7.3.1 (the next-core residues; the neighbour family's residues are these), `reverse.md` 14.1.1 (the predecessor characterization and the backward tree), 14.10 (the dual ladder), 14.14.1 (the global edge parameterization), `itinerary.md` 14.15.1.1 (the door odd part), `bridge.md` §16 (the neighbour's core is the Bridge object — pointer only), `symbols.md` (registry; the collision index for `q`, `σ`, `a`, `m` — use the registry's symbols), `index.md` (resolver granularity), and `briefs/rubin-3adic-baker-brief.md` with its findings file (the form of a brief, a findings file and a per-item commit stream on this repository).

## Provenance

The author's proposal of 2026-09-09: pin a family (a core `ω` at all depths) and list its forward neighbour families `Ω(ω, d)`. The list is exact already: the neighbour at depth `d` is the family of stage4's `C = 3^d ω − 1 + 2^s`, so its residues are 11.8.7.3.1's and its core is the Bridge. Two guaranteed-neighbour laws and their closure surfaced in the main session's pre-check with throwaway code (deliberately not filed; you write the script of record from scratch). Every claim below is **a claim to be re-derived and re-verified here, not a fact**:

1. **Law (i), merge/skip by `ω mod 8`.** At every off-spike depth `d` (`s(ω, d) = 1`): `ω ≡ 5, 7 (mod 8)` gives `F(ω, d+1) = F(ω, d)` (the pair lands on one state — the neighbour list is 2-to-1 on adjacent pairs), while `ω ≡ 1, 3 (mod 8)` gives `F(ω, d+1) = F²(ω, d)` (the depth-`(d+1)` child is the depth-`d` grandchild). Mechanism: the mod-8 table read at `3^d ω mod 8` (`7` gives `m₊ ≥ 2`, `3` gives `m₊ = 1`) composed with 15.1.1's off-spike branch. Pre-check: 20,018 cases, 0 failures. State it as a split by `m₊` first (merge iff `m₊ ≥ 2`), then as the `ω mod 8` reading.
2. **Law (ii), the two-row commutation.** Whenever `s(ω, d) = 3` and `m₊(ω, d) ≥ 4`: `F(ω, d+2) = F(ω, d) − (0, 1)` — the same neighbour family one depth lower, letters `(3, m) → (6, m − 3)`. Mechanism: `A_(d+2) = 9A_d + 8 = 8(9e + 1)` and `9e + 1 = 9(e + 1) − 8`, so with `u = e + 1 = 2^m 3^a Ω` one has `e'' + 1 = 9u/8`, which has the core of `u`. Pre-check: 3,000 cases, 0 failures; it accounts for 789 of the 792 non-adjacent same-family repeats in the box `ω < 4,000`, `d ≤ 40` (the other 3 are small-number coincidences — list them). In bit language: multiplying the numerator by 9 rewrites `1 0 0 1^m …` into `1 0^5 1^(m−3) …` (322 cases, 0 failures).
3. **The cousin search closes the family.** Climbing `k` rows from `(ω, d)` with `u = e + 1`: `A_(d+k) = 3^k·2^s·u − c` with `c = 3^k(2^s − 1) + 1`. A whole-stratum repeat of the neighbour core needs `c = 2^t`; otherwise `u` divides a fixed number and only finitely many small `u` repeat. `3^k(2^s − 1) + 1 = 2^t` is `(2^t − 1)/(2^s − 1) = 3^k`, a base-`2^s` repunit equal to a power of 3. Pre-check: length 2 (`1 + 2^s = 3^k`) has exactly `(k, s, t) = (1, 1, 2)` and `(2, 3, 6)`; length 3 is impossible (`v₃(1 + x + x²) = 1` for `x ≡ 1 (mod 3)`); lengths `≥ 4` have no solution with `k, s ≤ 200` (searched). **The main session expects the general case to close elementarily by Zsigmondy's theorem** (Bang 1886 for base 2: for `t ≥ 3`, `t ≠ 6`, `2^t − 1` has a prime divisor dividing no `2^j − 1` with `j < t`, and every such primitive prime divisor is `≡ 1 (mod t)`): the quotient `(2^t − 1)/(2^s − 1)` with `s < t` contains that primitive prime, so equalling `3^k` forces the primitive prime to be `3`, hence `t | 2`; the excluded `t = 6` is exactly the solution `(2, 3, 6)` (`63/7 = 9`), and `t ≤ 2` gives `(1, 1, 2)`. **This is a claim: write the proof out, check every case, and if it fails record where.** If it holds, no external reference beyond Zsigmondy/Bang is needed and the equation is closed, not searched.
4. **The unified statement — the lemma.** For `(k, s, t)` one of the two solutions, `F(ω, d) = (Ω, D)` with entry depth `m`: if `m ≥ t − s + 1` then `F(ω, d+k) = (Ω, D + s − t + k)`; if `m = t − s` then `F(ω, d+k) = F(Ω, D + s − t + k)` (the boundary lands one step further along column `Ω`; at `(1, 1, 2)` this is (i)-skip, at `(2, 3, 6)` it is the `m₊ = 3` case, `F(ω, d+2) = F(Ω, D − 1)`); if `m < t − s`, nothing beyond finite coincidence. Mechanism at the boundary: `u = 2^(t−s)·u'`, `A_(d+k) = 2^t(3^k u' − 1)`, and `3^k u' − 1` is the numerator of the state `(Ω, k + a)`. Pre-check: 10,000 cases, 0 failures. Laws (i) and (ii) are the **only two primitive commutation relations**, and a census confirms it: over 120,000 random states and `k = 1..8`, the same-core rate is exactly `1.000` on the strata `(k, s, m₊) = (1, 1, ≥2)` and `(2, 3, ≥4)` and `0.000` on every other stratum with `≥ 200` cases; the partial rates seen (`0.253` at `(2, 3, 3)`, `0.063` at `(3, 1, 1)`) are compositions ((ii)-boundary then (i)-merge in column `Ω`; (i)-skip then (ii)). Reproduce the census and the two composition explanations.
5. **Bits gained, read backward — a reading, not a new fact.** Backward, the predecessor's numerator is `3^d ω = 1 + 2^s(2^m 3^a Ω − 1)`, so its low `σ = s + m` bits are exactly the letter in unary, `1, 0^(s−1), 1^m`, followed by the bits of `3^a Ω − 1` shifted up by `σ` (pre-check 20,116 cases, 0 failures). Forward, the door gains `d` ternary digits, `y ≡ −2^(−s) (mod 3^d)`, fixed by `s` alone, and the 3-gain `a = v₃(2^s − 1)` is that signature read against `−1` (stage2's `h(s)`; 19,973 non-resonant cases, 0 failures). No freedom lives in the gained bits; the backward freedom is the door choice `(a, s)`, and the signature is that choice written out. This is a reading of stage3.md 11.8.6.3, reverse.md 14.14.1, itinerary.md 14.15.1.1 and stage2.md 11.8.5 — record it as a remark with pointers, restate none of them, and verify it in the script because law (ii)'s bit rewrite depends on it.
6. **The mirror versions at reverse.md 14.10 — expected and unchecked.** The dual ladder steps `s → s + 2` at a fixed door. Look for the 3-adic twins of (i) and (ii): is there a merge/skip law by `y mod 9` on the `d = 1` (`T₃`) branch, and a two-row commutation on some stratum? **Record obstructions; do not force the analogy.** If a twin holds, it goes into a remark 14.10.2 with its own verification line; if the shape differs, one paragraph saying how, and stop.
7. **The family graph paragraph.** In-degree: the drainage basin's small families dominate (`1` is hit 114 times from the box `ω < 4,000`, `d ≤ 40`; `5` 62 times) — this is the backward tree of reverse.md 14.1.1 and nothing new. One paragraph naming it as such.

Grade at delegation: **both laws are local (mod 8; one stratum) and are spent by the next step like everything else; law (ii) is the first integer-level composition of ladder and step, the object 15.4 names; no front moves; stopping rules untouched.** A parallel delegation (`briefs/ladder-afterlife-brief.md`, branch `ladder-afterlife`) owns the spike case (`s ≥ 2`, the tear) and ladder.md **15.6**; the seam between the two is law (i) (off-spike), which belongs **here**.

**Stopping-rule compliance:** identities on states and profiles; no cycle search, no per-period anything; no equidistribution proof effort; no front reopened; the Bridge unchanged.

## Queue

1. **Mathematics first, on paper in the findings.** Statements and proofs for items 1, 2, 3 and 4 from the numerator identity and the definitions, before running anything. The Zsigmondy closure of item 3 written in full with the small cases tabulated. Where a pre-check claim is wrong or needs a boundary case, record the corrected statement and what was wrong; **a claim is not dissolved by naming its type** — every correction carries a re-derivation.
2. **Fresh verification code — `experiments/ladder_family_graph.py`, committed with its output.** Imports nothing from any existing script (not `ladder.py`; the main session's scratchpad code does not exist for you); exact integer arithmetic at every pass/fail decision; canaries first (the three known cycles' states, `(1, 1)`, 15.1.1 on a few hundred states, and the neighbour-family identity `Ω(ω, d)` = core of `C = 3^d ω − 1 + 2^s` against stage4's residues). Contents: (a) law (i) both readings; (b) law (ii) and its bit rewrite, plus the repeat census in the stated box with the 3 coincidences listed; (c) the repunit equation: the length-2 and length-3 arguments checked, and the search to `k, s ≤ 200` reproduced as a control on the proof; (d) the unified statement on all three `m`-regimes at both solutions; (e) the stratum census with rates per `(k, s, m₊)` and the two composition explanations verified case by case; (f) item 5's two bit/digit identities; (g) whatever item 6 finds, on random `(y, s)`; (h) the in-degree count of item 7. **One documented command reproduces the committed output in full** (`python experiments/ladder_family_graph.py`, no phases, no flags needed for the record); if a phase is slow, keep it in the single run and print timing. Record counts, ranges, seed, date.
3. **Wiki edits — conservative; content and structure in separate commits.**
   - `ladder.md`: a new section **15.7. Guaranteed neighbours: the two commutation laws** — on your branch 15.6 does not exist yet, so append 15.7 at the end of the file after 15.5 and say so in the findings; the main session orders the sections at merge. Contents: law (i) as a lemma; law (ii) as a lemma; the repunit proposition (item 3) with its proof; the unified lemma (item 4, the repunit proposition as its hypothesis) with the census as its verification line; the bits-read-backward remark (item 5, pointers); the family-graph paragraph (item 7). Verification inline: what was checked, the range, the date — one line per statement, per AGENTS.md. Front matter `updated` only; **do not edit 15.4 or the Current-state paragraph** (the parallel branch owns them; the main session adds the pointers at merge).
   - `reverse.md` 14.10: remark 14.10.2 only if item 6 finds a twin; otherwise nothing on that page and the obstruction in the findings. Front matter `updated` if the page changes.
   - `symbols.md`: only if you introduce a symbol; prefer not to.
   - `index.md`: add 15.7 to the resolver only if it lists at the granularity of 15.5; otherwise leave it.
   - `briefs/ladder-family-graph-findings.md`: the derivations, the Zsigmondy proof, what the pre-check got wrong (if anything), the verification record, the census table, the item-6 result or obstruction, and a *For the main session at merge* section listing the exact pointer sentences 15.4, the Current-state paragraph and 15.6 will need.
   - **Do not edit** `HANDOFF.md`, `anchors.md`, `README.md`, `TOUR.md`, `publication.md`, `paper/`, or anything under `sources/`. Do not create 15.6.
4. **Compliance.** Run `experiments/encoding_scan.py` before the final commit and record `RESULT: CLEAN` in the findings.

## Record

- `experiments/ladder_family_graph.py` + committed output (canaries first; one reproducing command).
- `ladder.md` 15.7 and its front-matter date; `reverse.md` 14.10.2 if earned; the resolver line if applicable.
- `briefs/ladder-family-graph-findings.md` as specified.

## Rules

- Branch **`ladder-family-graph`** from your worktree HEAD. FIRST verify the worktree contains this brief (`briefs/ladder-family-graph-brief.md`) and that `git log --oneline -1` shows a commit dated 2026-09-13 or later; worktrees are sometimes cut from a stale HEAD — if either check fails, rebase onto local `main` before starting, and state your base SHA in the findings.
- No web access needed (Zsigmondy/Bang is standard; cite it by name and year); no contact with anyone; no pushes; no edits outside this repository.
- File edits via the Edit/Write tools only — never PowerShell `Get-Content`/`Set-Content` (see HANDOFF quirks; the repo's `—`/`≤`/`₂` are destroyed silently).
- Register: flat, calibrated prose. Heuristics labeled heuristics; nothing labeled proved without the fresh code of Queue 2 **and** a written proof.
- Record obstructions; do not force analogies (item 6 in particular).
- Run the script in the foreground and wait for it; do not stop until every commit exists on the branch.
- Do NOT merge — the main session reviews (re-runs the script, re-derives the Zsigmondy closure and one law, checks the 15.6 seam) and merges. Stop after Record.
