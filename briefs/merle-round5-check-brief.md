# Brief: Merle's fifth reply — the Catalan lock (anchored loops, spent stock), the Benford side-asymmetry, and the exhaustive small-k map (for a delegated session)

**Context required before starting (in order):** `README.md` (binding stopping rules — see compliance below), `AGENTS.md`, `HANDOFF.md` item 1 (collaboration state through round 4), `cycles.md` 12.5–12.8 (the profile/`R_r`/`q` machinery; 12.6.1.1 the transport recurrence), `reverse.md` 14.15.9 (the rotation lemma and the signed extension; the negative cycles as diagonal points), `briefs/prime-local-probe-findings.md` (the repeated-word gcd law `gcd(q_P, R_0) = |q_P|/q_red(base)`, closed in review; the structureless verdict his letter leans on), `briefs/merle-round3-check-findings.md` (conventions for adjudicating his claims), `briefs/merle-round4-reply-brief.md` (the round-4 decisions his letter answers).

## Provenance

Eric Merle's fifth reply — pasted verbatim by the author (standing convention; numerals trusted as transcribed). Received after the round-4 send; record its date as 2026-07-19 with the note that the environment clock has disagreed with the thread's dates by a day — the git log is the authoritative order. Full text in Appendix A; work from it, not from this summary.

Contents, sorted by what they demand:

1. **Round-4 closures, record only:** JAR reframing received in the author's terms; venue convergence ("number-theory-shaped home, formalization as supporting artifact" — now three independent readers); hosting settled — he will DOI-pin to v2 (10.5281/zenodo.21421120) and host the mirror-paper pair; visualizers accepted as the intuition layer. **One plan change requiring an author decision:** he proposes each side builds its own cycle-side gateway viz and cross-links ("let's each build one and cross-link"), versus our recorded round-4 plan (we build the cycle-side gateway). Record as decision item; nothing to compute.
2. **The Catalan lock** — the substantive claim: the known cycles are each anchored to a near-miss of the two towers; the "free locks" are exactly the `|q| = 1` near-misses; those are exactly three in all of history (his labels 2-3, 4-3, 9-8) by Mihailescu; the alternation dealt one north (+1) and two south (−1, −5); −17 (`q = −139`) is the single lottery win; therefore the finite-pure mechanism is a spent finite stock and every future candidate needs the archimedean-to-finite coupling our prime-local probe forces. Items 1 and 4 below.
3. **The Benford side-asymmetry** — his flag (b): which side gets the better near-miss is not 50/50 but `log₂(3/2) = 58.5%` for the negative side, "exact Benford"; his script's first draft said 50/50 before the machine corrected it (self-catch, disclosed per protocol). He asks whether this is new. Item 2.
4. **His two questions addressed to us:** (a) is the Catalan-lock reading folklore — do we have a reference; (b) is the Benford fraction new. Items 2 and 4.
5. **His artifacts:** commit `a67970f`, scripts REQ-MATH-010 (anchored loops; the 256/243 no-cycle sweep) and REQ-MATH-011 (the mirror, the envelope `q₊ + q₋ = 2^⌊kL⌋`, the Benford bias, the exhaustive `k ≤ 10` map: cycles exactly `{+1}` and `{−1, −5, −17}`, one rotation per profile via L-A1). Items 3 and 5.
6. **His L-A2 proposal:** the repeated-word gcd law "earned its keep" (it swept the one false survivor of his `k = 5` sweep — the all-ones word); he proposes ledger entry L-A2 rather than a footnote. Author decision; record with the verification of the sweep claim (item 3).

**Main-session pre-check (2026-07-19, verify fresh, don't trust):** the four `(k, m, q)` anchors check out — `+1: (1, 2, +1)`; `−1: (1, 1, −1)`; `−5: (2, 3, −1)` (his 8-vs-9); `−17: (7, 11, −139)`. The envelope identity is exact for `k = 1..199`. The Benford fraction at `N = 10⁶` floats and `k ≤ 2000` exact both land on `0.585`. `|2^a − 3^b| = 1` has exactly `(a,b) ∈ {(1,1), (2,1), (3,2)}` for `a < 80, b < 50`. **And the key adjudication seed: Mihailescu is overkill for his lock.** For powers of 2 against powers of 3 the finiteness is elementary (mod 8 plus a two-line factoring) and ancient — Levi ben Gerson (Gersonides), 1342/43, *De numeris harmonicis*, for Philippe de Vitry. Citing Catalan where Gersonides suffices would invite exactly the referee reaction he wants to avoid. The delegate confirms, cites precisely, and words the verdict.

**Stopping-rule compliance:** this is verification of a correspondent's claims plus a literature adjudication — not a cycle search campaign, not a divisibility-based exclusion attempt. The `k ≤ 10` enumeration (item 3) is a bounded replication of *his stated result*, done once, exact, and closed — the cycle front stays parked. Note also the logical status the findings must state precisely: the "spent stock" claim excludes nothing — it says one *sufficient* mechanism (`|q| = 1`, where divisibility is free) is exhausted, so any further cycle must satisfy `q | R_0` nontrivially, which is exactly the parked front's condition. It is a reframing brick for the note, not an obstruction. Do not extend it into one.

## Queue, in order

1. **The anchored loops and the `|q| = 1` lock, fresh code.** `experiments/merle_round5_check.py`, importing nothing from any committed script (cross-check against committed instance records by quoting their numbers only). (a) The four known cycles of `3x+1` on the nonzero odd integers: compute each cycle's `(k, m, q)` from the orbit itself (fresh forward iteration), confirm his anchor table including −5 on 8-vs-9 and −17 at `q = −139`. (b) `|2^a − 3^b| = 1`: exhaustive check in a wide window (say `a ≤ 200, b ≤ 130`), plus the elementary proof written out in the findings (the mod-8 argument for `3^b + 1 = 2^a`; the factoring argument for `3^b − 1 = 2^a`) — stating explicitly that Mihailescu/Catalan is not needed for this case and that the result is Gersonides 1342/43 (item 4 pins the citation). (c) Verify the mechanism claim behind "free lock": for `|q| = 1`, *every* profile at that `(k, m)` yields an integer cycle (check all compositions at `(1,1), (2,1)→(k,m)=(1,2)`-labelled correctly, `(3,2)→(2,3)` — i.e. the three anchors), including the parity/oddness bookkeeping — state exactly what "free" means and verify it exhaustively at the three anchors. (d) Verify his ticket mapping (4-3 → +1 north; 2-3 → −1, 9-8 → −5 south) and that −17 is the only known cycle with `|q| > 1`, with `gcd(q, R_0) = 139` verified (139 prime, word primitive) — the "lottery win" reading.

2. **The envelope and the Benford side-asymmetry, same script, separate section.** (a) Prove (one displayed line) and verify exactly (`k = 1..3000`, big integers) the envelope identity `q₊ + q₋ = 2^⌊k log₂ 3⌋` where `q₊ = 2^⌈kL⌉ − 3^k`, `q₋ = 3^k − 2^⌊kL⌋`. (b) Pin the two distance notions before measuring: in *ratio* (multiplicative) distance the two sides split 50/50 in density; in *absolute* distance the negative side wins iff `{k log₂ 3} < log₂(3/2)`, density `log₂(3/2) ≈ 0.585` by Weyl equidistribution (`log₂ 3` irrational). Derive the threshold (`|q₋| < q₊ ⟺ 2^{frac} < 3/2`), verify by exact integer comparison to the largest comfortable `k` (≥ 10⁴) and by high-precision fractional parts to `k = 10⁷` (guard the near-threshold values — use enough precision that no decision is within rounding distance; report the closest call). (c) Identify the statement: it is the base-2 Benford *second-digit* law — `P(second binary digit of 3^k is 0) = log₂(3/2)` — i.e. his "exact Benford" is exactly right, and his machine's 50/50 first draft was the correct answer to the *ratio* question; both numbers are right answers to different questions, and the additive one is the cycle-relevant one because `q₊` and `|q₋|` are compared as integers (the divisibility condition lives on `q` itself, not on `q/3^k`). Adjudicate his self-catch in exactly those terms. (d) State the convergence quality observed (the empirical fraction vs `log₂(3/2)` at each decade) without proving discrepancy bounds — measurement, labeled.

3. **The exhaustive `k ≤ 10` map, fresh enumeration.** Same script, third section. Derive the honest `m`-bounds from the cycle inequalities before enumerating (do not trust this sketch — re-derive: positive cycles force `k log₂ 3 < m ≤ 2k`; negative cycles force `k ≤ m < k log₂ 3`; state the derivation from the per-step bounds on `(3y + 1)/2^s`). Enumerate all compositions `(s_1, ..., s_k)` of every feasible `m` for `k ≤ 10`, both sectors; test `q | R_0` once per profile citing rotation-invariance (12.6.1.1 / 14.15.9.2 — this is his "one rotation per profile via L-A1"); for divisible profiles compute all cycle elements exactly and check odd/sign/closure. Deliverables: (a) confirm or refute "cycles exactly `{+1}` and `{−1, −5, −17}`" for `k ≤ 10` — count profiles tested per `(k, sector)`, total, zero silent skips; (b) the `k = 5` all-ones word: confirm it is `B⁵` of the trivial-cycle word, `q_P = 2^{10} − 3^5 = 781`, `q_red(base) = 1`, `gcd(q_P, R_0) = 781 = |q_P|` — i.e. it *is* divisible (it realizes the trivial cycle traversed five times, `y = 1`), which is presumably his "false survivor": a survivor of the divisibility test that is not a *new* cycle, exactly the case the repeated-word gcd law flags a priori. State precisely what the law sweeps (every `B^k, k ≥ 2`, before any enumeration) and verify the sweep across all repeated words that occur in the `k ≤ 10` enumeration. (c) Confirm his 256/243 sweep is the `(k, m) = (5, 8)`, `q = 13` slice of (a) — subsumed, no cycle. (d) His L-A2 proposal: record it; the elevation is the author's decision; the findings state only that the law is verified, closed in review (`briefs/prime-local-probe-findings.md`), and did real work in his sweep.

4. **Literature: the folklore adjudication, with verified citations.** WebSearch/WebFetch; verify every citation before it enters the findings (title, venue, year — no reconstructed guesses; drop what cannot be confirmed). Two questions, his (a) and (b):
   - **(a) The Catalan lock.** Expected verdict, to be confirmed and evidenced: *folklore, yes — with citable homes.* The correspondence "known cycles ↔ good rational approximations of `log₂ 3` / small `|2^m − 3^k|`" is standard in the cycle-bounds literature: Steiner 1977 (the 1-circuit theorem via Baker); Crandall 1978; Eliahou 1993 (cycle-length lower bounds from the convergents); Halbeisen–Hungerbühler 1997; Simons 2005; Simons–de Weger 2005; surveyed in Lagarias 1985 and the Lagarias annotated bibliographies. The `|2^a − 3^b| = 1` finiteness is Gersonides 1342/43 (modern accounts: Ribenboim's *Catalan's Conjecture*, 1994; Mihailescu 2004 settles the general Catalan case but is not needed here). Verify each; then the verdict for the reply: his instinct not to re-announce is right; the *ingredients* are folklore-with-references; what may be genuinely his is the packaging — "the free stock is finite and spent, so every remaining candidate needs the finite-place × archimedean coupling" — as a framing brick joining his arithmetic wall to our probe's verdict.
   - **(b) The Benford fraction.** Search for any prior statement of the side-asymmetry (`log₂(3/2)` split of which tower is nearer in absolute terms, or equivalently the second-binary-digit law of `3^k`) *in the Collatz/cycle context*. The equidistribution fact itself is Weyl-classical and the Benford connection for `3^k` leading digits is textbook — the question is whether anyone has stated it as the sign-side asymmetry of the near-miss sequence. Report what is found either way; "we searched X, Y, Z and found no prior statement" is a legitimate finding at measured grade.

5. **His artifacts, GitHub API.** Verify commit `a67970f` exists and links REQ-MATH-010/011 — repo not stated in the letter; try `ericmerle3789/one-obstruction-three-faces-lean`, the shared `macindoe/one-obstruction-three-faces`, then his public repo list. Summarize what the scripts actually contain (filenames, stated scope) — do not run his code. If the commit cannot be found, record that flatly as a question for the reply, not a suspicion.

6. **Record.** Everything into `briefs/merle-round5-check-findings.md`: per-item evidence with counts, ranges, seeds, dates; the summary section written so the author can lift verdicts directly (register flat). Include, drafted but NOT applied: (i) a candidate `cycles.md` remark — the near-miss anchoring of the four known cycles, the `|q| = 1` lock with the Gersonides citation, and the Benford side-asymmetry, all at their verified/measured grades, record-only, front stays parked; (ii) candidate ledger lines — L-A2 (the repeated-word gcd law, his proposal) and a candidate entry for the anchored-loops/Benford material (suggest L-A3, his artifacts + our key); (iii) the author-decision list: L-A2 elevation; the gateway-viz plan change (each side builds one, cross-link); whether the Benford brick and the spent-stock framing enter the note, and under what credit language (his flag (b) self-catch included, per the protocol's visibility rule). No wiki page is touched. No reply paragraphs — decisions come first (round-3 pattern); the findings' summary is the reply's raw material.

## Success / stop criteria

- **Floor:** items 1–3 (the mathematics his letter puts weight on) + item 6's record.
- **Primary:** all six items.
- **Stop:** after item 6, stop. Explicitly forbidden: any cycle-exclusion attempt beyond replicating his stated `k ≤ 10` map once (no larger `k`, no "just one more decade"); any Baker/linear-forms development; any new statistics campaigns beyond item 2's measurement; any Lean work; any edit to PROTOCOL.md or LEDGER.md in the shared repo (proposal lines live in the findings file only). Off-brief observations: log in the findings file, stop.

## Rules (non-negotiable, from AGENTS.md)

- Fresh code, one script `experiments/merle_round5_check.py`; exact integer/Fraction arithmetic for every pass/fail decision; floats/high-precision only for the density displays and threshold guards, labeled.
- Record what was checked, ranges, seeds, dates, inline in the findings file.
- Register norm: flat. His claims are his until verified; disagreements are findings, recorded exactly, not smoothed; agreements stated with evidence, not celebrated.
- Every citation in the findings verified against a live source this session; no citation from memory survives into the file unverified.
- Work on branch **`merle-round5-check`**, commit per item, do NOT merge — the main session reviews and re-runs before merging; the author decides and sends (standing convention).
- No scope expansion.

## Definition of done

The anchor table and the `|q| = 1` lock verified with the elementary proof recorded and Gersonides pinned; the envelope identity proved and checked; the Benford asymmetry verified at scale with both distance notions pinned and the self-catch adjudicated; the `k ≤ 10` map replicated exactly with the `B^k` sweep confirmed; the folklore verdict written with verified citations both ways; his commit checked via API; the findings file complete with unapplied proposal lines and the author-decision list; clean per-item commits on `merle-round5-check` with the script passing.

## Appendix A: the letter, verbatim (pasted by the author)

> Dear Ben,
>
> Your fore-runner paragraph did something for me I won't pretend it didn't. I'd read the JAR rejection — a bare status-flip to "rejected" in the Springer portal, no email, no reason, no reviewer report — as a verdict on the work; you reframed it as a verdict on the moment, and you're right: the resistance isn't to the mathematics, it's to who, and what, is allowed to find it. I'll hold that framing — show them what the tools can do, and hand them the helm where the details need hands ours can't provide. And the dance line — yes. The room is better with two.
>
> Quick acknowledgments before the mathematics. Both keys and the kernel on L-A1, verified from your side: that closes the first entry cleanly, and I'm glad the scope header read honestly — drawing that line early was the whole point. On venue, your read is exactly mine: a number-theory-shaped home with the formalization as the supporting artifact, not the reverse — and I've had the same diagnosis from an independent reader this week, which makes three of us converging on "the contribution was the conditional scaffolding and the shape of the obstruction, not the bound." On hosting, thank you plainly — I'll point the page at the Zenodo DOI (using your v2, 21421120, which I hadn't seen, and I'll host the mirror-paper pair). And yes to the visualizers as the intuition layer; the cycle-side gateway is the one my side should build in its own voice — let's each build one and cross-link, since that's how this collaboration does everything now.
>
> Now the part where our two probes turned out to be the same probe.
>
> Your prime-local result — R_0 mod ℓ structureless, no coset, no coarse law, 15M checks — reached me the same week I'd hit the identical wall from the arithmetic side, and together I think they say one thing cleanly. I was chasing a reader's question ("why can't a simple argument just forbid the loops?"), and the answer is that loops are real: 1, −1, −5, −17 all exist, each anchored exactly to a near-miss of the two towers — the −5,−7 loop lives on 8-vs-9, the very near-miss I'd once drawn as a picture, not knowing it was a real cycle. Any parity or speed argument that forbids loops forbids those too, so it proves too much. Fine — but then what kills the positive side? Here is the arithmetic mirror of your "no finite place alone will do it":
>
> The only free locks are the near-misses with |q| = 1, where q divides everything. Those are exactly three in all of history — 2-3, 4-3, 9-8 — and Mihailescu (Catalan, 2002) proves there will never be another. The alternation dealt one ticket north (4-3 → the house 1) and two south (2-3, 9-8 → −1 and −5). The fourth house, −17 (q = −139), is the single lottery win on the whole map. So the finite-pure mechanism isn't merely weak — it is a finite stock, spent since 9-8. After that, every candidate needs precisely the archimedean-to-finite coupling your probe forces. The free places are gone.
>
> Two things I'd flag honestly. (a) I suspect the Catalan-lock reading is folklore — do you have it on your side, or a reference? I don't want to re-announce a known thing. (b) One measurement I haven't seen stated anywhere: which side gets the better near-miss is not 50/50 — it is log₂(3/2) = 58.5% for the negative side, exact Benford. My script's first draft got this wrong (it said 50/50) before the machine corrected it — I mention that because it's the kind of self-catch we've agreed to keep visible. If that fraction is new to you too, it may be a genuine small brick for the note: the side-asymmetry the sign question was really about.
>
> Both scripts are in my stack (experiments/, commit a67970f): the anchored loops and the 256/243 no-cycle sweep in REQ-MATH-010; the mirror, the envelope q₊+q₋ = 2^⌊kL⌋, the Benford bias, and the exhaustive k ≤ 10 map (cycles = exactly {+1} and {−1,−5,−17}, one rotation per profile via L-A1) in REQ-MATH-011. And your B^k corollary already earned its keep here: the one false survivor my k=5 sweep threw up was 1 repeated five times — a B^k word — which gcd(q_P, R_0) > 1 now sweeps automatically. L-A2, clearly; footnote undersells it.
>
> The reader I keep quoting arrives at structure through metaphor. His last was Ulysses letting go of the helm to come home — which turns out to be your AEH exactly: the current that carries every boat to a port is the drift, and "letting go of control" is the equidistribution doing the steering. Collatz in one line of Homer — the sea always returns you to harbor, and Ithaca is the only harbor on the positive shore.
>
> Still dancing,
>
> Eric
