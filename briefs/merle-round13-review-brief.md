# Brief: round-13 review window — verify PR #3, replicate the peak spec, prepare the round's paperwork — for a delegated session

**Context required before starting (in order):** `README.md` (binding stopping rules), `AGENTS.md`, `HANDOFF.md` item 1 (note: its live-conditions paragraph is STALE — this brief's Provenance is the current state; part of your job is fixing it), `briefs/merle-round12-pr-findings.md` (the round-12 package this round answers, esp. §2 arithmetic and §6 review changes), `briefs/merle-la9-check-findings.md` (§6.4 — the offers h1–h5 verbatim; §3–§4 — the c*/deficit derivations), `briefs/merle-la7-rhin-check-findings.md` (the four Rhin carriers and the BILAN adjudication).

## Provenance

Merle approved and **merged both round-12 PRs himself** on 2026-08-17 (he is a collaborator; merge commits `424973f` = PR #1, `308d6bb` = PR #2, both authored `ericmerle3789`). Shared repo `macindoe/one-obstruction-three-faces` HEAD is now **`308d6bb`**. The same day he opened **PR #3** — his round 13, branch `round-13` at **`ef1742d`**, our approving review the second key per PROTOCOL §13. It carries three files:

1. **`LEDGER.md`** (+8/−4, all in the L-A9 section): offers h1–h4 applied to the claim block (single-convention headline with the `≈ 0.038` razor; the dream deficits relabelled to their true regime; the Barina parenthetical marked; the α scope with the `> 1/2` forever-threshold and the straddle), plus a dated acceptance block ("round-12 offers h1–h5 accepted") stating h5 is "accepted for the L-A8 (d-bis) block."
2. **`rounds/R13-merle.md`** (+142): the letter — L-A9 acceptance with his own independent check (`c* = 0.961722` cannot be a measure, margin `0.0383`); Rhin closed on his side, round-11 warning withdrawn, **BILAN_R201's R201-I3 "PROUVÉ" retracted in place at a third repo of his, `ericmerle3789/Collatz-Junction-Theorem`, commit `77e3f07`, "nothing deleted"**; §3 blanket acceptances (the 3,100-byte CRLF explanation, the 7→15→18 count, §10.2); §4 returns the frozen-Version-note flag as ours to remedy; §5 the item-2 peak-detector spec (below); §6 the §96 offer; §7 the note approved.
3. **`briefs/merle-breach-campaign-map.md`** (+85, new): a curated one-key digest of his 49-round negative campaign (journal `Collatz-Racine-Mur` §53–§96, local) — two "⊢" items (§96 finiteness of the quotient; the `θ = 1` Cramér–Lundberg boundary at `p = 3`), two "✓" cross-domain facts (the Erdős base-3 sieve; the `x* = 7/3` crossover), a seven-entry diagnostic map, and the Furstenberg closing pointer citing our `aeh.md` 13.6.7.

Two defects already spotted at pre-check, to verify and carry into the review draft:
- **h5 accepted but apparently not applied.** The LEDGER diff touches only the L-A9 section; no L-A8 (d-bis) hunk exists. Either the census-line replacement is deliberately left to us or it is an oversight.
- **The map's grading header is in tension with itself:** "Everything here is **Merle-side, one key**" vs. "Two items are theorem-grade and **verified cross-side this round** (⊢)". Cross-side verification is exactly what has NOT happened yet — unless he means this PR's review supplies it, which the wording should not presuppose.

**The author's decisions, on record for this window (2026-08-22):** (i) the Zenodo metadata remedy for the frozen Version-note defect is now ACCEPTED — Merle independently confirmed the defect and named the remedy, and the author has flipped the round-12 "deliberately unaddressed" decision; the execution (the Zenodo edit itself) stays the author's, this window drafts the text. (ii) The §96 earns-a-place answer is NOT yet decided — leave an author placeholder wherever the review draft needs it. (iii) The review click on PR #3 is the author's; nothing in this window turns a key or writes to any remote.

**Stopping-rule compliance:** verification of a co-edit round plus replication of a statistics spec on the continued fraction of `log₂3` — not a proof effort on the open condition, not a cycle search. Cycles front stays PARKED.

## Queue

1. **State check, read-only.** Fresh scratchpad clones: the shared repo (expect HEAD `308d6bb`; branches `round-13` at `ef1742d`, `note-v1-draft` at `96ccadf`, `round-12` at `accda4b`), his Lean repo (expect HEAD `d48ba9e`, unmoved — consistent with the retraction living in the third repo), and `ericmerle3789/Collatz-Junction-Theorem` (new to our record; record its HEAD). If anything has moved, record flat and continue read-only. Archive verbatim in the findings: his PR #1 review, his PR #2 review, and the PR #3 body (via `gh`, read-only) — these are the round-12 second-key artifacts and belong in our record.
2. **The LEDGER co-edit, number by number** (fresh code, `experiments/merle_r13_check.py`, exact integers for all `q`, controlled precision for logs; imports nothing of his; per-item commits). Check each h-application against the offer's own verbatim text (read h1–h5 from the merged round-12 `LEDGER.md` in your clone) and against `merle-la9-check-findings.md` §6.4. Recompute every number the new text states, old and new: the `0.0383` margin in both single conventions; `μ* ≈ 1.96` and the Hurwitz variant `1.92`; the at-the-floor factor `2.9` on `k` (h1 text) vs `2.93` (h2 text) — reconcile; the true-dream deficits (`≈ 2` on `k`, `1.96` at `κ = 1`, `~2^2` of computation); the Salikhov displacement claim (his `c = 5.125` is Salikhov 2007's measure of `ln 3`, not of `log₂3` — verify the citation); and the h4 additions, which are NEW numbers with no prior record on our side: the extended chord `0.5001` out to `2^2000`, the local-slope floor `0.32` at 30-bit widths with the band-exit below ~100 bits, and the `μ > ~2.05` widening threshold. For these three, re-measure with your own α machinery (the la9 findings §4 describe ours) — do not transcribe.
3. **The h5 gap.** Read the L-A8 (d-bis) block at `ef1742d` and confirm the seed-free census line is or is not applied; read h5's offered text; recommend the disposition (raise in review for him to fix before merge, vs. accept the prose and apply ourselves in the next round) and draft the review paragraph either way.
4. **The Rhin retraction, at its address.** In the `Collatz-Junction-Theorem` clone: verify `77e3f07` exists, read `BILAN_R201.md` at that commit, and check the three claims — R201-I3's "PROUVÉ" verdict retracted **in place**, **nothing deleted** (inspect the commit's own diff: annotative, not destructive), and our L-A7 record named as the cross-adjudication. Record verbatim the retraction text. This closes the last open item of the round-11 warning; say so explicitly in the findings so the L-A7 line in HANDOFF can carry it.
5. **The peak-detector spec — replicate, then correct the null** (`experiments/merle_r13_peak_replication.py`, fresh code, canaries printed first). His spec, verbatim from R13 §5: partial quotients of `log₂3`, kept to the first index where two working precisions disagree, capped at `N = 1800`; a peak is `a_i ≥ S = 10`; statistics: `variance/mean²` of inter-peak gaps (clustering, P4) and the max Fourier coefficient of the centred peak-indicator in units of its variance (spectral, P3).
   - *(a) Replication on `log₂3`.* His figures: 250 peaks, mean gap 7.19, clustering `0.831`, spectral `5.62×` at frequency 124. Reproduce with your own CF code (two precisions well above the canary threshold; state your dps pair and the stable-N you get). His controls (five i.i.d. Gauss–Kuzmin series: clustering band `[0.745, 0.949]`, spectral max `6.64×`) are draws without a recorded seed — do not chase his exact values; run a much larger control ensemble, record your own band, and say whether his band is consistent with it.
   - *(b) The corrected null he asks for* — the part that is ours to key once replicated: the same two statistics for **real continued fractions on a common footing** — `log₂5`, `log₂7`, `π`, each at the same `S` and the same stable-`N` protocol (and any further constants you judge useful; state the choice). Locate our memory-clause corrected-footing precedent in `briefs/` (grep; it is the method being mirrored) and follow its comparison discipline. Deliverable: does `log₂3` sit inside the real-CF band on both statistics, as it sat inside the i.i.d. band? Verdict either way, stated flat; then draft the ledger wording offer for the item-2 entry on his side ("yours to key once replicated" — the key recommendation is the main session's and the author's, not yours).
6. **The map and §96, at the grade they claim.**
   - *(§96 mechanism, reconstruction not reproduction.)* His artifacts (`run_048.py`, `run_049.py`) are local and unpublished; you cannot verify his runs. Instead check the skeleton independently with fresh code: for the accelerated `px+1` map, build the residue graph on odd residues mod `2^k`; confirm that a strictly-decreasing `V(x) = x·f(x mod 2^k)` forces `Σ(log p − v·log 2) < 0` around every directed cycle of that graph (the telescoping step — write it out in the findings); then search small `(p, k)` for cycles violating that inequality and classify which are phantom (edges integer-realisable, cycle not). Report whether the mechanism reproduces at `p = 7, k = 8` in your reconstruction, and at `p = 3` for small `k`, recording every operational definition you had to guess (edge realisability convention, faulty-cycle definition) as a gap, flat. This informs the author's earns-a-place decision; it is not a key.
   - *(The `θ = 1` boundary.)* Verify the algebra: `θ` the root of `(p/2)^θ + (1/2)^θ = 2`; at `θ = 1` this reads `(p+1)/2 = 2 ⟺ p = 3`. Note in the findings that the surrounding tail-model claim (excursion tail `R^{−θ}` under the §75 bijection) rests on his local artifacts and is not checked here.
   - *(The two ✓ facts.)* Verify with fresh code: `2^n ≡ 2 (mod 3)` for odd `n` (the one-line Erdős argument) and that the three known exceptions `n = 0, 2, 8` are even; the sieve density `(1/2)·(2/3)^{k−1}` with the exact-count check `k = 1..14` and the `99.743%` figure; `log₃2 = 0.630930…`; the `x* = 7/3` identities (`(3+3/7)/(3−3/7) = 4/3`, `log₂(4/3) = 2 − log₂3`). Also verify his citation into our wiki: `aeh.md` 13.6.7 exists and says what the map's Furstenberg paragraph says it says ("one missing genre of theorem" — quote it exactly).
   - *(The grading tension.)* Adjudicate the ⊢ header wording (Provenance, second defect) and draft one kind review sentence proposing a fix (e.g. "offered for cross-side verification this round").
7. **The round's paperwork, drafted (nothing sent, nothing pushed).**
   - *(The review draft.)* The full text of our approving review of PR #3, in the register of his two (verified-independently facts first, then the keys the review turns, then the raised items): the recomputed numbers; the Rhin retraction verified at `77e3f07`; the peak replication result including the corrected-null verdict; the h5 gap; the map's grading-header fix; and the author placeholders `[THE AUTHOR'S: the §96 answer]` wherever the earns-a-place question is engaged. The review turns the second key on the corrected L-A9 entry — say so in the draft's own words only if your verification supports it; if any number fails, the draft flags instead of approving and the main session re-plans.
   - *(The NOTE bracket patch, local-only.)* `NOTE-v1.md` on main line ~39 still carries `[GRADE AT SIGNING — re-read L-A9 before signature.]` and a "second key under review" grade sentence that expires when PR #3 is keyed. In a scratchpad clone, branch `note-grade-resolve` from `308d6bb`: one edit restating the grade sentence to the post-round-13 state (the Dirichlet half two keys; the measured half permanently a measurement) and deleting the bracket; nothing else on the page moves. Archive as a portable patch at `briefs/merle-round13-review-patches/` (format-patch; verify clean `git am` on a pristine second clone, record the tree hash). PR #3 does not touch `NOTE-v1.md`, so the patch survives its merge. The push and PR are the author's.
   - *(The Zenodo metadata note.)* Draft, in the findings, the exact text for the v3 record's additional-notes field: the PDF's internal Version note is a drafting-time self-description frozen at publication; this record is the publication it refers to; the maintained history is `paper/collatz-reduced-version-history.md`. Read the actual Version-note wording in `paper/collatz-reduced-v3.pdf` and the version-history file first so the note quotes itself correctly. Also draft (in the findings, not applied) the one-line version-history addendum to enter once the author has made the edit.
   - *(HANDOFF item 1.)* Rewrite the live-conditions paragraph and the L-A7/L-A9 ledger lines on your branch to the current state: both round-12 PRs merged 2026-08-17 by Merle himself (he holds and uses merge rights — record this as a live convention fact), shared HEAD `308d6bb`, PR #3 open at `ef1742d` awaiting our key, the third repo `Collatz-Junction-Theorem` with the R201-I3 retraction at `77e3f07`, the peak replication ours-to-key with this window's verdict, the §96/campaign-map offer pending the author's answer, the Zenodo decision flipped 2026-08-22 with the drafted text's location, and the NOTE bracket's prepared patch. Do not restructure anything else in the file.

## Record

- `experiments/merle_r13_check.py` and `experiments/merle_r13_peak_replication.py` + committed outputs (canaries printed first; per-item commits).
- `briefs/merle-round13-review-findings.md` — the verbatim archives (three review/body texts; the retraction text; the h-applications against their offers); every recomputation with its derivation written out; the h5 and grading-tension adjudications; the §96 reconstruction with its guessed-definitions list; the peak replication and corrected-null verdict; the four drafts (review, NOTE patch reference, Zenodo note, version-history addendum).
- `briefs/merle-round13-review-patches/` — the NOTE bracket patch, `git am`-verified, tree hash recorded.
- `HANDOFF.md` — item 1 only, per Queue 7. (Single window this round; no race.)

## Rules

- Branch **`merle-round13-review`** from your worktree HEAD. FIRST verify the worktree contains this brief (`briefs/merle-round13-review-brief.md`); worktrees are sometimes cut from a stale HEAD — if it is missing, `git merge main --ff-only` before anything is read, and state your base SHA in the findings.
- Read-only everywhere outside this repo: fresh clones of the three Merle-side repos in the scratchpad; `gh` for the PR/review texts, read-only; no pushes, no comments, no reactions, no fork/issue/star/watch, no contact with anyone; web only for the Salikhov citation if needed.
- File edits via the Edit/Write tools only — never PowerShell `Get-Content`/`Set-Content` (HANDOFF quirks; the repo's `—`/`≤`/`ε` are destroyed silently). Run `experiments/encoding_scan.py` before your final commit and record RESULT: CLEAN.
- Long computations run in the foreground of your session — do not detach them and do not end the session until your final commit exists on the branch.
- Discrepancies recorded and flagged, never disputed in prose; every defect found is a finding delivered kindly — the register of his own two reviews is the model.
- No key turns (recommendations and drafts only), no merges — the main session reviews (re-runs both scripts) and merges; the review posting, the push of the NOTE patch, and the Zenodo edit are the author's. Stop after Record.
