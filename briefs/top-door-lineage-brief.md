# Brief: the top-door lineage as an object of its own — the odd stratum of the cycle equation, run lengths, and the checkable question "must a cycle be top-door?" — for a delegated session

**Context required before starting (in order):** `README.md` (strategy and **binding stopping rules** — no per-period cycle search, the cycle front reopens only with a divisibility-aware idea), `AGENTS.md` (house norms: nothing is labeled proved without independently written verification code; no change logs in tracked files; every fact lives in exactly one page; content and structure in separate commits), `HANDOFF.md` (register norm; the delegation pattern; the PowerShell quirk), `spine.md` §9.8 (the reduced map `F`, states `(ω, d)`, the exit `e`, the next state through `e + 1 = 2^m 3^a Ω`, the 3-gain `a`, the depth `d = m + a_(prev)`), `stage2.md` 11.8.5 (the 3-gain law `a = v₃(2^s − 1)` off resonance, `h(s)`; the density of `s`), `reverse.md` 14.1.1 (the predecessor characterization: doors `y_a`, the parity constraint on `s`), 14.5.1–14.5.2 (door mortality; Gardens of Eden), 14.6 (the rigorous density tree — note which door it is built on), 14.6.5 (the multi-door lift), 14.8.1–14.8.3 (the `a = 0` representative `y₀`, the top-door increment law, the freeze), `cycles.md` §12 preamble, Proposition 12.6.1 and Remarks 12.6.1.1–12.6.1.7 (the reduced cycle equation, the transport recurrence, the parked condition `q | R₀`, the `|q| = 1` stock, the odd-step stratum's margin in 12.6.1.5), Remark 12.8.3 (the staircase family; the `p = 7` seed) and Consequence 12.8.5 (the stopping rule and its reopen clause), `itinerary.md` 14.15.1.1 and 14.15.9 (the door odd part; the whole-period height laws and the `q`-dichotomy), `open-problems.md` 11.6 and 11.12 (the form of an open entry phrased so that closure is checkable), `TOUR.md` (the dictionary of coined terms — this brief coins two), `symbols.md` (registry; `a`, `m`, `q`, `s` collide — use the registry's frames), `index.md` (resolver granularity), and `briefs/modq-spectrum-brief.md` with its findings file (the rule this brief works under: identities on profiles, no cycle search; the form of a brief, a findings file and a per-item commit stream on this repository).

## Provenance

The author's proposal of 2026-09-10, with the terminology fixed by him: the `a = 0` representative `y₀` of 14.8.1 is the **top door**; the `a ≥ 1` representatives are **side doors**; a **top-door lineage** is a backward path through top doors only. Forward, the same object is an orbit with no 3-gain at any step. Pre-checked in the main session with throwaway code (deliberately not filed; you write the script of record from scratch), 0 failures throughout. Every claim below is **a claim to be re-derived and re-verified here, not a fact**:

1. **Aliveness and parity.** The top door of `(Ω, D)` is alive iff `y₀ ≡ 1 (mod 3)` (14.5.1 read at `y₀ = 2^D Ω − 1`), and then every admissible branch `s` is odd (14.1.1's parity constraint; 73,796 checks). Forward: `s` odd ⟺ `a₊ = 0` off resonance (stage2's law: `v₃(2^s − 1) = 0` iff `s` odd; 19,904 checks), so a top-door orbit has `d_t = m_t` at every state — depth equals entry depth, no `3^a` factor anywhere.
2. **The known instances are all top-door.** The cycle at `1` is `(1,1)`; `−5` is `(2,1)`; `−17` is the `G`-period-2 word `((4,1),(3,3))`; all `s` odd. The `p = 7` staircase seed of 12.8.3 has `s = (1,1,1,1,1,1,49)`, all odd. A census of all profiles with `p ≤ 4`, entries `≤ 6` and `q | R₀` returns twelve profiles: exactly those three words and their repeats (12.6.1.4's descent). **This is no evidence beyond the known instances; say so.**
3. **Standing in the record — pointers, not restatements.** The mortal lineage of 14.5.1 and 14.8.3 (half of all top doors are dead, so top-door lineages die backward at rate `1/2` per step); the rigorous density tree of 14.6 is built on the opposite door (`m = 1`, never dead) with the side doors `a = 1..d−2` as the 14.6.5 lift, so **the proved density lives on the side doors, not the top**; the odd-step stratum of 12.6.1.5 is the top-door stratum by another name (margin `0.267` per step against the general `0.079`).
4. **To do (a): the top-door cycle equation.** Write Proposition 12.6.1 and the transport recurrence 12.6.1.1 on the odd stratum: `d_t = m_t`, `a_t = 0`, so `ω_r · q = R_r` with no `3^a` factor, `n = Σ m_t`, `K = Σ s_t + n`, all `s_t` odd. Record what simplifies and what does not (the size conditions `q ≤ R_r` still vary by rotation; the gcd invariance is unchanged). One remark on cycles.md, verified on the three known cycles as canaries and on random odd-stratum profiles against the general identity.
5. **To do (b): realized top-door run lengths.** Along forward orbits of random odd starts (state a range and a seed), the run length of consecutive `a = 0` steps against the ledger rate: derive the rate from the density of `s` (geometric with ratio `1/2` under the record's law — cite where it is stated), `P(s odd) = 2/3`, so runs are geometric with ratio `2/3` under independence. Report the realized histogram against `(2/3)^n` as calibration, at bulk scale; flat prose, no inference beyond "consistent" or "not consistent, by this much".
6. **To do (c): the checkable question — must a cycle be top-door?** State it in open-problems.md so that closure is checkable: *either a profile `(m_t, s_t)` with some even `s_t` and `q | R₀` (under 12.6.1.1 this is one condition), or a reason none exists.* Under the mod-q rule: identities on profiles, **no cycle search** — the bounded census of item 2 (`p ≤ 4`, entries `≤ 6`) may be reproduced as a canary and **must not be extended**. Record the calibration flatly: no mechanism is in hand; the `3^(Σa)` bookkeeping is automatically consistent for any realized segment, so a proof would have to come from closure, not from the per-step laws; a positive answer would be a divisibility-aware statement of the kind 12.8.5's reopen clause names, and would confine the parked condition to the odd stratum. If you see a mechanism, record it as a candidate with its obstruction and stop — do not pursue it.
7. **Flat note.** The two guaranteed-neighbour laws of a parallel delegation (`briefs/ladder-family-graph-brief.md`) are top-door (`s = 1, 3`), because `1 + 2^s = 3^k` forces `s` odd. One clause, pointing at ladder.md 15.7 (which does not exist on your branch — write the clause with the pointer and the main session checks it at merge).

Grade at delegation: **a named object with its cycle-equation form and one open question, phrased checkably; no front moves; the cycle front stays parked; stopping rules untouched.**

**Stopping-rule compliance:** identities on profiles and orbits; the only enumeration is the pre-check's bounded census as a canary; no per-period search, no exclusion attempted, no proof effort on equidistribution. The front stays parked whatever comes out.

## Queue

1. **Mathematics first, on paper in the findings.** Statements and proofs for items 1 and 4 from the definitions; the run-length rate of item 5 derived; the open question of item 6 drafted in its checkable form before any computation. Where a pre-check claim is wrong or needs a boundary case (resonance in item 1 — `s` odd but `3 | 2^s − 1` never happens, `s` even with `a = 0` never happens; check the statement at the resonant `s`), record the corrected statement and what was wrong; **a claim is not dissolved by naming its type** — every correction carries a re-derivation.
2. **Fresh verification code — `experiments/top_door_lineage.py`, committed with its output.** Imports nothing from any existing script (not `modq_spectrum.py`, not `margin_asymptote.py`; the main session's scratchpad code does not exist for you); exact integer arithmetic at every pass/fail decision; canaries first (the three known cycles as top-door words; `(1,1)`; Proposition 12.6.1's sanity identity for the fake period-`p` trivial cycle). Contents: (a) item 1 both directions on random states; (b) item 2's census at exactly the pre-check's bounds, listing the twelve; (c) item 4's odd-stratum equation against the general one on random odd-stratum profiles and on the three known cycles; (d) item 5's run-length histogram with the seed, range and the `(2/3)^n` column; (e) item 7's clause. **One documented command reproduces the committed output in full** (`python experiments/top_door_lineage.py`, no phases, no flags needed for the record). Run it in the foreground and wait for it. Record counts, ranges, seed, date.
3. **Wiki edits — conservative; content and structure in separate commits.**
   - `reverse.md`: a new **Remark 14.8.4 (top door, side doors, the top-door lineage)** after 14.8.3: the definitions (the author's terminology, one sentence each), item 1 with its verification line, item 3's three pointers (mortal lineage; the density tree built on the opposite door; the odd stratum of 12.6.1.5). Front matter `updated`; **no change to the Current-state paragraph** (a parallel branch may touch this page's front matter; keep your edit to 14.8.4 and the date).
   - `cycles.md`: a new **Remark 12.6.1.8 (the odd stratum: the top-door cycle equation)** after 12.6.1.7: item 4 with its verification line, item 2's census as the calibration sentence (twelve profiles, the known words and their repeats, no evidence beyond), item 5's run-length calibration in one sentence, item 7's clause. Front matter `updated`; the Current-state paragraph unchanged (the front's status does not move).
   - `open-problems.md`: a new entry **11.13. Must a cycle be top-door?** in the form of 11.12: the question in checkable form, what is known (items 1–4 by pointer), the calibration of item 6, the reopen-clause remark. Front matter `scope` gains the 11.13 clause in the existing style; `updated`.
   - `TOUR.md`: one dictionary row for *top door / side doors / top-door lineage* (literature name: none; the nearest object is the `a = 0` branch of the Syracuse preimage tree — say only what is true), pointing at 14.8.4. Nothing else on that page.
   - `symbols.md`: no new symbol; if you must name the stratum, use words.
   - `index.md`: add 14.8.4 / 12.6.1.8 / 11.13 to the resolver only if it lists at that granularity; otherwise leave it.
   - `briefs/top-door-lineage-findings.md`: the derivations, what the pre-check got wrong (if anything), the verification record, the census list, the run-length table, and a *For the main session at merge* section (the 15.7 pointer to check; any front-matter seam with the parallel branches).
   - **Do not edit** `HANDOFF.md`, `ladder.md`, `anchors.md`, `aeh.md`, `README.md`, `publication.md`, `paper/`, or anything under `sources/`.
4. **Compliance.** Run `experiments/encoding_scan.py` before the final commit and record `RESULT: CLEAN` in the findings.

## Record

- `experiments/top_door_lineage.py` + committed output (canaries first; one reproducing command).
- `reverse.md` 14.8.4; `cycles.md` 12.6.1.8; `open-problems.md` 11.13; the `TOUR.md` row; the resolver lines if applicable.
- `briefs/top-door-lineage-findings.md` as specified.

## Rules

- Branch **`top-door-lineage`** from your worktree HEAD. FIRST verify the worktree contains this brief (`briefs/top-door-lineage-brief.md`) and that `git log --oneline -1` shows a commit dated 2026-09-13 or later; worktrees are sometimes cut from a stale HEAD — if either check fails, rebase onto local `main` before starting, and state your base SHA in the findings.
- No web access needed; no contact with anyone; no pushes; no edits outside this repository.
- File edits via the Edit/Write tools only — never PowerShell `Get-Content`/`Set-Content` (see HANDOFF quirks; the repo's `—`/`≤`/`₃` are destroyed silently).
- Register: flat, calibrated prose. The census is no evidence beyond the known instances; the question is open; say both flatly. Heuristics labeled heuristics; nothing labeled proved without the fresh code of Queue 2 **and** a written proof.
- Record obstructions; do not force a mechanism for item 6.
- Run the script in the foreground and wait for it; do not stop until every commit exists on the branch.
- Do NOT merge — the main session reviews (re-runs the script, re-derives item 4, reads 11.13 for checkability) and merges. Stop after Record.
