# Brief: round-8 verify + record (for a delegated session)

**Context required before starting (in order):** `README.md` (binding stopping rules — compliance note below), `AGENTS.md` (verification protocol: fresh independent code, exact arithmetic at pass/fail decisions, failures recorded not deleted), `HANDOFF.md` item 1 (through the round-7 shipped state), `briefs/merle-round7-check-findings.md` (the round-7 verification record, including the addendum with shared-repo HEAD `61d2cf3` and the exact NOTE.md/LEDGER.md texts at that point), `cycles.md` 12.6.1.1–12.6.1.3, `itinerary.md` 14.15.5–14.15.6 (the pinned sections his ledger now cites).

## Provenance

Eric Merle's eighth reply (pasted verbatim by the author, 2026-07-24). Round 7 is closed by his opening (reply received, cross-link and visibility record accepted). The letter's claims, in his structure:

1. **L-A4 seeded** — "Every periodic profile (a repeated word in reduced coordinates, in the tuned regime) is a cycle iff its base is: `q_P | R_0(P) <=> q_B | R_0(B)` — the fixed-point inheritance of your L-A2, one level up. 3,600/3,600 exact, canary = trivial-cycle inheritance." Consequence he draws: no cycle can hide in a structured (periodic-profile) family; any nontrivial cycle must be genuinely aperiodic. Seeded for our co-edit.
2. **Congruence door closed, renamed** — the odd-step stratum "carries no gcd/congruence rigidity the general family lacks (measured, not asserted)"; the near-integer mass is a size artifact — "94 to 98 percent of it comes from the degenerate `R_0/q < 1` region, where no cycle can live"; the residual gap is "the equidistribution of `R_0` mod `q` along the generic family."
3. **L3 correction (his own Face III)** — the session-2 "purely global" reading (solvable over R, Z_2, Z_3, insoluble only over Z) is retracted as imprecise: it tested only the structural primes. For a linear divisibility `q | R` there is no Hasse gap — `q | R` iff `v_p(R) >= v_p(q)` at every `p` — so a global failure forces a local one. His instance: `p = 7` has `7 | q` with `v_7(q) = 3 > v_7(R_r) = 1` at every rotation, failing already in `Z_7` (gcd = 7). He asserts L3's two-key data (the distance profile) is untouched; the correction is interpretation only, kept in L3, "yours to weigh."
4. **Both round-7 offers accepted** — (A) the signed characterization (our Theorem 14.15.6.7) as the frame; (B) the digit-match-ceiling identification (cycles.md 12.6.1.3), now quantified his side: "the capacity-demand margin is about `0.27 n` in the odd-step stratum (about `0.08 n` in general), positive and growing linearly, so the no-conspiracy cycle count decays like `2^(-margin)`." Both "in the ledger"; our "positive **odd** integer" precision "folded into section 4."
5. **Prime-7 isolated** — 5, 11, 13, 31, 127 all equidistribute; 7 = 2^3 − 1 is a solitary special case; mod 7 alone is a weak lever.
6. **State pins** — shared repo `github.com/macindoe/one-obstruction-three-faces` at commit `b8842bb`; "Merle-side scripts at `017288f`" (five scripts, canaries green — repo not named; his known repos are under handle `ericmerle3789`, including `one-obstruction-three-faces-lean`). NOTE.md sections 4 and 6 rewritten accordingly.

SHAs and digits above are author-verbatim paste (the safe channel per HANDOFF quirks); a mismatch at clone time is a finding, recorded and flagged, never "corrected."

**Stopping-rule compliance (same footing as `briefs/merle-pincer-check-brief.md`):** every computational item below is independent re-verification of a correspondent's stated claims, plus clean-room re-derivation of a proposed ledger entry — not a per-period cycle search, not a divisibility-based exclusion attempt, not an equidistribution proof effort. The cycles front stays PARKED throughout; nothing in this brief reopens it. Register norm applies: his `2^(-margin)` decay sentence is heuristic language — if quoted, it is quoted as heuristic.

## Queue

1. **Verify the shared repo state** (no commit; feeds item 4). Clone `github.com/macindoe/one-obstruction-three-faces` read-only into the scratchpad (public since 2026-07-24; if the clone fails unauthenticated, record and flag; do not authenticate around it). Record HEAD and whether it is `b8842bb`. Against the letter:
   - (i) **L-A4 entry** — exact current text; its stated claim vs the letter's (statement, check count 3,600/3,600, canary); which keys the entry says have turned (expected: his only).
   - (ii) **L3** — the correction text; confirm the original seed-entry content (the distance profile and its data, per the round-3 record and ledger history — use `git log`/`git diff` on the clone) is untouched, i.e. the correction adds interpretation without altering recorded data. Any data change is a flag.
   - (iii) **L-A3** — the accepted addition (B): does the entry now carry the digit-match-ceiling identification, does it cite cycles.md 12.6.1.3 accurately, and does its margin quantification match the letter's numbers; record whose keys the addition carries.
   - (iv) **NOTE.md sections 4 and 6** — exact current text of both; the signed-characterization frame (A) — does it cite Theorem 14.15.6.7 / §14.15.6 accurately; the "positive odd integer" fold-in — present and correctly placed; section 6's equidistribution naming — record the exact sentence.
   - (v) Record every commit hash relied on, and the diff range `61d2cf3..HEAD` file-by-file (names + one line each).

2. **Locate and record the Merle-side scripts** (no commit; feeds items 3 and 4). Check his known public repos (`ericmerle3789/*`) read-only for a commit `017288f`; if found, record repo, script names (letter says five), and what each claims to check — read only, do not run his code as verification (it may inform, never substitute). If `017288f` resolves nowhere public, record and flag; no web search beyond GitHub.

3. **Independent computational verification** (fresh code, committed; this is the round's substance). One script, `experiments/merle_round8_check.py`, importing nothing from his repos and nothing from prior Merle-check scripts; exact integer arithmetic at every pass/fail decision; canaries hand-computed first and printed before the sweeps. Three parts, in priority order:
   - (a) **L-A4 clean-room** (our prospective key; do the derivation before reading his entry text more than needed for the statement). From the transport recurrence (cycles.md 12.6.1.1, `R_{r+1} = (3^{m_r} R_r + (2^{s_r} − 1)q) / 2^{σ_r}`), derive the base-inheritance claim for a repeated word `P = B^k` in the tuned regime: state precisely what `q_P`, `R_0(P)` are in terms of `q_B`, `R_0(B)` and prove or verify the biconditional `q_P | R_0(P) <=> q_B | R_0(B)`. Then verify on our own grid — all words `B` up to a stated size bound × repetition counts `k` (target the same order as his 3,600 checks; state the grid explicitly), canary = trivial-cycle inheritance exactly as he names it, plus one canary of our own choosing. Record derivation, grid, counts, failures (expected 0; any failure is a flag, not a fix).
   - (b) **L3 correction instance.** State the no-Hasse-gap principle for linear divisibility (elementary; one paragraph, it needs no computation). Then re-verify his `p = 7` instance from our own records of that case (the round-3 record and the recorded `p = 7` staircase seed): compute `v_7(q)` and `v_7(R_r)` at every rotation independently and check `v_7(q) = 3 > v_7(R_r) = 1` at all of them. Record exact values.
   - (c) **Margin measurement.** Define the capacity-demand margin exactly as the ledger entry defines it (record that definition verbatim; if the entry's definition is ambiguous, flag the ambiguity and measure the closest well-defined reading, stating it). Re-measure independently on both families (odd-step stratum, general) at a stated sample size; record our numbers next to his `≈0.27 n` / `≈0.08 n` with agreement or discrepancy flagged. Do NOT extend into exclusion claims; this is measurement replication only.
   - (d) **Door-closure spot checks** (reduced scale, explicitly labeled spot checks, not key turns): the near-integer degenerate-mass fraction (his 94–98% from `R_0/q < 1`) and the equidistribution-vs-bias contrast for one prime from each list (e.g. 5 vs 7) — enough to say "consistent with the letter at reduced scale" or flag, no more.

4. **Record** (commits on the branch, per-item discipline):
   - `experiments/merle_round8_check.py` + its run output (one commit; the findings file cites its counts).
   - `briefs/merle-round8-check-findings.md` — the verification record: items 1–3, findings only, flags separated from confirmations, hashes/texts verbatim, the L-A4 derivation written out (it is the co-edit substance), and a one-paragraph adjudication per letter-claim: confirmed / consistent-at-reduced-scale / flagged.
   - `HANDOFF.md` item 1 update — round-8 received (pasted verbatim); round-7 closed by his opening. New state per findings. Owed our side: (a) L-A4 co-edit + our key turn in the shared LEDGER (separate delegate; **push only on the author's explicit go-ahead**); (b) our key on the corrected L3 (same gate); (c) reply (drafted after a–b settle; sending stays with the author). Keep the Repos-and-ledger facts paragraph current (L-A4 seeded one-key; L3 corrected, data untouched if verified; L-A3 addition landed; NOTE §4/§6 rewritten; new shared-repo HEAD).

## Rules

- Branch **`merle-round8-check`**, commits as itemized in item 4, do NOT merge — the main session reviews and merges (and will re-run the verification script before merging).
- Read-only everywhere outside this repo: no pushes to the shared repo or any Merle repo, no edits to `viz/`, no web actions beyond GitHub clones/reads of the named repos. If any push or external write seems needed, stop and hand back.
- Discrepancies (digits, hashes, texts, measured numbers) are recorded and flagged, never disputed in prose and never silently reconciled.
- Obstructions are recorded, not forced: if the L-A4 derivation does not go through as stated, or a definition is ambiguous, that is a finding — write it down and continue to the next item.
- No reply paragraphs; no shared-LEDGER key turns; no scope expansion; stop after item 4.
