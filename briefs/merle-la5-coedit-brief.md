# Brief: L-A5 co-edit — our verification block + the gloss restatement offer (for a delegated session)

**Context required before starting (in order):** `README.md` (binding stopping rules), `AGENTS.md`, `HANDOFF.md` item 1 (through the L-A5-verified state), `briefs/merle-la5-check-findings.md` **entire** (the evidence base: per-claim adjudication, the T1/T2 clean-room derivation, the `−17` exhibit, the exact proposed co-edit language, the flags), the shared repo's `LEDGER.md` L-A5 entry at `740edb3` (verbatim in the findings), `briefs/merle-round8-coedit-patches/0001-*.patch` (the L-A4 key-turn block — the style model: appended verification block + offers marked as offers, acceptance his call).

## Provenance and authorization

Author go-ahead 2026-07-25 for the L-A5 co-edit round, prepared LOCAL-ONLY: the push happens from the main session in manual permission mode on the author's word after review — **no pushes from this session anywhere**; if one seems needed, stop and hand back. The reply draft is a separate round after this one.

**Stopping-rule compliance:** this round records a completed verification into the shared ledger and offers a scope correction to a correspondent's gloss — no cycle search, no exclusion attempt, no equidistribution proof effort; the cycles front stays PARKED. Register: flat; the correction is offered exactly the way his L3 correction and our "positive odd integer" precision were — the mathematics is his, the restatement makes the lemma's true strength visible; nothing in our block may read as triumph.

## Queue

1. **Verify the shared-repo state.** Fetch/clone read-only into the scratchpad; confirm HEAD is still **`740edb3`**. If it has moved: if the movement is pure-additive and does not touch the L-A5 entry, proceed re-seated on the new HEAD and record; if the L-A5 entry itself changed, STOP the prep (record the new entry text and a delta adjudication in the findings, hand back — the main session re-adjudicates).

2. **Prepare the co-edit commit** (scratchpad clone, local branch `la5-coedit` from HEAD; author identity `macindoe <begemite0.o@gmail.com>`, matching prior practice; `LEDGER.md` only; ONE commit). Append to the L-A5 entry, after his artifacts paragraph, a block in the L-A4 key-turn style, containing — draft the exact prose yourself from the findings, keeping every number citable:
   - **Our verification record** (dated 2026-07-25): invariant confirmed (rotation-invariance and `C = 1 ⟺ q | R_0`, 600/600 each, both signs, tuned and untuned); landscape consistent at reduced scale, with the `B^j` climb made exact via the descent identity (`1 − C(B^j) ~ c/j`, climbing for **every** base; `C(B^j) = 1 ⟺ C(B) = 1` — the L-A4 identity's corollary); **T1/T2 re-derived clean-room** from the `W0` fold alone, closed forms matching his exactly, verified 2,025 + 2,025 exhaustive + 900 + 900 random + the `R_0`-frame forms, corollary 664/664, all 79 unit-seam cases at shared content exactly 1. Lean artifact status, flat: read, statements matching the entry clause-by-clause, no `sorry`/`native_decide`/user axioms in the text; not built our side; the committed `#print axioms` output would close the kernel-3 claim (stated as an invitation, not a demand). Artifact: `macindoe/collatz` `experiments/merle_la5_check.py` with committed output (commit `a87b94a`, on `main`; ~10,372 exact decisions, 0 failures).
   - **The exhibit, stated flat:** the `−17` cycle's word (`m = (4,3)`, `s = (1,3)`, `q = −139`, `R_0 = 139`) is primitive, untuned, at `C = 1` exactly — and **totally isolated**: shared content 1 with all six adjacent-transfer neighbours, forced by the corollary itself (`gcd(139, 3³ − 2³) = gcd(139, 19) = 1`). The separation lemma's showcase: a peak with no shoulders, realized.
   - **Offer (a) — the gloss restatement** (the entry's final two sentences, replacement offered, acceptance his call): use the exact corrected language from `briefs/merle-la5-check-findings.md` (the long form; quote it faithfully, adjusting only what grammar requires). The framing sentence before it, flat: T1/T2 prove adjacency separation — no word is *connected* to `C = 1` by one-unit transfers — not non-existence; unscoped, the `−17` pattern realizes an isolated aperiodic peak; tuned-scoped, existence of such a peak is exactly the parked condition `q | R_0`, NOTE §6's residual gap.
   - **Offer (b) — domain note:** `C` is 0/0-undefined at `|q| = 1` (exactly the spent-stock words, L-A3/12.6.1.2/12.6.1.3); a one-clause domain restriction (or the convention `C := 1` there, his call) closes the interval claim.
   - **Key status, honestly:** our key turns on the invariant, the landscape, and the separation lemma (T1/T2 + corollary) as verified; on the entry's closing gloss it turns **with offer (a)** — i.e. the entry reaches **two keys** upon his acceptance of a restatement (his own wording equally welcome); status line stays DRAFT with this stated until then.
   - Export the commit as `git format-patch` into **our** repo at `briefs/merle-la5-coedit-patches/`, committed.

3. **Record** (branch commits, per-item):
   - `briefs/merle-la5-coedit-findings.md` — the prepared block verbatim, the local commit hash, the patch filename, judgment calls, and the explicit line: **shared repo NOT pushed; push gated on the author's word from the main session (manual mode)**.
   - `HANDOFF.md` item 1 Pending update — L-A5 co-edit prepared local-only (patch in-repo), awaiting the author's push go-ahead; then the reply draft (carries: round-8 key turns pushed, the margin asymptote constants, the solitary-7 refinement, the L-A5 verification with the `−17` exhibit and the gloss offer; sending stays with the author).

## Rules

- Branch **`merle-la5-coedit`** from your worktree HEAD (verify `9366d49` or a descendant; state the base in the findings). Per-item commits; do NOT merge — the main session reviews and merges.
- No pushes anywhere; shared repo gets one local commit + patch export only. Any push impulse = stop and hand back.
- The offered restatement must keep his lemma's strength fully visible — the correction narrows the gloss, not the theorem; no sentence of ours may read as scoring a point.
- Discrepancies/obstructions recorded, not forced. No reply paragraphs; no NOTE.md edits; no scope expansion; stop after item 3.
