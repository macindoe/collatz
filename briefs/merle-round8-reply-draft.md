# Round-8 reply — business paragraphs (draft)

Drafted per `briefs/merle-round8-reply-brief.md`, 2026-07-25. Conventions as round 7:
the personal opening and closing are the author's own — these are the business paragraphs
only, for the author to fold in, reorder, or cut. Every fact below exists at the named
section; numbers are quoted from `briefs/merle-round8-check-findings.md`,
`briefs/merle-round8-coedit-findings.md`, `briefs/merle-la5-check-findings.md`, and
`briefs/merle-la5-coedit-findings.md`. Paragraph order: his letter's own order for the
three answered items, then the gift-B extension and the solitary-7 refinement, then the
L-A5 arrival where it happened in time, and the map of where everything lives last.
Sending stays with the author.

---

L-A4 is verified and our key is turned — the entry now reads two keys. The clean-room
derivation, from 12.6.1's conventions alone, came out one strength level up from the
biconditional: it falls out of a multiplicative identity, `R_0(B^k) = R_0(B)·(q_P/q_B)`,
numerator and seam gap scaling by the same positive integer — and the identity needs no
tuning hypothesis at all, both signs of `q`, every profile with entries ≥ 1. So "the
fixed-point inheritance of L-A2, one level up" is exactly right: the gcd law re-derives
from it in one line. Verified at 12,888 exact checks, 0 failures, your trivial-cycle
canary run first. Two offers sit inside the entry, both your call and neither folded in:
the scope can widen to match the mechanism (or stay as the application's stated scope, as
you prefer), and "primitive" — not a proper power of a shorter word — is offered as the
finite-word-correct term for your "genuinely aperiodic," in the same spirit as the
"positive odd integer" word from round 7.

Your L3 correction is accepted into the two-key record, verified exactly as you stated
it: `v_7(q) = 3 > v_7(R_r) = 1` at all 7 rotations, `gcd(q, R_r) = 7` everywhere, the
closure equation insoluble already in ℤ₇; the diff is pure addition, and the recorded
distance profile recomputes digit-exact (`[0.0538, 0.4784]`). One sentence on the manner
rather than the mathematics: you corrected your own entry unprompted, data kept and
interpretation repaired in place, and that register is the spine of this whole exchange.

The congruence-door closure and your naming of the residual gap both check out at our
reduced scale: the degenerate-mass fractions came out 0.94 and 0.97 against your 94–98%,
and the 5-versus-7 contrast reproduced. One bookkeeping note our side: "equidistribution"
now names two different statements in this program's records — AEH's orbit-statistics
form and your family statement about `R_0 mod q` — and the disambiguation is recorded at
aeh.md 13.6.7 so the two never get conflated in anyone's prose, ours included.

Your quantification of (B) now has exact limits. The margin per step converges,
elementarily (entropy bounds on the binomials, plus the largest-term sandwich for the
stratum sum): `margin/n → c_gen = β − β·log₂β + (β−1)·log₂(β−1) = 0.0793186…` for the
general family, and `c_strat = 0.2667875…` for the odd-step stratum (variational form,
interior maximum at `α* = 0.3747344…`), with the exact counts approaching both constants
monotonically from above through `n = 163,840` (`0.0794`/`0.2669`) — and your grid
reproduced digit-exact on the way (`0.2730`/`0.0854` at `n = 1280`). The operational
definition is now pinned into L-A3 from REQ-MATH-014, with one vocabulary clause joining
your capacité/demande to our capacity/demand pair under the one pinned name. The
asymptote is offered in the entry; your acceptance completes (B)'s two keys. Attribution
as the ledger states it: the quantification is yours, the constants are ours.

On the solitary 7, a refinement offered flat, because the structural core is stronger
than your letter claims for it: it is provable that 7 is the *unique* prime with the
minimum possible 3-element `2^s − 1` orbit (`ord_p(2) = 3` forces `p | 7`), the maximal
collapse — hence the largest and most robustly detectable bias. But "solitary" is
resolution-dependent: at `N = 30,000`, mod 5 in your own window also crosses your
significance bar (total variation 0.013 against 7's 0.015), and exhaustive small-`n`
family aggregates even reverse the headline. So what is unique to 7 is the mechanism,
not the detectability. Your conclusion — structural, not a lever — survives unchanged
and gains its mechanism; the whole table is at cycles.md 12.6.1.6 for your reading.

Your L-A5 seed arrived while our three key turns were mid-push; they were re-seated over
it, and the history keeps the sequence honest. Then we read it properly, and it is
verified: the invariant confirmed exact (rotation invariance and `C = 1 ⟺ q | R_0`,
600/600 each, both signs, tuned and untuned), and T1/T2 re-derived clean-room from the
`W0` fold alone — the closed forms match your Lean statements exactly, 2,025 + 2,025
exhaustive and 900 + 900 random, corollary 664/664, with all 79 unit-seam cases at
shared content exactly 1. The anteriority sweep and the credit framing are received as
stated, with thanks.

And your lemma has a showcase, which we computed rather than imagined: the `−17` cycle's
word is primitive, untuned (`K = 11`, `q = −139 < 0`), at `C = 1` exactly — and totally
isolated, shared content exactly 1 with every one of its six adjacent-transfer
neighbours, forced by your own corollary (`gcd(139, 3³ − 2³) = gcd(139, 19) = 1`). A
peak with no shoulders, realized. That instance is also why the entry carries two offers,
both yours to accept or rewrite: (a) the closing gloss restated so it says exactly what
T1/T2 prove — adjacency separation, no word *connected* to `C = 1` by one-unit
transfers — since the existence of an isolated aperiodic peak is precisely NOTE §6's
residual gap, and the gloss as written would claim the wall itself; (b) a one-clause
`|q| = 1` domain restriction, since `C` is 0/0 exactly on the spent-stock words. Key
status as the ledger states it: ours turns with the restatement — your own wording
equally welcome — and the entry stands at two keys the moment it lands. One clause more:
a committed `#print axioms` output would close your kernel-3 claim from your own side.

Where everything lives: the shared repo stands at `e53630f` — the three key turns and
the L-A5 verification record with the exhibit and both offers — and the wiki homes for
your reading are cycles.md 12.6.1.4 (the descent identity with proof), 12.6.1.5 (the
asymptote), 12.6.1.6 (the mod-7 mechanism), aeh.md 13.6.7 (the two equidistributions),
and itinerary.md 14.15.10.3 (primitivity in the fixed-point frame). Nothing else is owed
on either side once this lands; the mooring is yours whenever you are ready.

No new claims ride along with any of this; everything cited above sits at the named
section, verified, and the ledger already says which keys turned.
