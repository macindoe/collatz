# Brief: round-9 co-edit preparation (L-A6 key + offers, L-A7 verification + re-source offer, L-A5 minor) — for a delegated session

**Context required before starting (in order):** `README.md` (binding stopping rules), `AGENTS.md`, `HANDOFF.md` item 1 (through the round-9 merged state, main `716b94a`), `briefs/merle-la6-check-findings.md` (entire — the L-A6 record, esp. the key recommendation and offers), `briefs/merle-la7-mu-check-findings.md` (entire — the μ adjudication §2, sensitivity §3(c), recommendation §4), `briefs/merle-la5-closure-findings.md` (the two flat observations), `briefs/merle-la5-coedit-findings.md` (the established co-edit style: verification record + offers inside the entry, acceptance his call, honest key-status lines).

## Task

Prepare, **LOCAL-ONLY, push gated on the author's explicit go-ahead from the main session**, one commit to the shared repo's `LEDGER.md` (nothing else) carrying our side of round 9. Model everything on the L-A5 co-edit (`e53630f`): verification records dated and pinned to our public artifacts, offers *inside* the entry per the established style, acceptance always his call, key-status lines that say exactly what is true and no more.

Content, from the findings records (do not re-derive; do not soften; do not inflate):

1. **L-A6 — our key TURNS, scoped.** Append a Macindoe verification record (dated 2026-07-25, artifact `experiments/merle_la6_check.py` + output, pin the wiki commit; 424 recorded checks / 816,871 census words, 0 failures): census exact-confirmed in his domain; **completed to all `S` at `n ≤ 14` our side — 23 words, the qualitative claim unconditional** (stated as a strengthening, with the one-line ghost + product-identity argument); budgets/tail replicated digit-exact (necklace λ 1.1175/2.6447, tranches at 4dp) as model computations; **the ghost identity proved exact** — state the lemma in full (`3·B(W) + q = 2^{σ₀}·B(shift W)`, `B(W)` always odd, hence `v_2(3x+1) = σ_0` forced at every step; formal hit IS a real cycle, a theorem); the calibration reading left at his own assessed grade, explicitly. Key-status line: **two keys** — census + realizability exact, budgets/tails as replicated model computations, the reading at assessed grade both sides. Offers (a)–(d) from the findings' key recommendation, inside the entry: (a) domain clauses (the `S ≤ 9` census window, `n ≥ 2` start, the near-tuned tail band — with our all-`S` completion offered as the closure), (b) the exact ghost lemma replacing "300/300 random words" as the mechanism sentence, (c) the residual's `n ≥ 61` cut pinned, (d) minor: "known" before "in existence", `λ_prim` units (1.0502/2.4282 → P(0) 0.350/0.088), the semiconvergent wording.

2. **L-A7 — verification record + re-source offer; key conditional.** Append a Macindoe verification record (dated 2026-07-25, artifact `experiments/merle_la7_check.py` + output, pin the wiki commit): algebra and numerics replicated digit-exact (`C₀ = −5.774` at `n = 2`, slack `14.483`, all seven tail cuts, headline cut `N = 600` exact). **The μ-source re-check he requested, stated flat with full citations from the findings §2:** `5.125` is Salikhov 2007's measure of `ln 3` (Dokl. Akad. Nauk 417 (2007) 753–755; Engl. Doklady Math. 76, 955–957; superseded for `ln 3` by Wu–Wang 2014, `5.1163051`, J. Number Theory 142, 264–273) — no measure of that strength is published for `log₂3`; the citable effective ingredient is **Rhin 1987** (Prop. p. 160, Progress in Math. 71: `|u₀ + u₁ln2 + u₂ln3| > H^{−13.3}`; Simons–de Weger 2005, Acta Arith. 117, Lemma 12, the printed Collatz-side precedent), under which **the same construction closes with headline "tail `< 5.2·10⁻⁴` beyond `n ≈ 2233`"** — the instrument intact, the ruler re-sourced; "effectively finite at every scale" true under every sourced row (Gouillon 2006 floor as the guaranteed fallback, crossing `~7.4·10⁸`). Offer, inside the entry (acceptance his call): the re-sourced ingredient line + restated bound (`R(n) ≤ −c_gen·n + 13.3·log₂K₀ + C₀`, `C₀ ≈ 2.06 + 3` repair bits) + restated headline (`n ≈ 2233`; exact computation below, `n ≤ 2000` already on both sides) + the `≈ 550` crossing sentence re-derived or dropped (our readings: 372 per-scale / 440 cumulative) + the two further ingredients named (the for-all-`n` margin inequality, verified `n ≤ 2000` min 2.84, elementary-but-unproved; the south floor `ε'_n` for the both-shore step, our slack 70.1 at `n = 3`). Key-status line, honestly, mirroring the L-A5 pattern: our key turns on the replication and on the construction **with the re-sourced ingredient** — **two keys upon his acceptance of a re-sourcing** (his own wording equally welcome); DRAFT with this stated until then. Keep his honest-scope paragraph untouched (the findings adjudge it correct).

3. **L-A5/L-A4 minor, one or two lines each, offers only:** (i) the wall rider's elided qualifier ("no isolated *primitive* `C = 1` peak…" — the trivial square is a literal positive-shore isolated `C = 1` word); (ii) date-stamp our now-satisfied "DRAFT until then" conditional; (iii) optionally, one line on the ContentDescent block recording our clean-room statement-match (`experiments/merle_contentdescent_check.py`, 4,541 exact checks; `G_k` = the 12.6.1.4 cofactor, verified recursive = closed = q-cofactor).

## Rules

- Work in your OWN worktree of the wiki repo. Branch **`merle-round9-coedit`** from main `716b94a` or a descendant; state the base SHA in the findings.
- Clone the shared repo fresh into the scratchpad; verify HEAD is `81431c7` **and record it** — if it has moved, STOP the shared-repo work, record the new state, and hand back (do not prepare on a moved HEAD). Prepare the commit on a local branch of the scratchpad clone over `81431c7`; **NO pushes anywhere, under any circumstances** — export `git format-patch` of the prepared commit into `briefs/merle-round9-coedit-patches/` in the wiki repo, and record the prepared commit SHA + full diff in the findings.
- Wiki-side commits, per-item: the findings record `briefs/merle-round9-coedit-findings.md` (the prepared LEDGER block VERBATIM, the patch filename, judgment calls listed, explicit "shared repo NOT pushed, push gated on author go-ahead"), the patch file(s), and a HANDOFF item 1 update (co-edit prepared local-only; owed: author push go-ahead; the reply draft is a parallel session — do not claim it).
- Every number and citation copied from the findings files exactly; anything you cannot find verbatim in a findings file is a STOP-and-hand-back, not a judgment call. Register: flat, calibrated; his prose never rewritten outside the offered lines; offers are offers.
- Do NOT merge to main; no reply paragraphs; no wiki-content edits beyond the listed files; stop after the HANDOFF update.
