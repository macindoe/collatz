# Brief: round-8 co-edit — key turns, margin asymptotes, primitivity, wiki homes (for a delegated session)

**Context required before starting (in order):** `README.md` (binding stopping rules — compliance note below), `AGENTS.md` (verification protocol), `HANDOFF.md` item 1 (through the round-8 verified state), `briefs/merle-round8-check-findings.md` **entire** (the evidence base: the L-A4 clean-room derivation, the verbatim shared-repo texts, the three flags), `experiments/merle_round8_check.py` and its committed output, `cycles.md` 12.6.1 entire, `aeh.md` 13.6 and 13.3.3 (scope paragraph), `itinerary.md` 14.15.10 (the dictionary this round's primitivity statement joins).

## Provenance and author authorization

Author go-ahead 2026-07-24 ("I trust your recommendations. Go ahead") for the recommended bundle: (1) exact asymptotic constants for the capacity–demand margin; (2) the "aperiodic" → "primitive" precision; (3) a wiki-side home for the descent identity; (4) the mod-7 explanatory remark; (5) the two-equidistributions calibration note; plus the queued key turns (our key on L-A4, on the L3 correction, and on L-A3 addition (B)'s quantification). **Authorization does NOT extend to pushing the shared repo**: shared-repo edits are prepared local-only in this session; the push happens later, from the main session, on the author's explicit word after review. If a push ever seems needed here, stop and hand back.

Main-session scratchpad computation to be independently re-derived (targets, not evidence — your own code decides):

- General-family margin limit, closed form: `c_gen = β − β·log₂β + (β−1)·log₂(β−1)` with `β = log₂ 3`; numerically `≈ 0.07932`. (Equivalent form: `β·(1 − H(1/β))`, `H` = binary entropy.)
- Stratum limit, variational: `c_strat = β − max_{α∈(0,1)} [H(α) + ((β−1+α)/2)·H(2α/(β−1+α))]`; numerically `≈ 0.26679` at `α* ≈ 0.37473`.
- Exact counts converge from above: margin/n = 0.2730/0.0854 (n = 1280, matching Merle's grid), 0.2669/0.0794 at n = 163,840.

**Stopping-rule compliance:** every item is either (i) recording/quantifying an existing unification note (the margin asymptote extends cycles.md 12.6.1.3's "logarithmic capacity against linear demand" by naming its constants — it extends the exclusion by nothing and must say so), (ii) a vocabulary precision, (iii) a wiki home for a two-key ledger identity, or (iv) an explanation of a *closed* door (the mod-7 bias). Nothing here is a cycle search, a divisibility exclusion attempt, or an equidistribution proof effort; the cycles front stays PARKED and every touched section keeps its parked/calibration language intact. Register norm throughout: flat, calibrated; heuristics labeled; the `2^(−margin)` decay clause is heuristic wherever quoted.

## Queue — part A: verification code (first, everything downstream cites it)

1. **`experiments/margin_asymptote.py`** (fresh code; may import nothing from `merle_round8_check.py` or any Merle repo; exact integer combinatorics for every count; floats only for logs, with the float-only role stated in the header). Contents, canaries first:
   - Canaries (hand-computed, printed before sweeps): small-n profile counts against brute-force enumeration (general and odd-stratum, n ≤ 12 — enumerate compositions directly); the Vandermonde identity `Σ_p C(n−1,p−1)C(S−1,p−1) = C(K−2, n−1)` exactly at several (n, S); odd-composition count closed form vs a DP.
   - **Derivation, in comments or an accompanying proof note (part B records it):** the entropy bounds `2^{aH(b/a)}/(a+1) ≤ C(a,b) ≤ 2^{aH(b/a)}` (standard, cite them as such) give `(1/n)·log₂ C(K−2, n−1) → β·H(1/β)`, hence `margin/n → c_gen` with the closed form above — a genuine limit, elementary. For the stratum, the same bounds termwise plus the largest-term/number-of-terms sandwich on `Σ_p C(n−1,p−1)·C((S+p)/2−1,p−1)` give `margin/n → c_strat` as the variational expression. Derive it yourself; if your derivation disagrees with the stated targets, the disagreement is a finding — record it, do not force the target.
   - Computation: exact-count margin/n on Merle's grid (`n = 10..1280` doublings; must reproduce the round-8 findings digit-exact: 0.2730/0.0854 at n = 1280) extended to at least n = 163,840 via lgamma/log-sum-exp (state the precision argument for the log-sum-exp — exact counts where feasible as cross-check at n ≤ 2560); the closed-form `c_gen` evaluated; `c_strat` located by ternary search with a bracketing check (evaluate g at α* ± ε, confirm interior maximum, record α*); convergence table margin/n vs the constants showing monotone approach from above; positivity of both constants (print with enough digits that positivity is unambiguous; state that positivity of the *limit* plus monotone-from-above convergence of the exact counts is what "positive and growing linearly" cashes out to).
   - **Mod-7 mechanism check** (feeds the cycles.md remark; verify before recording): for `p ∈ {5, 7, 11, 13, 31, 127}`, tabulate `ord_p(2)`, `ord_p(3)`, and the value-set size of `2^s − 1 mod p` over `s` odd vs `s` unrestricted; then the induced distribution of single terms `3^M·2^S·(2^s − 1) mod p` at small depth. The remark's claim to verify: `ord_7(2) = 3` collapses the `2^s − 1` factor to a 3-value orbit (a 2-value orbit on odd `s` if that is what the table shows — record what it shows), while primes with large `ord_p(2)` have no such collapse; the recorded mechanism goes exactly as far as the table supports, no further. This explains the closed door only; it proves nothing about the open wall and the remark must say so.
   - Commit script + full output (one commit).

## Queue — part B: wiki-side records (our repo; per-item commits)

2. **cycles.md — Remark 12.6.1.4 (the descent identity; L-A4's our-side home).** Model: the way 12.6.1.3 houses L-A3. Content: the multiplicative identity `R_0(B^k) = R_0(B)·(q_P/q_B)` with `q_P/q_B = G_k = Σ_{c<k} 3^{(k−1−c)n_B} 2^{cK_B}` (the geometric factor of `x^k − y^k`); proof by the two bookkeeping identities exactly as written in `briefs/merle-round8-check-findings.md` item 3(a) (re-derive into wiki prose, don't paste); scope: every profile with entries ≥ 1, untuned, both signs of `q`; corollaries, flat: (i) the biconditional `q_P | R_0(P) ⟺ q_B | R_0(B)` (the shared L-A4, one key his + our verification 12,888/12,888, ledger status stated honestly as of this writing); (ii) L-A2's gcd law re-derived one level up; (iii) **primitivity**: any cycle whose profile is a proper power `B^k` descends to a cycle on `B`, so a minimal — in particular any new — cycle has a **primitive** profile (correct vocabulary for a finite word; his letter's "genuinely aperiodic" is this statement, and the remark says so without correcting him in prose). Calibration sentence per 12.6.1.3's model: the front stays parked; this closes a structured refuge, it opens nothing.
3. **cycles.md — extend 12.6.1.3 with the asymptote** (a short dated addendum paragraph inside 12.6.1.3, or a 12.6.1.5 if the flow reads better — your call, state it in findings). Content: the margin definition (`margin = K − log₂ #profiles`, both families named precisely), the two limits with their closed/variational forms and numeric values, convergence from above with the table's endpoints cited, attribution flat (quantification seeded Merle-side REQ-MATH-014 at n ~ 10³; asymptote closed our side this round), and the calibration sentence: extends the exclusion by nothing; the decay reading remains the no-conspiracy heuristic; nothing about `q | R_0` moves.
4. **cycles.md — the mod-7 remark** (placement near the 12.6.1.1 `gcd = 7` note or adjacent to the L3-related record; your call). Content per the part-A table only: why 7 is solitary (small `ord_7(2)` collapses the term orbit; the isolated bias is structural, not a lever — his letter's own conclusion, now with the mechanism named), why the listed large-order primes show no collapse. Explicit sentence: this explains a closed door; the open wall (generic equidistribution) is untouched and stays parked.
5. **aeh.md — the two-equidistributions calibration note** (short remark in §13 near 13.6, plus a one-line pointer from the cycles.md asymptote text). Content: AEH's genericity form (13.6) is an orbit-statistics statement; the Merle-side target (`R_0 mod q` along the generic profile family, LEDGER/NOTE naming 2026-07-24) is a family statement about cycle closure residues; distinct objects, neither implies the other as stated, both honestly on the ×2×3 gap; conflation in future prose is the failure mode this note exists to prevent. 13.3.3's scope discipline applies unchanged.
6. **itinerary.md — one dictionary line in 14.15.10** (only if it fits the table's existing grammar; if it would need surgery, record that and skip): new cycles ⇔ primitive profile words (pointer to 12.6.1.4(iii)).

## Queue — part C: shared-repo co-edits (prepared LOCAL-ONLY; the push is not yours)

7. Clone `github.com/macindoe/one-obstruction-three-faces` into the scratchpad; verify HEAD is still `b8842bb` (if it has moved, stop part C, record the new HEAD and its diff in the findings, and leave part C unprepared — the main session re-adjudicates). Prepare commits on a local branch `coedit-round8` (author = the repo's configured user, i.e. the author's identity, matching `430c00c`/`d2407b9` practice), one commit per ledger entry, `LEDGER.md` only (NOTE.md stays his; anything NOTE-shaped goes in the reply later):
   - **L-A4** — turn our key: append our verification block (clean-room derivation independent, the identity form `R_0(B^k) = R_0(B)·(q_P/q_B)`, 12,888/12,888 exact over the three grids, canaries incl. trivial-cycle inheritance; artifact = our repo's `experiments/merle_round8_check.py` at its main-merge commit — cite the hash after part B's merge is known… it is not; cite the branch commit `d9ef06b` plus "wiki main" and flag for the main session to pin the final hash at review). Status line **DRAFT → two keys**. Two offers *inside the entry, marked as offers for his acceptance* (per the L-A2/L-A3 co-edit style): (a) scope widening — the identity holds untuned, both signs, entries ≥ 1 ("in the tuned regime" can drop or stay as the application's scope, his call); (b) vocabulary — "genuinely aperiodic" → "primitive (not a proper power)", the finite-word-correct term, mirroring the "positive odd integer" precision.
   - **L3** — turn our key on the correction: one appended line-block — correction verified our side (no-Hasse-gap principle confirmed as stated; `v_7(q) = 3 > v_7(R_r) = 1` and `gcd(q, R_r) = 7` at all 7 rotations, independent exact code; recorded two-key data confirmed byte-untouched by diff; distance profile recomputed digit-exact), correction accepted into the two-key record.
   - **L-A3** — on addition (B): append the operational definition (from REQ-MATH-014, resolving the values-without-definition flag; note the vocabulary alignment with cycles.md 12.6.1.3 in one clause), our digit-exact replication, and the asymptote upgrade: the two limit constants with their forms and the convergence-from-above sentence (attribution flat, as in item 3); the `2^(−margin)` clause explicitly labeled heuristic (his artifact's own label, kept). Status of (B): quantification **two keys** once his acceptance of the asymptote lands — write the status honestly as "our key turned on the n ~ 10³ replication and the asymptote offered" pending his word.
   - Export the prepared commits as `git format-patch` files committed into **our** repo branch under `briefs/merle-round8-coedit-patches/` (so the prepared texts survive any scratchpad loss), alongside the exact final LEDGER.md text of each edited entry quoted in the findings file.

## Queue — part D: record

8. `briefs/merle-round8-coedit-findings.md` — what was produced and why, per item; the asymptote derivation written out (wiki-grade, it backs item 3); the mod-7 table and exactly what it supports; the shared-repo prepared-commit hashes (local) and patch filenames; flags and judgment calls (12.6.1.3-addendum vs 12.6.1.5, dictionary-line fit, anything skipped); explicit line: **shared repo NOT pushed; push gated on the author's word from the main session.**
9. `HANDOFF.md` item 1 update — pending block only: co-edit round produced and merged pending review; shared-repo key-turn commits prepared local-only (patches in-repo), awaiting the author's push go-ahead; then the reply draft (separate round; sending stays with the author).

## Rules

- Branch **`merle-round8-coedit`** from your worktree HEAD (verify it is `eb30427` or a descendant — state the base in the findings; if the worktree was cut behind main, rebase or recreate from the right base and say so). Per-item commits as itemized. Do NOT merge — the main session reviews (re-runs part A, checks every derivation and every prepared patch) and merges.
- **No pushes anywhere.** Our repo: commits on the branch only. Shared repo: local commits + format-patch export only. Any push impulse = stop and hand back.
- Wiki edits follow the wiki's own register: no excitement, calibration sentences kept, parked stays parked, section numbering and cross-link style matched to the page.
- Discrepancies with the stated targets (constants, derivations, mechanism table) are findings, recorded not forced.
- No reply paragraphs; no NOTE.md edits; no scope expansion; stop after part D.
