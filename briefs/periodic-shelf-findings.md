# Findings: periodic-shelf (off-brief observations)

Branch `periodic-shelf`, 2026-07-17, per `briefs/periodic-shelf-brief.md`. The two corrections this session produced are **on-brief** and live in the section itself, not here: `14.15.7.5`'s H2 smallness hypothesis is unconditional at every period (Lemma `14.15.9.1`(2), `2|a| < Q_1/4`), and the spectrum-coincidence invariant is the signature `(q, g_P, C, ε)` rather than the probe's triple (Theorem `14.15.9.11`(3), with the witness inside the mandated grid). One genuinely off-brief observation arose; per the stop line it is logged here and stopped.

**Observation (per-letter windows: the class halves already extend; only the door law rotates).** In proving the whole-period class theorem (`14.15.9.5`), both halves individually extend to period-cutting depths with no new machinery: the forward follower class equals the `2`-adic class of `y^*` at **every** prefix length `L` — the contraction induction along rotations never uses `p | L` — and the backward admissibility class at **every** depth `ℓ` equals `{qz ≡ a (mod 3^{M_ℓ})}`, because the depth-`ℓ` deepest-first prefix map carries the rotated fixed point `y^*_{(p−ℓ) mod p}` to `y^*` (so its offset is `≡ y^*` at the `3`-adic place, by the same one-liner as the whole-period case). So the *class* characterization appears window-general; what rotates mid-window is only the deepest-door analysis — the deepest door of a depth-`ℓ` chain sits near the rotated fixed point `y^*_{(p−ℓ) mod p}`, so a per-letter mod-3 door law would run through the rotation's own numerator `a_{(p−ℓ) mod p}` and residue `ε_{(p−ℓ) mod p}` (Lemma `14.15.9.2`) rather than through `a` and `ε`. This suggests the per-letter window height laws are within reach of the existing apparatus plus the rotation lemma's `p`-tuple of numerators. Not pursued — the brief's stop line explicitly forbids per-letter/period-cutting windows — and no claim beyond this paragraph is made; the scope note in `14.15.9`(e) records the gap as open.
