# Brief: the tear afterlife — the ladder composed with the step law (ladder.md 15.6) — for a delegated session

**Context required before starting (in order):** `README.md` (strategy and **binding stopping rules**), `AGENTS.md` (house norms: nothing is labeled proved without independently written verification code; no change logs in tracked files; every fact lives in exactly one page; content and structure in separate commits), `HANDOFF.md` (register norm; the delegation pattern; the PowerShell quirk), `ladder.md` in full (15.1.1 is the law this brief composes; 15.4 names the composition as the unexamined item; 15.5 is the valuation-level reading), `spine.md` §9.8 (the reduced map `F`, states `(ω, d)`, the numerator `A(ω, d) = 3^d ω − 1 = 2^s e`, the exit `e`, the next state through `e + 1 = 2^m 3^a Ω`), `stage3.md` 11.8.6.3 (entry depth and target shift), `stage4.md` 11.8.7.1–11.8.7.3 (the anchor `M(ω)`, the digit cost `σ = s + m₊`) and 11.8.7.7 (the digit budget), `reverse.md` 14.10 (the dual ladder — its `d ≥ 2` branch is the mirror tear), 14.14.1 (the global edge parameterization; 14.14.1.1 is the cost identity the tear-line paragraph uses), `bridge.md` 16.2 (the core-extraction deficit), `anchors.md` 17.3 (the pointer list this brief adds one line to), `symbols.md` (registry; check every symbol you use against it), `index.md` (resolver granularity), and `briefs/rubin-3adic-baker-brief.md` with its findings file (the form of a brief, a findings file and a per-item commit stream on this repository).

## Provenance

ladder.md 15.4 records, as the one open connection not yet pursued, whether the ladder law (15.1.1) and the step law compose into anything. The author's questions of 2026-09-09 — *what happens to the two states after a tear?* and *do the tear-lines fray?* — were pre-checked in the main session with throwaway code (deliberately not filed; you write the script of record from scratch). The pre-check's claims, every one of which is a **claim to be re-derived and re-verified here, not a fact**:

1. **The torn state.** At a spike of height `s ≥ 3` at `(ω, d)` with exit `e`, the next reduced state of the deeper neighbour is `F(ω, d+1) = (3·2^(s−2)·e + 1, 1)`: entry depth `1`, no 3-gain, and the torn core is `≡ 1 (mod 2^(s−2))`. (This is 15.1.1's kick `e' = 3·2^(s−1)e + 1` carried one step further: `e' + 1 = 2·(3·2^(s−2)e + 1)`, the bracket odd and `≡ 1 (mod 3)`.) The case `s = 2` is the boundary: state it separately (the bracket `3e + 1` is even there, so the entry depth is `≥ 2`).
2. **The opening run.** The torn state's reduced word opens with exactly `⌊(s−1)/2⌋` letters `(s, m₊) = (1, 1)` and never more; the run ends at a depth-1 core `≡ 5 (mod 8)` or `≡ 3 (mod 4)`. Mechanism to find and prove: a core `≡ 1 (mod 2^j)` at depth `1` descends by one `(1,1)` letter to a core `≡ 1 (mod 2^(j−2))`.
3. **The torn core's anchor.** `v₂(M(Ω')) = s − 4` for `s ≥ 5`, so the column of `Ω'` coincides with the column of `1` at every depth `d'` with `v₂(d') < s − 4` (the columns agree wherever the anchor digits agree; state the exact depth range and prove it from the anchor's definition).
4. **The mirror tear.** reverse.md 14.10's `d ≥ 2` branch lands at `(4·3^(d−1)·ω − 1, 1)` with core `≡ −1 (mod 3^(d−1))` — the 3-adic twin of item 1, already proved at 14.10.1; what is new is only the reading (torn core near the mirror fixed point) — record it as a remark with a pointer, restate nothing.
5. **Two metrics, one calibration.** The torn core is *2-adically near the fixed point* `1` (item 1) and *archimedean-ly far* from the sister exit (`Ω' ≈ 3·2^(s−2) e`); the mirror tear is 3-adically near `−1`. This is the `+1`-side twin of the rising run: `x ≡ −1 (mod 2^m)` climbs `m` odd steps (the entry depth; locate the record's statement of the rising run and cite it — TOUR.md's dictionary row for the reduced map names it), while `x ≡ +1 (mod 2^k)` descends `⌊k/2⌋` letters. Grade at delegation: **a unification note placed at the tear; no front moves; the stopping rules are untouched.**
6. **Measured, not proved — carry it as such.** The upper sister `e' = 3·2^(s−1)e + 1` was never found downstream of `e` in the orbit of `e` (3,247 generic spikes and every small case; a sign argument proves it for one and two steps only). The reverse — `e` downstream of `e'` — happens only in the drainage basin (`e ∈ {1, 5, 13, 23}`), never generically. The two orbits merge strictly below both sisters in 3,167 of 3,247 cases. Reproduce the measurement with your own bounds and record it as a measurement. If the one-and-two-step sign argument holds, write it as a lemma at that scope and no further; do not extend it by search.
7. **The tear-lines as level sets (the "fraying" question).** `s(ω, d) ≥ k` iff `ω ≡ 3^(−d) (mod 2^k)`: one residue class per row, nested, halving in density with `k`. A height-`k` line fixes `k + 1` digits of `3^d ω`, and the step at any of its cells spends `σ = s + m₊ ≥ k + 1` (14.14.1.1), so **every tear-line is torn completely in one step**: measured in the pre-check as total variation from uniform over the `2^k` classes after one step of `F`, at the noise floor for `k = 8, 12, 16` (40,000 starts each). There is no reachability structure between tear-lines; the fraying is the digit budget (11.8.7.7) and the core-extraction deficit (16.2) read as field geometry. This is **one calibration paragraph for 15.6, not a lever** — say so flatly.
8. **Off-spike.** At `s = 1` the two neighbours share one orbit (15.1.1) and their next reduced states coincide or sit one step apart. The exact split by entry depth and its role in the family graph are the subject of a parallel delegation (`briefs/ladder-family-graph-brief.md`, branch `ladder-family-graph`, which owns ladder.md **15.7**). **Do not state or prove the off-spike split here**; one clause in 15.6 saying the off-spike case is recorded with the neighbour laws is enough — the main session sets the pointer at merge.

Pre-check counts, for calibration of your own ranges: 6,768 spikes, 26,245 mirror edges, 121 anchor cases, 0 failures throughout.

**Stopping-rule compliance:** a local composition of two proved local laws, placed on the ladder page. No cycle search of any kind; no proof effort on equidistribution; no front reopened; the Bridge unchanged. If any item above turns out to be a lever, stop and record it as a candidate — do not pursue it.

## Queue

1. **Mathematics first, on paper in the findings.** For each of items 1–3 and 5, write the statement and its proof from the numerator identity `A(ω, d+1) = 3A(ω, d) + 2` and the definitions in spine.md §9.8 and stage4.md 11.8.7.2 — before running anything. Where the pre-check's claim is wrong or needs a boundary case, record the corrected statement and what was wrong; **a claim is not dissolved by naming its type** (a class label is not a cause) — every correction carries a re-derivation.
2. **Fresh verification code — `experiments/ladder_afterlife.py`, committed with its output.** Imports nothing from any existing script (not `ladder.py`, not `mirror_baker_cap.py`; the main session's scratchpad code does not exist for you); exact integer arithmetic at every pass/fail decision; canaries first (the three known cycles' states, the `p = 7` staircase's first state if convenient, `(1, 1)`, and 15.1.1 itself on a few hundred states). Contents: (a) item 1 on every spike in a stated box plus random states to a stated bit size; (b) item 2, the opening run's exact length and the terminal residue; (c) item 3, the anchor valuation and the column coincidence range, checked against the anchor computed from its definition; (d) item 4 against 14.10.1 on random `(y, s)`; (e) item 6 as a bounded measurement with the bound printed; (f) item 7, the level-set identity on a grid and the one-step total-variation measurement at `k = 8, 12, 16`, with the noise floor stated (the TV of a uniform sample of the same size against uniform, so the reader sees both numbers). **One documented command reproduces the committed output in full** (`python experiments/ladder_afterlife.py`, no phases, no flags needed for the record); if a phase is slow, keep it in the single run and print timing. Record counts, ranges, seed, date.
3. **Wiki edits — conservative; content and structure in separate commits.**
   - `ladder.md`: a new section **15.6. The tear afterlife** after 15.5: the lemma (item 1 with the `s = 2` boundary), the opening-run proposition (item 2), the anchor-coincidence proposition (item 3), the mirror remark (item 4, pointer to 14.10.1), the two-metric calibration remark (item 5, with the rising-run citation), the measured line (item 6, labeled measured), and the tear-line calibration paragraph (item 7). Verification inline: what was checked, the range, the date — one line per statement, per AGENTS.md. One clause in 15.4 replacing "whether the two exact laws compose into anything is unexamined" by a pointer to 15.6 (keep the rest of 15.4). Front matter `status` and `updated`; the Current-state paragraph gains at most one sentence naming 15.6.
   - `anchors.md` 17.3: one bullet after the ladder-law bullet, a pointer to 15.6 — pointer only.
   - `symbols.md`: only if you introduce a symbol; prefer not to. `Ω'` for the torn core is acceptable as local notation inside 15.6 without a registry row; say "local notation" once.
   - `index.md`: add 15.6 to the resolver only if it lists at the granularity of 15.5; otherwise leave it.
   - `briefs/ladder-afterlife-findings.md`: the derivations, what the pre-check got wrong (if anything), the verification record, the measurement table for item 6, the TV table for item 7, obstructions, and a *For the main session at merge* section listing the exact pointer sentences the parallel branch will need (the off-spike clause of item 8; the 15.7 cross-reference).
   - **Do not edit** `HANDOFF.md`, `reverse.md`, `README.md`, `TOUR.md`, `publication.md`, `paper/`, or anything under `sources/`. Do not create 15.7.
4. **Compliance.** Run `experiments/encoding_scan.py` before the final commit and record `RESULT: CLEAN` in the findings.

## Record

- `experiments/ladder_afterlife.py` + committed output (canaries first; one reproducing command).
- `ladder.md` 15.6, the 15.4 clause, the front matter and Current-state sentence; the `anchors.md` 17.3 bullet; the resolver line if applicable.
- `briefs/ladder-afterlife-findings.md` as specified.

## Rules

- Branch **`ladder-afterlife`** from your worktree HEAD. FIRST verify the worktree contains this brief (`briefs/ladder-afterlife-brief.md`) and that `git log --oneline -1` shows a commit dated 2026-09-13 or later; worktrees are sometimes cut from a stale HEAD — if either check fails, rebase onto local `main` before starting, and state your base SHA in the findings.
- No web access needed; no contact with anyone; no pushes; no edits outside this repository.
- File edits via the Edit/Write tools only — never PowerShell `Get-Content`/`Set-Content` (see HANDOFF quirks; the repo's `—`/`≤`/`₂` are destroyed silently).
- Register: flat, calibrated prose. Heuristics labeled heuristics; measurements labeled measured; nothing labeled proved without the fresh code of Queue 2 **and** a written proof.
- Record obstructions; do not force analogies (the mirror remark in particular: if the 3-adic reading does not match item 1's shape exactly, say where it differs and stop).
- Run the script in the foreground and wait for it; do not stop until every commit exists on the branch.
- Do NOT merge — the main session reviews (re-runs the script, re-derives one proof, checks the 15.7 seam) and merges. Stop after Record.
