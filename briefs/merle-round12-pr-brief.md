# Brief: the round-12 response as the first PR — co-edit + letter + protocol, prepared local-only — for a delegated session

**Context required before starting (in order):** `README.md` (binding stopping rules), `AGENTS.md`, `HANDOFF.md` item 1 — the round-12 paragraphs from "ROUND 12 RECEIVED" through "v3 PUBLISHED" (the author's five decisions, the five window verdicts, and the amended owed-next are all there), `briefs/merle-round12-letter.md` (his letter, both parts), then the six findings files this window assembles from: `briefs/merle-la9-check-findings.md`, `briefs/merle-r12-drift-check-findings.md`, `briefs/merle-la7-rhin-check-findings.md`, `briefs/junction-followup-recon-findings.md`, `briefs/erratum-v3-prep-findings.md` (§6, the canonical-citation line), `paper/collatz-reduced-version-history.md` (the v3 story and the frozen-clause defect note).

## Provenance and the new mechanics

Merle's §13 proposal is **accepted by the author**: the technical half of the correspondence moves into the shared repository, **one round per pull request, the second key = the approving review**; mail keeps everything that is not a claim. This window prepares **the first PR** — it enacts the acceptance rather than announcing it. The deliverable is a branch of the shared repo (`github.com/macindoe/one-obstruction-three-faces`), prepared **local-only in a scratchpad clone, never pushed**, carrying:

1. `rounds/R12-macindoe.md` — the round's letter as a file (business paragraphs only; this creates the `rounds/` convention in the shared repo, mirroring his `rounds/R11-merle.md` in his Lean repo);
2. `LEDGER.md` — the round-12 co-edit;
3. `PROTOCOL.md` — the regime column, added as this PR's protocol change (his §4(d) proposal, accepted by the author; his approving review is the second key on it).

Plus one wiki-side item in OUR repo (item 5 below), on your branch, separate from the shared-repo work.

Numbering: his letter is R11-merle in his numbering (his reply to our round-11 letter); this response opens round 12, so the file is `R12-macindoe.md`.

**Stopping-rule compliance:** assembly and record-keeping of verified material; nothing reopens a front; the cycle front stays PARKED and the letter says so where relevant.

## The verification standard (non-negotiable)

Every number, SHA, section reference and quotation in everything you write must be **verified at its named place** in the findings files or the live repos — nothing from memory, nothing from this brief alone (the brief is a map, not a source). Load-bearing arithmetic (the L-A9 exponent conversions, the D(x)/δ series coefficients, the §9 constants) is recomputed in-session at controlled precision before it is transcribed. Where a findings file and this brief disagree, **the findings file wins** — record the divergence in your findings (the round-10 precedent: where a brief contradicts a findings file, the findings file is the authority).

## Queue

1. **State check first.** `ls-remote` + fresh scratchpad clone of the shared repo: expected HEAD `7c05458` (his L-A9 hash fix). If moved, record what landed, read it, and adapt — anything of his that touches L-A9/the offers changes the co-edit text and the letter, and if it is a new letter, STOP and hand back. Also verify our public wiki `main` (`ls-remote github.com/macindoe/collatz`): every wiki pin you cite must resolve there (`6ffe463` or later expected; all six findings files and the version history are public at that commit).

2. **`LEDGER.md` co-edit** (his prose untouched everywhere; offers inside entries per the established co-edit style; every block you add is assembled from the findings files' own drafted text where one exists, adjusted only for ledger context):
   - **L-A9** — the Macindoe verification record (50 checks, 0 failures; the entry's arithmetic replicates to the digit; the clean-room `c*` derivation; the scissors replication including the `2^2000` extension) and the **split-grade key status** exactly per `merle-la9-check-findings.md` §6.3: the Macindoe key turns on the **Dirichlet half** (claim, chain reduction, floor argument) **conditional on his acceptance of offer h1** — the single-convention restatement — DRAFT until then (the round-9 L-A5 conditional pattern); the **scissors half stays at measured grade permanently**, with the reason stated (proving `α > 1/3` is a `μ(log₂3) ≤ 3`-class statement). Offers **h1–h5** verbatim-adjusted from §6.4 (h1 convention repair; h2 dream-deficit relabelling; h3 Barina label; h4 α scope with the straddle-at-1/2 fact; h5 the seed-free census line). The grade line he asked us to attack is answered inside the entry: what failed, what survived, stated flat.
   - **L-A8** — the corrections to **our own** round-11 hand-back block, accepted as ours with mechanisms named, from `merle-r12-drift-check-findings.md` §1.4 and §2.4 (the `D(x_min)` display is a truncation — the exact chain `D > δ(1+1/(27x²)) > δ` replaces the equality; the 44-decimals diagnosis refuted by scale-invariance, ratio `0.4755266…` a pure shape function). His §4(e)/(f) credit fixes recorded as he set them (factor 2 jointly arrived at; corollary his, proof ours).
   - **L-A7** — one short addition: the round-12 re-source warning adjudicated — 13.3 CONFIRMED for exactly what the chain consumes (the four printed carriers cited from `merle-la7-rhin-check-findings.md` §2; the BILAN's premise refuted, its trigger real — R200's transcription, not Rhin's attribution), no number in the entry moves; the small-`H` volume-confirm flag carried forward unchanged.
3. **`PROTOCOL.md` — the regime column.** Draft the addition in his own §4(d) terms, short: beside the two keys, each claim records **the regime it was discharged in**, and any asserted equality records **a second regime chosen against the claim**. Note in the PR description that this enters by his own proposal, accepted, and that his approving review is its second key. Do not restructure anything else in the file.
4. **`rounds/R12-macindoe.md` — the letter.** Business paragraphs only; **no personal opening/closing** (under the accepted §13 split, the personal half travels by mail — the file needs no placeholders for it). Structure follows his R11 section order where it answers him directly. Must carry, each from its named source:
   - **§5 answered first (he asked to know quickly):** the binning convention he guessed **is exactly ours** — quote the operational definition from `experiments/merle_r11_hygiene_check.py` (`gk_stats`: classes `{1},…,{B−1},{≥B}`, `dof = B−1`, closed-form tail), with the honest caveat that our committed run tried 13 binnings, not all 38, and that his exhaustive max `0.56657 < 0.567` is **verified our side** (la9 findings §5.4). His REQ-067 defect verified in full: divergence at index 385 reproduced from fresh code, `0.00078` and all six table numbers to the digit.
   - **The L-A9 verdict** (la9 findings §§4, 6): the attack he asked for, delivered flat — arithmetic flawless, conclusion confirmed and strengthened, the grade line's margin does not survive (the convention mix, the one-power-of-`k` gap, the true-dream deficits), the Barina label, the α width-dependence and the straddle at 1/2; the split-grade key offer and h1–h5 pointed at in the ledger. Note the cross-check: the `+1` conversion is the same one the Rhin adjudication derived independently.
   - **The Rhin answer** (rhin findings §§2–3, 5): his warning adjudicated — the BILAN is the artifact to retire, with the mechanism (R200 transcribed Rhin's bound into the LMN `(log)²` shape; the auditor rejected the attribution where the defect was the transcription); the four carriers named; L-A7 unmoved; delivered kindly — he asked for this.
   - **The §4 acceptances as ours** (drift findings §§1–3): the truncation, the refuted shared diagnosis, the corollary credit as he fixed it; one sentence each, mechanism named, pointing at the ledger corrections.
   - **The §9 answer** (drift findings §4): all fourteen claims verified — the plateau `J = 13..25` and the fall at `J = 26` confirmed structurally, his "cost not failure" arithmetic exact — and the incorporation: the block enters `cycles.md` 12.8.6.1's marked section **under both names**, as he asked (item 5 below; cite the wiki commit once it exists — coordinate the pin with item 5's commit).
   - **The §10 answer — the silence, answered** (drift findings §5): the silence was real and total on two of three clauses, our own overstatement named; the recommendation delivered as the answer — the memory clause replicated on the corrected footing both sides; clustering/spectral recorded his-side unreplicated in the `+0.22/−0.29/−0.57` shape; **the stopping rules do NOT place replication out of reach** — what does is the unrecoverable detector spec, one clause each to supply if he wants the second key.
   - **The §§1–2 answer** (junction findings, the reply-material paragraph — use it nearly verbatim): all three round-11 not-founds closed at full weight, the push-log corroboration, our 18-against-18 count, the 7→15→18 convention note handed over as a fourth instance of his own finding.
   - **The two small corrections, ours**: the 3,175 = CRLF mechanism (answers "the one number of yours I could not reproduce"); the §10.2 fifteen-quotients correction accepted with the addendum applied.
   - **The erratum answer — the section that changed, write it against real v3** (version history + HANDOFF's "v3 PUBLISHED" paragraph): the sequence question is answered by the fact — **v3 is published, 2026-08-03, DOI 10.5281/zenodo.21730505** — and the delta from what we described is stated plainly, in his own §2 vocabulary: *what we described as a drafted one-sentence erratum shipped as a full revision after external review — the object shipped is a superset of the object described.* What that revision is (one paragraph: every definition/statement/scope word reconciled; the v2 note and gap sentence replaced by the Status paragraph; the narrative public at `paper/collatz-reduced-version-history.md`); the citation guidance (**v3** for the dichotomy phrases — verbatim-verified surviving — and for Theorem 4.6's obstruction sentence, still inside the theorem environment, the environment now carrying only proved content; **v2** for the contiguous `p ∈ {2,…,23}` evidence note, which v3 replaced); the canonical-citation line from `erratum-v3-prep-findings.md` §6.3 adapted to the published state, with the flat ccchallenge-is-v1 note; and **the frozen Version-note clause self-reported at full grade before he finds it** (the published PDF describes itself as "not yet published"; recorded in the version history; the Zenodo metadata note is the author's pending remedy).
   - **The §12 answer, business half only:** the author accepts the two-page-note shape (counting dichotomy + located obstruction, prerequisites deferred to a second version), and the erratum precondition he set is now met. **The pen sentence is the author's own — leave the marked placeholder `[THE AUTHOR'S: the pen]` and write nothing for it**, and likewise `[THE AUTHOR'S: the cost answer]` for his rhythm question (one available fact for the author's use, stated in your findings, not in the letter: we hit his exact failure mode once this round — a review agent cut off at the monthly limit, resumed cleanly).
   - **§13 answered by the object itself:** one short paragraph noting that this PR is the acceptance enacted — one round, one PR, the second key his approving review; the regime column inside it, per §4(d), likewise his to key.
   - **The where-everything-lives map**, updated: the six new findings files with check counts, the version history file, the v3 DOI, wiki `main` pin (`6ffe463` or later — CHECK AT SEND TIME marker if you cannot verify resolution).
5. **Wiki-side (our repo, your branch): the §9 block into `cycles.md` 12.8.6.1's marked section under both names**, from drift findings §4.2's proposed text, placed where that section says (between *What is consumed* and *Superseded formulation*), the plateau result tagged his. This is the one edit to `cycles.md`; nothing else on the page moves. The ledger/letter cite this block by the commit that carries it — your own branch commit, noted in the findings as the pin to re-check at merge (the main session re-pins if the merge SHA differs).
6. **Packaging.** Shared-repo work as one branch (`round-12` suggested) over the verified HEAD in the scratchpad clone, per-file commits (letter; ledger; protocol), portable patches archived at `briefs/merle-round12-pr-patches/`, each verified applying clean on a pristine clone of the base HEAD with the resulting tree hash recorded. **Draft the PR title + description** (short: what the round carries, where the letter is, what needs his key — the L-A9 conditional turn, the regime column, the L-A8 corrections) in your findings. **Nothing is pushed anywhere; no PR is opened** — the push and the PR are the author's send, gated on his go-ahead.

## Record

- `briefs/merle-round12-pr-findings.md` — every prepared file verbatim or by patch reference; the verification log (each number → its named source); the state-check results; the PR title/description; the pins and their CHECK-AT-SEND markers; anything you could not verify, with reasons.
- `briefs/merle-round12-pr-patches/` — the portable patches.
- The `cycles.md` commit (item 5) on your branch, separate from the briefs commits.
- Do NOT edit `HANDOFF.md`, `publication.md`, or any existing findings file.

## Rules

- Branch **`merle-round12-pr`** from your worktree HEAD. FIRST verify the worktree contains this brief; worktrees are sometimes cut from a stale HEAD — if missing, merge/rebase onto local `main` first; state your base SHA in the findings.
- Shared repo and his repos: read-only clones in the scratchpad; **no push, no PR, no interaction**. Any push impulse = stop and hand back.
- File edits via Edit/Write tools only — never PowerShell `Get-Content`/`Set-Content`. Run `experiments/encoding_scan.py` before your final commit and record RESULT: CLEAN (note: the scanner was just fixed to survive its own report on cp1252 consoles — if it prints ISSUES FOUND, the finding is real; do not paste literal mojibake into any file, describe it).
- Register: flat, calibrated; corrections of ours stated as ours with mechanisms; findings that run against him delivered kindly and factually — he asked for every one of them.
- His prose in the shared repo is untouched everywhere; offers only.
- The two placeholders (`[THE AUTHOR'S: the pen]`, `[THE AUTHOR'S: the cost answer]`) stay empty — no drafting, no glossing, per the standing exclusion.
- Do NOT merge — the main session reviews (re-verifies patches, trees, pins, and the load-bearing arithmetic) and merges; the push/PR is the author's. Stop after Record.
