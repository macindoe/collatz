# Schema and Maintenance Workflows

This repository follows the LLM-wiki pattern: three layers with different mutation rules.

## Layers

1. **`sources/`** — immutable. Historical drafts (v000–v078), PDFs, residue data. Read for provenance; never edit, never delete. New monolith drafts are no longer created — the wiki pages are the live document.
2. **Wiki pages** (`index.md`, `spine.md`, `program.md`, `stage1.md`–`stage3.md`, `open-problems.md`, `archive/`) — evergreen. Each carries front matter (`status`, `scope`, `updated`) and a "Current state" paragraph. Pages are rewritten in place to carry the current best answer; history is git's job, not the page's.
3. **`experiments/`** — verification and pilot code. Each script states which page/result it supports. Scripts are kept runnable; results (counts, ranges, dates) are quoted in the owning page.
4. **`README.md`** — the human-facing map, including the program's strategy and stopping rules. Those rules are binding on agents too: check them before opening new computational fronts (in particular: no per-period cycle searches unless they serve the uniform trim lemma).
5. **This file** — the schema. Update it when the structure or workflows change.

## Conventions

- Monolith section numbers (`11.8.6.3`, `9.8.3`, …) are the stable citation anchors. Do not renumber them. The resolver in `index.md` maps numbers to pages.
- Every fact lives in exactly one page. Other pages point to it; they do not restate it. (The staleness that motivated this wiki came from duplicated status claims.)
- Math statements are edited conservatively: never "improve" a proof while doing organizational work. Separate commits for content vs. structure.

## Workflows

### When a result's status changes (proved / refuted / dissolved)

1. Update the owning page: front matter `status` + `updated`, the "Current state" paragraph, and the statement itself.
2. Update the compact ledger (stage1.md 11.8.4.5).
3. Sweep `open-problems.md` for entries that posed the question as open; add a calibration note pointing to the closure.
4. If stage-level, update `program.md` and the status summary in `index.md`.
5. Refuted claims are not deleted: move the claim + refutation evidence to `archive/`, leave a pointer.

### When adding a new result

- Proved structural material about the formalism itself → `spine.md`.
- Stage results → the owning stage page.
- New open questions → `open-problems.md`, phrased so that closure is checkable.

### Before marking anything "proved"

Run an independent numerical check (not the one quoted in the text — a fresh implementation), and record what was checked, the range, and the date in the page. Precedent: the v077 laws were re-verified with independent code over 8,000 random states and orbit traces, zero discrepancies (2026-07-06).

### Periodic status pass

Occasionally diff every page's claims about *other* pages against those pages' own front matter. Any mismatch is a bug. This is the wiki's replacement for the monolith-era problem of §11.1–11.6 rotting while §11.8 moved on.

## Verification record

- 2026-07-12 — `viz/anchor_digit_visualizer.html` REVIEWED (follow-up, branch `anchor-digits`, commit `c67bcad`): added a live random-control twin (client-side `Math.random()`, regenerate button) beside each M(ω) plot, per the author's request. Reviewed the refactor: both recurrence and chaos-game drawing routines were correctly generalized to take `(bits, n, canvasId, noteId, label)`, so the prior dark-mode contrast fix applies identically to the control panels; the control persists across UI changes (window width, sample count) until explicitly regenerated, since all representative ω share one bit length (8192) so `ensureControl` never auto-refreshes on ω switch — matches the stated intent. Regenerating `viz/anchor_digit_visualizer.html` from the updated template reproduced the committed file exactly. No issues found; ready to merge alongside the rest of branch `anchor-digits`.
- 2026-07-12 — anchor-digit-search brief REVIEWED (main session, branch `anchor-digits`, per the mirror-queue/kl-lp re-verification precedent): re-ran all four scripts (`anchor_digit_structure.py`, `anchor_compressibility.py`, `anchor_spectral.py`, `anchor_visualize.py`) independently and every reported number reproduced exactly (density, chi-squared/p-values, NIST artifact flags, LZ76/zlib means and p-values, spectral KS statistics); regenerating `viz/anchor_digit_visualizer.html` from its template produced a byte-identical file before the fix below. Spot-checked the generator's algebra by hand on `ω=3`: since `ω²=9` equals the log's own base, `Ly` and `L9` collapse to the same value inside `compute_M_omega`, forcing `N=-1` exactly — independently explains the worked-example table's `M(3)=-1` rather than merely matching it, which increases confidence the series implementation is right and not merely hardcoded-lucky. Confirmed the "known-law" positive control in the correlate sweep is discriminating for a real reason (digit-determinacy ties `M(ω)`'s low bits only to `ω`'s own low bits, not to digit-sum parity, which depends on all of ω) rather than passing by construction. One bug found and fixed: the recurrence plot's dark-mode grayscale mapping had identical windows (`frac=0`) rendering at `v=0`, nearly indistinguishable from the `~(26,28,34)` dark background, while light mode correctly rendered identical windows at high contrast (`v≈25` against white) — meaning the diagonal and any real similarity stripe were nearly invisible in dark mode specifically. Fixed in `viz/_anchor_digit_visualizer_template.html` (dark-mode formula flipped so identical windows are always the bright, high-contrast feature) and regenerated `viz/anchor_digit_visualizer.html`. No other issues found; the statistical conclusions in anchors.md §17.7.1 are unaffected (the bug was a display-only color mapping, not a computation). Ready to merge to main pending the author's go-ahead.
- 2026-07-12 — anchor-digit-search brief EXECUTED (anchors.md §17.7.1, branch `anchor-digits`, delegated session per `briefs/anchor-digit-search-brief.md`): the single-sequence digit-structure search on M(ω) ran to completion, a clean pass on every planned test at stated significance levels. Four independent generator implementations (`experiments/anchor_digit_structure.py`, `anchor_compressibility.py`, `anchor_visualize.py`, `anchor_spectral.py`), none importing another, each validated exactly against the 12-value worked-example table in anchors.md §17.7 before use. Single-sequence statistics (200 ω, seed 20260712, 4096 bits): monobit density 0.49999; run-length vs Geometric(1/2) pooled chi2=12.93/df13 p=0.45 with clean per-sequence meta-check; autocorrelation lags 1-64 pooled chi2=46.4/df64 p=0.95, no lag past the Bonferroni threshold; overlapping-window chi-squared (w=3,4,5) pooled p∈[0.80,0.94] — a per-sequence meta-check artifact was found and traced to the test's own overlap-correlation approximation by reproducing it identically on a matched true-random control, not a property of M(ω); cross-string correlate sweep (ω mod 16/32, digit-sum parity) significant at bits 0-24 (the already-proven digit-determinacy law, not new) and NOT significant at bits 2000-2024 (the genuinely open region). Compressibility (200 ω, seed 20260713): Lempel-Ziv-76 (validated against the Kaspar-Schuster 1976 worked example) and zlib ratio both indistinguishable from a matched i.i.d.-coin control (Mann-Whitney p=0.46, p=1.00); NIST SP 800-22 battery via `nistrng` (standard package, not hand-rolled) — 7/11 eligible tests clean outright, 4 show a mean-of-p-values meta-check artifact confirmed shared identically by the matched control; two `nistrng` bugs found and worked around, not patched (Monobit needs 0/1 not ±1 input; cumulative-sums overflows on int8). Visualizations (10 representative ω, 8192 bits) committed to `viz/anchor_digit_visualizer.html`: recurrence plot + chaos-game (both the brief's literal 2-corner spec and a standard 4-corner CGR variant), qualitatively confirming the bottom/bulk distinction before the bulk was tested statistically. Spectral flatness (if-time item, 200 ω, seed 20260714): pooled periodogram two-sample KS vs matched control D=0.00197 p=0.40. No single-sequence structure found in the genuinely open regime; findings written into anchors.md §17.7.1/§17.10, index.md updated. Branch `anchor-digits` not merged to main pending main-session re-verification, per the brief and the mirror-queue/kl-lp precedent.
- 2026-07-12 — anchors.md (§17) created + briefs/anchor-digit-search-brief.md written: not a proof or a computational result, a structural addition, logged per the "when adding a new result" workflow. anchors.md consolidates the anchor concept (definitions, algebra, per-step laws, effective bounds, cycle-walk framing, AEH's existing cross-sectional statistics) as pointers only — no facts restated, per one-fact-one-page. Its new content (§17.7–17.9) precisely scopes an unexecuted search: whether a single M(ω)'s bit string has intra-sequence structure (autocorrelation, compressibility, self-similarity, spectral flatness), confirmed as genuinely untested territory by checking every existing experiment script. A worked example (12 computed M(ω) bit strings, ω=1..37) is recorded in §17.7, including the algebraic degeneracies at ω=1 (M=0) and ω=3 (M=-1, since 3²=9 is the log's own base). index.md's page table, resolver, and status paragraph updated to include §17. The delegation brief follows the mirror-queue/kl-lp precedent exactly (branch discipline, fresh-code verification, explicit stop criteria treating a clean pass as a valid outcome, no scope creep) and is scoped for a fresh session per the author's own token-cost consideration, on branch `anchor-digits`.
- 2026-07-12 — cycles.md 12.8.2's explicit `n_0(p)` CLOSED: completed the `γ ↔ Λ` conversion flagged as a sketch earlier the same session. Proof chain: `q = 2^(K-γ)` (definition) and `2^K = 3^n + q` (12.1.1) give `Λ = log(1+q/3^n) < q/3^n = 2^(Λ/log2 - γ)` (via `log(1+x)<x`); the crude, always-true bound `Λ < p` (Corollary 12.1.2, since each `1/x_t <= 1`) breaks the self-reference, giving the unconditional `Λ < exp(p - γ·log2)`. Combined with Rhin's bound (`Λ > exp(-13.3(0.46057+log n))`, pinned earlier this session) and Theorem 12.8.1's `γ > 0.585n/(1.585^p-1) - log_2 p`, this yields an explicit equation defining `n_0(p)`. Solved numerically (`python`, bisection) for `p ∈ {4,5,10,20,50,91,92,100}`, e.g. `n_0(91) ~ 2.99e21`; separately verified the contradiction persists (widens monotonically) for all `n` beyond threshold up to `n=10^30` at `p=10` — no reversal, so `n_0(p)` is a genuine ceiling, not a local artifact. Written into cycles.md 12.8.2 as a full Lemma-style proof plus verification note, replacing the earlier sketch. This closes the last item from the 2026-07-12 bridge-perimeter stress test (bridge.md 16.4.4, open-problems.md §11.8) — all three citation/constant gaps found that session are now resolved with checked numbers, none merely asserted.
- 2026-07-12 — spike-height exponent PINNED (stage1.md 11.8.3.11): user supplied the full text of Bugeaud–Laurent (1996, J. Number Theory 61, 311–342) after Yamada's secondary-source note proved insufficient (it solved a different corollary, Fermat quotients). Our case turns out to be their simplest one: `v_2(9^n·ω-1) = v(Λ)` for `Λ = 9^n - ω^(-1)`, both terms rational so `D=1`, and `p=2`'s trivial residue field makes every unit principal (`g=1`, automatic — no case-split needed), landing exactly on their Corollaire 2 (Dong Ping Ping's special case). Fully explicit, unconditional bound: `v_2(9^n·ω-1) <= 208·log9·logω·(max{log b'+log(log2)+0.4, 10log2, 10})^2`, `b'=n/logω+1/log9` — confirms the exponent is exactly `2` (not `log n`), with an explicit `C(ω)=208 log9 logω` (up to the theorem's own floor of 10 in the max, dominant until `n` reaches the tens of thousands). Numerically sanity-checked (`python`, this session): finite, slowly growing, consistent with the wiki's prior qualitative claim. Written into stage1.md 11.8.3.11, open-problems.md §11.8, bridge.md 16.4.4. Both companion gaps from the 2026-07-12 perimeter stress test are now closed with real citations and checked numbers (see the `log3/log2` entry below); only cycles.md 12.8.2's explicit `n_0(p)` remains as a short follow-up.
- 2026-07-12 — `log 3/log 2` measure PINNED (cycles.md 12.5.3, 12.7.5): user supplied the full text of Simons–de Weger (2005, Acta Arith. 117) after the sandbox's earlier scanned-PDF blocker. Their Lemma 12 applies G. Rhin's effective Proposition (p. 160 of *Approximants de Padé et mesures effectives d'irrationalité*, Progr. Math. 71, 1987) with `u_0=0, H=u_1=K+L, u_2=-K`, giving the unconditional bound `Λ = K log2 - n log3 > exp(-13.3(0.46057+log n))` in this wiki's own `K, n` notation (their `K` = our `n`; their `K+L` = our `K` — verified the correspondence exactly matches cycles.md's existing `K log 2 - n log 3` phrasing). Numeric check (`python`, this session): at `n=20,001` the bound gives `Λ > 2^(-198.9)`, while cycles.md's own trim lemmas require `Λ < 2^(-3994.2)` (12.5.3, period 2) and `Λ < 2^(-2297.1)` (12.7.4, period 3) — contradictions of ~3795 and ~2098 bits respectively, closing both `n > 20,000` cases with a fully explicit, cited, unconditional bound (no more `[#TODO pin measure]`). cycles.md 12.8.2's general corollary is sketched with the same tool (`Λ ≈ 2^(-γ)` relation) but the exact `n_0(p)` derivation is left as a follow-up, flagged honestly in-page rather than asserted. stage1.md 11.8.3.11's companion gap (the spike-height exponent, Bugeaud–Laurent 1996) remains open; recovered the general theorem shape via Yamada's arXiv note (math/0607072) but not the precise application to this program's linear form — needs Bugeaud–Laurent's own paper, not yet supplied.
- 2026-07-12 — #TODO consolidation + first chase attempt (open-problems.md §11.8): every live `#TODO` in the wiki indexed in one place, split into "resolved bibliographically" (5 items, publication.md's pinning pass) and "open substantively" (2 items: the spike-height exponent, stage1.md 11.8.3.11; the `log 3/log 2` effective measure, cycles.md 12.5.3/12.7.4/12.8.2). First real attempt at the second gap: identified that the correct classical tool is a two-integer-variable Baker/Feldman/Laurent–Mignotte–Nesterenko-style bound `|K log2 − n log3| > C·H^(−κ)`, not a generic irrationality measure of a fixed real number — ruled out G. Rhin's `μ(γ)<8.616` for `γ∈Qlog2+Qlog3` (verified via primary text, Zudilin survey arXiv:math/0404523 Theorem 3, extracted with `pdftotext`) as almost certainly the wrong tool despite superficial fit. Confirmed the wiki's own polylog framing matches Baker's actual bound shape (independent secondary source: Tao's 2011 blog post). Blocked on extracting the actual numeric constant: Simons–de Weger (2005) is a scanned PDF with no OCR available in this sandbox; Laurent–Mignotte–Nesterenko (1995) is paywalled, not on arXiv. No wiki proof was touched — no unverified number was pinned into cycles.md or stage1.md. Recorded as an honest dead end per house norms, not a refutation.
- 2026-07-12 — perimeter stress-test (bridge.md §16.4.4): checked every edge of the "encircled on all sides" claim (16.5) against its owning pages. One soft spot found and recorded, not a refutation: the cycle escape's "tools with present traction" (effective `p`-adic Baker theory, stage1.md 11.8.3.11) has its citations bibliographically pinned (publication.md) but two numeric parameters it actually needs were never pinned to a checked value — the spike-height exponent (`(log d)^2` vs `(log d)^1`) and the effective irrationality measure for `log_2 3` that Corollary 12.8.2 needs for an explicit `n_0(p)` (both still `[#TODO]` in stage1.md and cycles.md 12.5.3/12.8.2, predating this check). The traction is real in shape (polylog spike heights, exponential-in-`p` finiteness) but unverified in its constants against the cited theorems' actual hypotheses. All other five perimeter edges (16.4.1, 16.4.2, 16.4.3, 16.4.5, 16.4.6) checked against their owning theorems/proofs and hold as stated. No code changes; this is a reading/citation audit, not a computational check.
- 2026-07-11 — bridge consolidation + core-extraction deficit (bridge.md §16): the forward and reverse per-step anchor precision loss verified as one phenomenon under 2↔3. Forward 2-adic law (depth coordinate held fixed to isolate the ω-tail, d ∈ {5,7,12}): ω mod 2^(σ+r) pins ω_+ mod 2^r with zero conflicts, broken one digit shorter — exactly σ = v₂(C) digits lost per step (mean σ ≈ 4.0, min 2). Reverse 3-adic law: y mod 3^(d+r) pins ω' mod 3^r, zero conflicts, broken one digit shorter — exactly d = v₃(N) digits lost per step (mean d ≈ 1.5, min 1). Fresh implementation importing nothing from the repo. Diagnostic only (not a lever): unifies the forward digit budget (11.8.7.7) and the reverse affine-collapse obstruction (14.13); changes nothing about what is provable. Code: experiments/digit_duality.py.
- 2026-07-11 — KL–LP refinement stages 2–3 (reverse.md 14.13, branch `kl-lp`): three residue-LP constructions attempted, none verified sound. (1) naive stationary `y mod 3^k` transitions — REFUTED by direct counter-check (3,120 transitions vs. varied higher digits, 2,279 failures); correct precision law re-derived and verified instead (parent known mod 3^k pins child only mod 3^(k−d), 4,160 checks, zero failures). (2) drop-on-overflow `(j,r)` DAG — mathematically valid but provably vacuous (finite acyclic graph ⇒ spectral radius ≡ 0 for all c; bisection collapses to search floor for k=1..6). (3) generic-fallback-credited-at-1 — produces attractive numbers (k=2..6: c*≈0.41→0.57) but conflates residue-precision exhaustion with the renewal induction's size-threshold base case; identified as unsound, NOT claimed as a result. Diagnosis: the collapse map is affine not multiplicative, so no stationary exact residue/anchor propagation exists (this is what obstructs both stage 2 and stage 3). Closes the brief per its stop criterion; primary bar (c>0.43) not reached as a verified theorem.
- 2026-07-11 — KL–LP refinement stage 1 (reverse.md 14.6.5, branch `kl-lp`, delegated session per `briefs/kl-lp-brief.md`): generic-door validity + distinctness (12,668 states, 21,169 live doors to ω≤2^14, zero collisions/recovery failures; 342 middle-door T-orbit checks, zero failures); exact ternary ledger (753 windows, k∈{2,3,4}, zero failures); multi-door critical exponent lifted 0.3304→0.33515 (bonus-disabled sanity check reproduces 0.3304 exactly). Falls short of the 0.43 success bar. Code: `experiments/density_lp.py`.
- 2026-07-06 — wiki split from `sources/drafts/collatz_reduction_rewrite_v078.md`; concatenation of page bodies reproduces the monolith byte-identically.
- 2026-07-06 — independent numeric verification of: unified depth-side law (11.8.5.6.2), entry-depth law all six classes (11.8.6.3), orbit projection + fiber of (1,1) (9.8), 8,000 random states + orbit traces, zero failures.
- 2026-07-07 — low-order anchor-increment law (11.8.7.3.1, stage4.md): predictor-from-truncations test (4,046 checks) and lift-invariance test (10,092 pairs), k ∈ {1,3,6}, zero failures. Code: experiments/anchor_increment.py.
- 2026-07-07 — one-step propagation (11.8.7.6.1, stage4.md): window-only decision along real orbits, k ∈ {4,8}, 21,296 steps — 21,000 decided with zero errors, 296 undecided with zero violations of the deep-cascade bound s_+ >= k+2. Code: experiments/one_step_propagation.py.
- 2026-07-07 — period-1 cycle classification (12.2.3, cycles.md): exact big-integer window+divisibility search, m <= 20,000, unique solution = trivial fixed point; independent direct F-orbit cycle search (ω < 3,000, d <= 32) finds only {(1,1)}. Code: experiments/period1_cycles.py.
- 2026-07-07 — period-2 cycle classification (12.5.3, cycles.md): two-step elimination identity verified on 3,000 random orbit pairs (zero failures); pruned exact search n <= 20,000 — 19 open windows, 11 size-passing quadruples, no nontrivial solutions. First search run had an off-by-one in 3^n, caught by an s-split consistency check and corrected; recorded run passes sanity assertions. Code: experiments/period2_cycles.py.
- 2026-07-07 — general-p elimination (12.6.1) verified on random orbits p ∈ {3,4,5} + trivial-cycle identity R = 4^p − 3^p for p ∈ {1,2,3,4,7}; period-3 search (12.7.1): n <= 20,000, 76 treated n, 886 exact evaluations, 51 size-passers, no nontrivial solutions; prune audited on 4,798 cells (skipped region + box boundary), zero violations. Two filter-constant errors caught and corrected in-session (0.138→0.098; missing box branch); corrected run changed no outcomes. Code: experiments/period3_cycles.py.
- 2026-07-08 — period-3 trim lemma (12.7.4): γ > 0.1157n − 2 for any period-3 candidate; verified against all 51 exact size-passers in the search range, worst margin +3.1 bits; completes the period-3 classification (Theorem 12.7.5). Code: experiments/period3_cycles.py part D.
- 2026-07-08 — AEH formalization + calibration (aeh.md §13): bulk uniformity confirmed across all tested cells with per-orbit statistics (~1,600 orbits/cell, |z| < 1 after bulk/bottom separation); bottom-regime biases quantified (z up to 41, finite-structural); three apparent discoveries dissolved under controls and are recorded as methodology; ONE open anomaly — class (1 mod 8, d=1), bits 3–4 = 3 at 0.2677 vs 0.25, z = 5.0, n = 2,610 orbits, survives 2^40 cut, unpredicted by depth-5 chain. Code: experiments/aeh_calibration.py.
- 2026-07-12 — paper 2 finalized as v1 after two external referee cycles (revisions 2 and 3 + final fixes, commits 132851c/0771aef/2e92824); synthesis found during cross-referencing: steering (iii) ≡ forward valuation law (one identity, two readings) — recorded as Remark 7.2 in the paper and 14.12.3 in the wiki.
- 2026-07-12 — wiki cleanup: paper-2 referee corrections propagated to reverse.md (M₃(1) exponent-group language; mortality residue-class phrasing; density constants 2^(−3.6)/2^12/2^11.415 aligned with the canonical five-lemma proof); steering laws back-ported as 14.12 with experiments/steering.py (freeze 1,983 checks, placement 0 failures, sharpness attained in 460 cases).
- 2026-07-11 — rigorous density bound (reverse.md 14.6): door-tree core with collapse identity y' = (2^(s+1)y−1)/3 (verified both predecessor types, 13,408 checks); triple law {0,3,6} mod 9 (20,000 checks); core tree built to 2^18, all sampled nodes reach 1, empirical exponent ≈0.45 above the proved worst-case c* = 0.3304; Theorem 14.6.4: π̃(X) ≥ 2^(−3.3)·X^0.3, renewal induction with partial mass 1.0502 at two windows. KL paper read (arXiv:math/0205002): their 0.84175 via LP mod 3^11; positioning stated honestly. Code: experiments/reverse_tree.py (14.6 additions).
- 2026-07-11 — mirror-queue review (delegated Sonnet 5 session, branch mirror-queue): all four dual theorems (14.7–14.10) re-verified by running experiments/mirror_dual.py in the review session — zero failures across ~50,000 checks; brief compliance confirmed (branch discipline, per-theorem commits, honest asymmetry findings); MERGED.
- 2026-07-11 — dead ends (reverse.md 14.5): door-mortality law verified exactly (20,000 states, all doors); Garden-of-Eden classification verified (600 states vs forward image); mortality-corrected renewal mass computed — min 1.52 at c ≈ 0.7, supercritical for all c < 1, predicting full density, consistent with tree exponents 0.970→0.981; rigorous KL-style truncated bound named as the open theorem target. Code: experiments/reverse_tree.py (14.5 additions).
- 2026-07-10 — ladder law (ladder.md §15): dichotomy verified on 30,000 states (9,906 off-spike: e(ω,d+1) = T(e(ω,d)); 10,085 spike: affine kick + s→1), zero failures both branches; stronger column-encodes-orbit hope tested and refuted (run-length histogram, all runs length 1); recorded per house norms. Code: experiments/ladder.py.
- 2026-07-10 — reverse front (reverse.md §14): predecessor rule verified exactly against brute force (7 targets); backward valuation law d = 1 + v₃(s − M₃(y)) verified (4,265 checks, zero failures); backward ledger 2·3^(−j) confirmed; M₃ algebra (M₃(1) = 3-adic −1; affine-log homomorphism, 300 pairs, zero failures); exact tree enumeration to ω ≤ 2^19 (408,302 states, exponent ≈ 0.98 rising). Recorded failure: first renewal-equation attempt wrong (representative multiplicity); multi-type equation is the open item. Code: experiments/reverse_tree.py.
- 2026-07-10 — mirror-queue (reverse.md §14.7–14.10, branch `mirror-queue`): forward per-step machinery (paper sec:anchor, Thm 3.5–3.6) fully dualized. 14.7 digit-determinacy facts (a')-(c'): 3,000 checks each, zero failures; combined window theorem r ∈ {1,3,6}, ~2,670 checks each, zero failures — finding: fact (c)'s cross-prime step has no mirror (N=2^s y+1 is odd by construction). 14.8 top-door ΔM₃ increment law: 6,000 trials, 965 alive/988 dead (freeze rate 0.5059 vs. Thm 14.5.1's exact 1/2), window recovery zero failures on all alive cases — identifies the "frozen" phenomenon as door mortality, a hard failure with no forward analogue (ΔM is total). 14.9 one-step dichotomy: K ∈ {2,4,6,8}, ~13,200 trials each, zero decision errors, undecided rate tracking 3^(−K), zero deep-bound violations — finding: forward's trichotomy collapses to a dichotomy (no non-lifting shortcut class exists backward). 14.10 dual ladder (adjacent s, fixed door): 30,000 trials, 13,445 + 6,547 split, zero failures both branches — exact dichotomy gated by Thm 14.2.4's first digit, coefficient 4 = 2² from the forced step-2 in s. Code: experiments/mirror_dual.py.
- 2026-07-09 — novelty sweep (publication.md): claim-by-claim verdicts; citations pinned (Hercher m<=91 arXiv:2201.00406; Barina 2^71 J. Supercomputing 2025; Steiner; Simons–de Weger; Eliahou; Rhin; Yu; Bugeaud–Laurent); three contemporary neighbors identified incl. arXiv:2603.11066 (parallel human–LLM Collatz work) and a Lean-formalized conditional cycle theory. Inline #TODO cite markers now resolve via publication.md.
- 2026-07-08 — anomaly resolution (aeh.md 13.5): fixed-horizon unweighted sampling flat (0.2503 ± 0.0015, 84,739 pooled visits, flat at T = 1..32 and all clean altitude bands); deterministic routing lemma 13.5.1 verified exhaustively (26,673 states, zero exceptions); artifact mechanism identified (ratio-estimator bias through routing-coupled excursion lengths). Standing rule adopted: fixed-horizon, unweighted, per-visit sampling only. Code: experiments/aeh_anomaly.py.
- 2026-07-08 — uniform trim resolution (12.8): Theorem 12.8.1 proved (arc-weight/reset argument); staircase sharpness family exhibited — p=7, n=94, γ=6.74 passes all 7 rotation size tests where the p=3 constant demands 8.9, plus 84 passers at p=6; all fail divisibility; all respect the uniform bound (max ratio 0.23). Crossover plan withdrawn; cycle front parked per stopping rules. Code: experiments/uniform_trim.py.
