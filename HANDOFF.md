# Handoff

This document lets any capable model (or a future session of any model) continue the project without loss. It is a working document: it carries the current state only; how the state got here is the git log and the `briefs/` record.

## Onboarding order for a new assistant

1. `README.md` — what this project is, the strategy, and the **binding stopping rules**.
2. `AGENTS.md` — the schema and verification protocol. Non-negotiable: nothing is labeled proved without independently written verification code; failures are recorded, not deleted; sources/ is immutable.
3. `index.md` — the resolver; then whichever pages the task touches.
4. This file — current state and open items.

**Register norm (author's explicit preference):** flat, calibrated prose. No excitement inflation — it degrades judgment and invites hallucination. Heuristics are labeled heuristics. Claims pass through verification before they pass into prose.

**The author's role:** Ben directs. Empirically, the project's load-bearing ideas — the coordinate system itself, the family diagram, the mirror front, the ladder, the dead-ends map — originated as his plain-language questions. Take his naive-sounding questions seriously and work them before defaulting to queued engineering.

## State of the fronts (as of 2026-08-12)

- **Forward per-step theory** — closed per step → spine.md §9, stage1.md–stage4.md; terminal open object consolidated as the Bridge → bridge.md §16.
- **Cycles** — PARKED → cycles.md §12 (periods 1–3 closed; uniform trim resolved; staircase sharpness **proved at every period**, 12.8.6 — unconditional for `p ≥ 16`, finite check for `3 ≤ p ≤ 15`, `p ∈ {2,4}` by exhibition; the divisibility system one rotation-invariant condition, 12.6.1.1; the spent `|q| = 1` stock identified as the rational-anchor instance of the digit-match ceiling, 12.6.1.3; the joint law of `R_0 mod q` over the whole profile family measured at fourteen cells and found flat at every cross-prime frequency and every prime-power pair, the only structure a first harmonic caused by the numerator's magnitude at good near-misses, 12.6.1.7, `experiments/modq_spectrum.py`, 2026-09-08). Reopens only with a divisibility-aware (anchor-rigidity) idea, per the stopping rules. The all-`p` staircase arc's record is its seven findings files in `briefs/` (`staircase-allp-construction`, `staircase-allp-diophantine`, `staircase-status-audit`, `record-defects-repair`, `staircase-gamma-upper`, `staircase-status-apply`, `record-consistency-sweep`); the consolidated state is 12.8.6 itself.
- **Statistics / AEH** — formalized, calibrated clean; proof effort parked → aeh.md §13; the single-sequence structural axis is at its endpoint → anchor-digit-search.md §17.7; the symbolic form is now a named, proved equivalence (the genericity form, aeh.md 13.6), with the depth marginal exactified (13.6.5).
- **Reverse / mirror** — ACTIVE → reverse.md §14.1–14.14 (duality 14.1–14.12; door/exit seam 14.14; KL–LP closed on a structural obstruction, 14.13/14.6.5; the 3-adic Baker cap on entry depth, the twin of 11.8.3.11, at 14.2.5); the itinerary language → itinerary.md §14.15 (whole-period height laws 14.15.9; the door-word ↔ near-miss-anchor dictionary 14.15.10).
- **Ladder** — closed as local law → ladder.md §15; at the valuation level identified as the third face of stage3's target-shift mechanism (15.5).
- **Papers** — both published → publication.md (paper 1 at **v3**, DOI 10.5281/zenodo.21730505, published 2026-08-03 — a full revision after external review, 15 pages, repair history at `paper/collatz-reduced-version-history.md`; v1 DOI 10.5281/zenodo.21273548, v2 DOI 10.5281/zenodo.21421120; mirror paper DOI 10.5281/zenodo.21303918).

## Open work items, in priority order

1. **Eric Merle collaboration — current state.**

   **Live conditions.** **Both round-12 PRs are MERGED** — `pull/1` and `pull/2` of `github.com/macindoe/one-obstruction-three-faces`, merged 2026-08-17 by Merle himself (**he holds and uses merge rights — recorded here as a live convention, not a one-off**). **PR #3 (round 13) is MERGED** — approved by us 2026-09-03 (the review text at `briefs/merle-round13-review-findings.md` §7.1, posted verbatim under account `macindoe`), merged by Merle the same day at 17:00 UTC, merge commit **`95e7212`**, after his own pre-merge commit **`f336e57`** applying offer h5 to the L-A8 `(d-bis)` block (our wording and figures, the superseded 200-draw control kept visible, the acceptance-standing-in-for-a-discharge gap recorded in the ledger's own words) and restating the map's grading sentence to "offered for cross-side verification". The **NOTE-v1 bracket patch is on `main` at `93c6ac4`** (`grep "GRADE AT SIGNING"` on `main` returns nothing; the patch archived at `briefs/merle-round13-review-patches/` is discharged). **PR #4 (round 14) is OPEN** — opened by Merle 2026-09-03 18:14 UTC, branch `round-14` at **`2f7b865`**, base `main`, zero reviews, `+208/−17` over three files: `LEDGER.md` (+38, the new entry **L-A10**, seeded at one key with artifacts), `briefs/merle-breach-campaign-map.md` (+36/−17: the ⊢ §96 item rewritten to the two-line form, a "Withdrawn (2026-09-03)" block kept visible, the cross-side paragraph rewritten), `rounds/R14-merle.md` (+134, the letter). **Our key is pending the author's posting of the approving review drafted at `briefs/merle-round14-review-findings.md` §9.1** — everything in it verified on our side 2026-09-04 with fresh code (`experiments/merle_r14_check.py`, **46 checks, 0 failures**): L-A10 holds on 4,000 `(p,k)` pairs and 24,000 witnesses; the round-13 `p = 3` non-reproduction is diagnosed on both objects (our graph kept one of the half-map relation's two edges from each odd residue — the `m = 0` lift's; his witness edge is the other one for `p = 3` at every `k` and at `(7,8)`, though not at every `(p,k)` — a scope note in the review, not a defect); his artifacts at `db0e89d` run as committed (`run_050` byte-identical; `run_048` halts on its own canary C3 as he says; `run_049` identical except one float-tie count in P3, conclusion unchanged). **His two retractions are on record:** the phantom census withdrawn in full, including the round-13 "100 % of the faulty residue cycles are phantoms at `p = 7, k = 8`" (`run_048.py` never ran — its canary halts it — and never tested `p = 7`; committed as written with a retraction header) — **our own round-13 "100 % phantom" figure is ours, on our object, under a bounded search, and no longer matches anything of his**; and the "climbs forever inside its class" formulation withdrawn (`511 → 767 → 1151 ≡ 127 (mod 256)`, the class left at step 2). His spectral figure `5.62× @ f=124` is withdrawn as a recoverable claim; the item-2 peak replication closes in our round-13 wording (clustering a clean non-finding, spectral marginal reported flat), adopted in his letter. The round-13 Rhin locatability item is **closed**: `Collatz-Junction-Theorem` `main` at **`8bcee67`** carries the R201-I3 retraction at the head of its README, `+10/−0` by its diff, additive. Two flat notes carried in the review draft: the "ours also has" wording point (the relation carries both edges `255 → 127` and `255 → 255`) and the `run_049` P1 realisability being true by construction. The wiki carries a one-sentence pointer to L-A10 at `open-problems.md` 11.6, phrased "two keys once the round-14 review is posted" — revert that clause if the author decides not to post. The **Zenodo metadata note is PUBLISHED, 2026-09-04** — the drafted text (`briefs/merle-round13-review-findings.md` §7.3) was added to the v3 record's additional-notes field by the author; the version-history file (`paper/collatz-reduced-version-history.md`, the v3 section) carries the matching addendum. **ccchallenge's `Macindoe2026` entry points at v1** — noted flat; canonical citation = the v3 DOI.

   **Standing conventions.** Sending and acknowledgements stay with the author. The author pastes Merle's replies verbatim (Gmail retrieval garbles numerals; see quirks below). Two-key/three-repo protocol: claims are verified on both sides before they pass into prose or the shared ledger. Under the accepted §13 medium the technical half moves in PRs — one round per PR, the second key = the approving review; mail keeps everything that is not a claim. Personal background, on record from his round-10 letter: he is a lycée electricity teacher, no doctorate, a year in — personal paragraphs are always the author's to answer. The author's next mail carries the dictionary paragraph (`briefs/literature-dictionary-findings.md` §6 — the wiki's new terms-against-the-literature dictionary at `TOUR.md`, with the map-symbol convention stated once); that paragraph is mail, not PR business, and does not enter any review.

   **Repos, current values.** Shared repo `github.com/macindoe/one-obstruction-three-faces` — **public** (flipped by the author himself, 2026-07-24). HEAD **`93c6ac4`** (the NOTE-v1 bracket patch, on top of the PR #3 merge `95e7212`); branches `round-14` at `2f7b865` (PR #4, open), `round-13` at `f336e57` (merged), `note-v1-draft` at `96ccadf` (merged), `round-12` at `accda4b` (merged). Contents: `PROTOCOL.md` (§7 acceptance recorded in-file; §13 the PR medium), `LEDGER.md` (L1–L4, L-A1–L-A9 on `main`; L-A10 on `round-14`), `NOTE.md`, `NOTE-v1.md`, `rounds/`, `briefs/merle-breach-campaign-map.md`. His handle `ericmerle3789`; his Lean repo `ericmerle3789/one-obstruction-three-faces-lean`, HEAD **`db0e89d`** (`experiments/run_048.py`, `run_049.py`, `run_050.py` with their outputs — the L-A10 artifacts; his round-12 technical body `rounds/R11-merle.md`); his third repo `ericmerle3789/Collatz-Junction-Theorem`, `main` at **`8bcee67`** (the R201-I3 retraction surfaced at the README head), the retraction itself at `77e3f07` on `proof-assembly-v1`.

   **Ledger state.**
   - L1 — corrected, both directions.
   - L2 — two keys.
   - L3 — corrected by him 2026-07-24; accepted into the two-key record at `c40aa58`.
   - L4 — AEH replication; our key turned.
   - L-A1 — transport recurrence; two keys plus kernel. Credit: independent simultaneous discovery, both names.
   - L-A2 — repeated-word gcd law; two keys (`ec4f229`).
   - L-A3 — anchored-loops/spent-stock/Benford; two keys, with the (B) margin-quantification additions, the conditional date-stamped.
   - L-A4 — descent; two keys, plus the ContentDescent kernel key on the structured half (`08dc3d5`/`67c428a`).
   - L-A5 — adelic content invariant; two keys via `49351e5`, closure-verified.
   - L-A6 — calibrated lottery; two keys, scoped; pushed `641a530`.
   - L-A7 — torsion ruler; two keys — the Rhin 1987 / Simons–de Weger 2005 re-sourcing accepted, headline `n ≈ 2233`; the margin inequality now proved on both sides, ours at the true `c_gen` with uniform surplus `1 + log₂β`. The round-11 Rhin re-source warning is closed on both sides (2026-08-17): `BILAN_R201`'s R201-I3 retracted in place (`Collatz-Junction-Theorem` `77e3f07`, on `proof-assembly-v1`), naming this entry as the cross-adjudication in its own commit message and file header, and since 2026-09-03 surfaced at that repo's README head on `main` (`8bcee67`, `+10/−0`, additive) — the round-13 locatability note is closed; no number here moves.
   - L-A8 — T1/no-hair; two keys on the mathematics, kernel claims scoped to the read-not-built audits, the ceiling repair verified; entry in his own proposed wording via `1d7907c`. The `(d-bis)` census-line correction (offer h5) is applied at `f336e57` (2026-09-03, his pre-merge commit for PR #3) in our wording and figures, the superseded 200-draw control kept visible — closed, verified at its address 2026-09-04.
   - L-A9 — δ8 impossibility; split-grade, offers h1–h4 applied and independently re-verified (round 13, PR #3, merged 2026-09-03): the Dirichlet half at two keys, unconditional theorem-grade in the single restated convention (margin `0.0383`, confirmed convention-invariant); the scissors half measured, permanently (`α > 1/3` is itself as hard as an unproved effective bound on `log₂3`).
   - L-A10 — no finite-quotient altitude descends: for odd `p ≥ 3`, fixed `k`, every positive `f` on `ℤ/2^k`, `V(x) = x·f(x mod 2^k)` fails to descend under the Terras half map on the positive integers (the class `x ≡ −(p−2)^{−1} (mod 2^{k+1})` rises within one residue mod `2^k`; at `p = 3`, `511 → 767`, both `≡ 255 (mod 256)`). Seeded one key (Merle, PR #4, artifacts at his `db0e89d`); **ours turns with the posted round-14 review** (verified `experiments/merle_r14_check.py`, 46 checks, 0 failures, 2026-09-04; review draft at `briefs/merle-round14-review-findings.md` §9.1). Excludes no cycle; not in Lean; the `k`-growing escape is prose only. The round-13 `p = 3` non-reproduction is diagnosed on both objects (our accelerated single-successor graph kept the `m = 0` lift's edge only); his phantom census is withdrawn in full, and our round-13 "100 % phantom" is ours, on our object, under a bounded search.

   **Standing decisions.** L-A1 credit: independent simultaneous discovery, both names. Hosting on collatz-lab.org: approved, DOI-pinned to v2 (10.5281/zenodo.21421120), the mirror-paper pair offered. Venue for the joint note: number-theory-shaped, with the formalization as supporting artifact. Citation posture: Gersonides 1342/43, not Mihailescu. Gateway visualization: each side builds its own cycle-side gateway and cross-links — ours is `viz/cycle_anchor_gateway.html`. His credit-deflection preference is on file with no record change (Remark 12.6.1.2 already reads packaging his / verification joint); the note's credit language at drafting time is the author's call.

   The hosting pin (v2) and the round-12 canonical-citation guidance (v3) are different objects and coexist; whether the hosting pin moves to v3 is the author's open decision.

   **The paper at v3.** Published 2026-08-03, DOI 10.5281/zenodo.21730505 — a full revision, not the erratum round 12 prepared: a six-round external-review arc rebuilt the paper (94 commits; record in `briefs/v3r*` and `paper/collatz-reduced-version-history.md`, not here). v2 is archived to `sources/paper/`. The Zenodo file is hash-identical to `paper/collatz-reduced-v3.pdf`. One recorded defect (ours): the published PDF's Version note still carries its drafting-time "not yet published" self-description, frozen into the immutable file — recorded in the version-history file. The paper's Status paragraph pins `cycles.md` at `9d9d1ec` and Appendix A at `6285485`, both resolving on public `main`. Citation guidance: v3 for the dichotomy phrases and the Theorem 4.6 obstruction sentence; v2 for the contiguous `p ∈ {2,…,23}` evidence note.

   **The author's round-12 decisions (2026-07-30).** (1) The cost answer is his to write — and it noted the spend-limit symmetry: the L-A9 delegate was cut off once by the monthly spend limit mid-run — Merle's own failure mode exactly — and resumed cleanly. (2) §13 accepted — the PR medium above. (3) Smallest-object accepted in principle; the pen taken (the note promised in PR #1, delivered as PR #2). (4) Erratum before the reply — discharged by v3. (5) The regime-column protocol accepted — carried in PR #1.

   **Pointers.** The Merle credit language (verbatim preservation everywhere): cycles.md 12.6.1.1 / 12.6.1.2 / 12.6.1.4 / 12.8.6.1 (rotation reformulation, both names) / 12.8.6.4 (pincer credit); aeh.md §13.4's external-replication line. Also itinerary.md 14.15.9(a); `experiments/transport_recurrence_vectors.json`; open-problems.md 11.6 (the L-A10 calibration sentence). The `briefs/merle-*` and `briefs/jointnote-*` files are the round-by-round record.

2. **Brett Rubin correspondence (new, 2026-09-05).** An independent worker on the problem since 2022, empirical rather than formal, wrote to the author after reading paper 1 at v3 with two claims and one observation. (i) His `columnCenter` law is paper 1's Theorem 3.3, the lifting clause, under renaming. (ii) His 3-adic law with the primes swapped, `v₃(e·2^((e mod 3) + 2k) + 1) = v₃(k − D) + 1`, is reverse.md 14.2.4 under `s = (e mod 3) + 2k`, with `D` the affine reparametrisation of `M₃` (`s − M₃(e) = 2(k − D)`) and his `e = 1` degeneration Lemma 14.2.1 — an independent rediscovery of the mirror paper's central law; he had not seen the mirror paper. (iii) The mirror side carried no Baker cap on `d` as a function of `s` — a real gap, filled at reverse.md 14.2.5 with the credit clause "flagged by Brett Rubin (correspondence 2026-09-05)" (merged 2026-09-08; `experiments/mirror_baker_cap.py`, 33,260 checks, 0 failures; record at `briefs/rubin-3adic-baker-findings.md`, brief alongside). Both his laws and his two quoted valuations were reproduced here with fresh code before the reply went out. **The author's reply was sent 2026-09-08:** thanks; the verification; the mirror paper's DOI (10.5281/zenodo.21303918, at v1, not revised); the repo as the live record (reverse.md and the TOUR.md dictionary named); the Baker-cap credit; yes to his offered scripts; citation guidance (the v3 DOI canonical). He waives priority explicitly, citing the author's March 2023 note as earlier than anything he holds, and asked how to cite. **Conventions carry over from item 1:** sending and personal paragraphs stay with the author; no address of his in any tracked file; his letters paraphrased, never quoted. **Open, all the author's:** his scripts, if sent, are run as reproduction, not as verification (the two-key habit); the mirror paper stays at v1 — a Zenodo additional-notes line on its record pointing at reverse.md 14.2.5 is the cheap alternative to a v2, not taken; the findings' §6 carries two qualifications to the reply's phrase "it transplants directly" (the squaring step to principal units; the two-point excluded locus `y = ±1`) and one caveat on the source (the general-`p` text is quoted from Bennett–Bugeaud 2011, Theorem 2.1, which cites the 1996 result as Théorème 3 against 11.8.3.11's "Corollaire 2"; the `p = 2` control reproduces 11.8.3.11's constant to `< 0.03 %`, so the content is confirmed and only the label is unverified name-for-name — resolvable by anyone holding the 1996 text). The forward pin's own `ω = 1` scope defect, found in this window, is fixed at 11.8.3.11 (the constant vanishes there; the exact isometry covers the case).

3. **KL–LP residual** (low priority; CLOSED front, see State of the fronts). One well-defined sub-question survives in 14.13: whether a *size-threshold-coupled* version of the `(j,r)` DAG (coupling precision-loss to the renewal induction's accumulated-offset variable, rather than crediting exhaustion for free) recovers real gains from residues. Reopens only with an idea for that coupling, not with more computation.

4. **The wiki's own longer-horizon items.** The anchor-pinning framing thread (README's 2023 seed question) has run its course through the door/exit seam, block-map layer, and itinerary language: the Bridge's remaining content is exactly the stratum word at unbounded length, proved free at every finite level (full shift, itinerary.md 14.15.2), with a symbolic name (the diagonal compatibility locus, 14.15.3(c)) — paused until a further step can engage the word at unbounded length directly, which is the Bridge itself. The equidistribution question (AEH, aeh.md §13) stays long-range per the stopping rules: proof effort waits on an idea. Community outreach (r/Collatz, ccchallenge) is de-prioritized, not a standing item; a substantive response would be verified against the wiki and answered with one reply, no questions.

5. **Fresh-eyes assessment (2026-09-04).** A critical re-read of the whole approach is on record at `briefs/fresh-eyes-assessment-findings.md`, with four filed scripts (`experiments/law_spotcheck_logseries.py`, `digit_budget_avalanche.py`, `syrac_residue_fourier.py`, `cycle_offset_identity.py`) and the calibration remarks they support (stage4.md 11.8.7.7, cycles.md 12.6.1.7, the README clause on the statistics half). No lever, no sidestep; every front status unchanged. The companion Tao Section 5 read is complete, verdict negative: Tao's proof does not deliver the letter statistics past the digit budget — Proposition 1.9 is applied once per fresh scale to a fresh logarithmically uniform draw, never to a first-passage law, and the whole-descent statement would need an argument the paper does not give — so publication.md's "unfound residue" stands as written, with the nearest mechanism now recorded there and at aeh.md 13.2 / 13.3.3 (`briefs/tao-section5-read-findings.md`, on `main` at `72b1a27`). The terminology-alignment question is decided (2026-09-04): no wholesale renaming of the record's coinages — they name objects the literature has not named and are frozen into both DOI'd papers and the shared ledger — and instead a dictionary of the record's terms against the literature at `TOUR.md`, with the map-symbol convention stated there once (this record's `T` is Tao's `Syr`, the literature's `T` is this record's `T₁`, and the raw map is `Col` in spine.md §3–4 only, sections no ledger entry cites) and literature names added at the points of definition (cycles.md 12.6.1.1, anchors.md 17.1; itinerary.md 14.15.1 already carried its own). The global swap of the odd-to-odd `T` to `Syr` across the wiki was measured and not performed — the author's separate call, with the counts and cost at `briefs/literature-dictionary-findings.md`.

6. **Tear afterlife (ladder.md, candidate 15.6) — backlog, delegatable.** The composition of the ladder with the step law, the one item ladder.md 15.4 leaves unexamined. Pre-checked in the main session with throwaway code (2026-09-09; script deliberately not filed, per the delegation pattern): at a spike of height `s ≥ 3` at `(ω, d)` with exit `e`, `F(ω, d+1) = (3·2^(s−2)·e + 1, 1)`, the torn core `≡ 1 (mod 2^(s−2))`; the torn state's reduced word opens with exactly `⌊(s−1)/2⌋` letters `(s, m₊) = (1,1)`, never more (the run ends at a depth-1 core `≡ 5 (mod 8)` or `≡ 3 (mod 4)`); off-spike the vertical neighbours merge in `F` (`F(ω, d+1) = F(ω, d)` for `m₊ ≥ 2`, `= F²(ω, d)` for `m₊ = 1`); `v₂(M(Ω')) = s − 4` for `s ≥ 5`, so column `Ω'` coincides with column `1` at every depth `d'` with `v₂(d') < s − 4`; the mirror tear (reverse.md 14.10, `d ≥ 2`) lands at `(4·3^(d−1)·ω − 1, 1)`, core `≡ −1 (mod 3^(d−1))`. Counts: 6,768 spikes, 26,245 mirror edges, 121 anchor cases, 0 failures. One line to carry as **measured, not proved**: the upper sister `3·2^(s−1)·e + 1` was never found downstream of `e` (3,247 generic spikes and every small case; provable by a sign argument for one and two steps only), while `e` is downstream of the upper sister in the drainage basin (`e = 1, 5, 13, 23`) and never generically; the two orbits merge strictly below both sisters in 3,167 of 3,247. Grade at delegation: unification note — the `+1`-side twin of the block cascade (`x ≡ −1 mod 2^m` climbs `m` steps; `x ≡ +1 mod 2^k` descends `⌊k/2⌋`), placed at the tear; no front moves; stopping rules untouched. To do: a brief on the `ladder-targetshift` pattern, fresh script `experiments/ladder_afterlife.py`, the lemma at ladder.md 15.6 with the two-metric calibration (2-adically near the fixed point, archimedean-ly far), pointers from 15.4 and anchors.md 17.3.

## The delegation pattern (proven)

One full case study exists: `briefs/mirror-queue-brief.md` → Sonnet session → branch `mirror-queue` → four theorems with per-commit discipline → independent re-verification in review (~50k checks re-run) → merge. The pattern: (i) written brief forcing AGENTS.md compliance and branch discipline; (ii) explicit "record obstructions, don't force analogies" instruction; (iii) reviewer re-runs all verification code before merging. Use it for the delegatable items above (Merle follow-up, KL–LP, the long-horizon items) — not author-only publication steps.

## Known infrastructure quirks

- The sandbox mount of the repo can serve stale reads mid-session (files appear truncated). The Windows-side files are authoritative; verify via PowerShell (`git status`, line counts) before "repairing" anything.
- **Never edit a tracked file with PowerShell 5.1 `Get-Content`/`Set-Content`.** `Get-Content` decodes UTF-8 as ANSI and `Set-Content -Encoding utf8` re-encodes the result as UTF-8, double-encoding every non-ASCII character and adding a BOM. These pages are full of `—`, `≤`, `⌈⌉`, `ε`, `β`, so the damage is total and silent — it survives commits, rebases and merges, and `git diff --stat` shows only the lines you meant to touch. Observed 2026-07-26 resolving a rebase conflict in `HANDOFF.md`: 125 mangled sequences, repaired by restoring the file from the last clean commit (`0378ee4`) and re-applying the wanted change from its own uncorrupted commit rather than attempting byte surgery. Use the Edit/Write tools for file content; if PowerShell is unavoidable, use `[System.IO.File]::ReadAllText/WriteAllLines` with an explicit `UTF8Encoding($false)`. **Check with `experiments/encoding_scan.py`** (run it over the tracked tree before committing anything PowerShell has touched): it reports files that are not valid UTF-8, files carrying a BOM, and files containing the double-encoding signatures of the characters these pages actually use. The same script found and fixed two older instances of the *inverse* fault — `merle_la6_check_output.txt` had been written in cp1252 (a `0x97` em-dash byte, from a round-9 delegate session) and five committed script outputs carried BOMs from PowerShell `>` redirection, which is what caused the "output identical modulo a BOM" friction at earlier reviews. Do not paste a literal mojibake sample into any tracked file — it makes the file itself a permanent false positive.
- The Gmail retrieval pipeline can silently garble numerals in correspondence (observed 2026-07-17: several two-digit groups rendered as single glyphs in a Merle email that displayed cleanly in the author's own client; a reconstructed guess then produced a spurious "disagreement," dissolved once the author read the true value — `briefs/merle-pincer-check-findings.md`, Correction). Before verifying or disputing any number quoted from email, have the author confirm the digits from his own client.
- LaTeX builds: the mount locks aux files; build in a sandbox temp dir and copy artifacts in.
- `\wp` and `\dp` are TeX primitives; the papers use `\wnext`, `\dnext`.
