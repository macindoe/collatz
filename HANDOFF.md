# Handoff

This document lets any capable model (or a future session of any model) continue the project without loss. It is a working document: it carries the current state only; how the state got here is the git log and the `briefs/` record.

## Onboarding order for a new assistant

1. `README.md` — what this project is, the strategy, and the **binding stopping rules**.
2. `AGENTS.md` — the schema and verification protocol. Non-negotiable: nothing is labeled proved without independently written verification code; failures are recorded, not deleted; sources/ is immutable.
3. `index.md` — the resolver; then whichever pages the task touches.
4. This file — current state and open items.

**Register norm (author's explicit preference):** flat, calibrated prose. No excitement inflation — it degrades judgment and invites hallucination. Heuristics are labeled heuristics. Claims pass through verification before they pass into prose.

**The author's role:** Ben directs. Empirically, the project's load-bearing ideas — the coordinate system itself, the family diagram, the mirror front, the ladder, the dead-ends map — originated as his plain-language questions. Take his naive-sounding questions seriously and work them before defaulting to queued engineering.

## State of the fronts

- **Forward per-step theory** — closed per step → spine.md §9, stage1.md–stage4.md; terminal open object consolidated as the Bridge → bridge.md §16.
- **Cycles** — PARKED → cycles.md §12 (periods 1–3 closed; uniform trim resolved; staircase sharpness at floor grade, contiguous `p ∈ {2,...,23}`; the divisibility system one rotation-invariant condition, 12.6.1.1; the spent `|q| = 1` stock identified as the rational-anchor instance of the digit-match ceiling, 12.6.1.3). Reopens only with a divisibility-aware (anchor-rigidity) idea, per the stopping rules.
- **Statistics / AEH** — formalized, calibrated clean; proof effort parked → aeh.md §13; the single-sequence structural axis is at its endpoint → anchor-digit-search.md §17.7; the symbolic form is now a named, proved equivalence (the genericity form, aeh.md 13.6), with the depth marginal exactified (13.6.5).
- **Reverse / mirror** — ACTIVE → reverse.md §14.1–14.14 (duality 14.1–14.12; door/exit seam 14.14; KL–LP closed on a structural obstruction, 14.13/14.6.5); the itinerary language → itinerary.md §14.15 (whole-period height laws 14.15.9; the door-word ↔ near-miss-anchor dictionary 14.15.10).
- **Ladder** — closed as local law → ladder.md §15; at the valuation level identified as the third face of stage3's target-shift mechanism (15.5).
- **Papers** — both published → publication.md (paper 1 DOI 10.5281/zenodo.21273548, v2 DOI 10.5281/zenodo.21421120; mirror paper DOI 10.5281/zenodo.21303918).

## Open work items, in priority order

1. **Eric Merle collaboration — current state.**

   **Standing conventions.** Sending and acknowledgements stay with the author. The author pastes Merle's replies verbatim (Gmail retrieval garbles numerals; see quirks below). Two-key/three-repo protocol: claims are verified on both sides before they pass into prose or the shared ledger.

   **Repos and ledger facts.** Shared repo `github.com/macindoe/one-obstruction-three-faces` — **public** (flipped by the author himself, 2026-07-24; historical flag on record in `briefs/merle-round7-check-findings.md`, addendum). **HEAD `81431c7`** (his, 2026-07-24 — six commits over our `e53630f` co-edit: review polish `6b9f2b1` (NOTE §6 arithmetic-vs-AEH-equidistribution disambiguation, accurate against 12.6.1.6/13.6.7; L-A5 citation gains the committed `#print axioms` link + unreduced-modulus note), **the L-A5 closure `49351e5`** (closing gloss restated per our offer (a), `|q| > 1` domain per offer (b)), L-A6 seeds `92a6edb`/`fb5e8fc` and L-A7 seed `81431c7` (checks in flight, separate briefs), and the ContentDescent kernel block `08dc3d5`; **L-A5 — two keys CONFIRMED**: the restatement is claim-identical to our offer, no drift anywhere else in the entry, closure-verified `briefs/merle-la5-closure-findings.md` (round record: `briefs/merle-la5-check-findings.md`, `briefs/merle-la5-coedit-findings.md`; two flat co-edit observations on file — the wall rider's elided "aperiodic/primitive", our now-satisfied DRAFT conditional); his Lean repo **HEAD `97b57d7`**, linear graph `017288f → 3ed1ef4 → 9932f3f → e297d9d → 905d75b → ae1edba → 67c428a → … → 97b57d7` — **`905d75b`** commits `ContentSeparation_axioms.txt` (kernel-3, all five theorems) plus the unreduced-modulus header note and REQ-MATH-021 (added *after* our la5 check examined `e297d9d`; reconciled flat, not missed), **`67c428a`** commits `ContentDescent.lean` + its axioms log). Contents: `PROTOCOL.md` (§7 acceptance recorded in-file), `LEDGER.md`, `NOTE.md` (his architecture skeleton — eight sections, every claim entering via a turned ledger entry; **§4 and §6 rewritten by him 2026-07-24** to the no-Hasse-gap/local-obstruction and equidistribution framing, our pin `430c00c` and "positive odd integer" precision `d2407b9` both preserved in his rewrite). His handle `ericmerle3789`; his Lean repo `ericmerle3789/one-obstruction-three-faces-lean` (kernel-verified L-A1 artifact; **scripts stack now `017288f`** — five scripts REQ-MATH-013..017 with outputs: stratum-rigidity negative, capacity-demand margin, Weyl/structure, size-artifact + descent, mod-7 local obstruction). Ledger entries: L1–L2 as before (L1 recorded "corrected, both directions"); **L3 — corrected by him 2026-07-24** (the session-2 "purely global" reading retracted: a linear divisibility has no Hasse gap, the p = 7 instance fails already in ℤ₇ with `v_7(q) = 3 > v_7(R_r) = 1` — verified independently our side; the correction is pure-additive, the original two-key data untouched, diff-verified); L4 — AEH replication, our key turned; L-A1 — transport recurrence, both keys plus kernel; L-A2 — repeated-word gcd law, **two keys** (`ec4f229`); L-A3 — anchored-loops/spent-stock/Benford, **two keys**, plus his round-8 "Additions accepted": (A) our signed characterization (Thm 14.15.6.7) as the two-shore frame, (B) the digit-match-ceiling identification (cycles.md 12.6.1.3) with his margin quantification ≈ 0.27·n stratum / 0.08·n general (REQ-MATH-014; replicated digit-exact our side; the entry states values without the operational definition — co-edit item); **L-A4 — new, seeded one-key (Merle)**: descent/no-new-cycle-in-structured-families, `q_P | R_0(P) ⟺ q_B | R_0(B)`, his 3,600/3,600 — clean-room re-derived and verified our side (12,888 checks, 0 failures; the identity `R_0(B^k) = R_0(B)·(q_P/q_B)` holds untuned, both signs — scope note in the findings); **L-A4/L-A2 kernel key on the structured half** (`08dc3d5`): `ContentDescent.lean` at `67c428a` — cocycle, `power_mult` (our multiplicative identity in `W0` form), `q_pow_factor`, `cycle_iff` both directions, `gcd_climb` (the L-A2 law, general form), kernel-3 with committed axioms log, statements matched clause-by-clause our side with `G_k` verified as our cofactor, re-confirmed clean-room (`experiments/merle_contentdescent_check.py`, 4,541 exact checks, 0 failures; read-not-built, no toolchain here); **L-A6 — seeded one-key (Merle)**: the calibrated lottery (`C = 1` census `n ≤ 14`, necklace budgets, realizability filter closed at 1) — check in flight, separate brief; **L-A7 — seeded one-key (Merle)**: the torsion ruler (counting constant × effective irrationality measure of `log₂ 3`; his μ source flagged for re-check) — check in flight, separate brief.

   **Standing decisions.** L-A1 credit: independent simultaneous discovery, both names. Hosting on collatz-lab.org: approved, DOI-pinned to v2 (10.5281/zenodo.21421120), the mirror-paper pair offered. Venue for the joint note: number-theory-shaped, with the formalization as supporting artifact. Citation posture: Gersonides 1342/43, not Mihailescu. Gateway visualization: each side builds its own cycle-side gateway and cross-links — ours is `viz/cycle_anchor_gateway.html`. His credit-deflection preference is on file with no record change (Remark 12.6.1.2 already reads packaging his / verification joint); the note's credit language at drafting time is the author's call.

   **Pending.** Round 8 received and verified (see `briefs/merle-round8-check-findings.md`; 12,888 checks, 0 failures). The co-edit round is **reviewed and merged** (main merge `55f23cb`; part A re-run at review, output identical, 49/49; `briefs/merle-round8-coedit-findings.md` is the record): part A `experiments/margin_asymptote.py` (asymptotic margin constants `c_gen = 0.0793186`, `c_strat = 0.2667875` at `α* = 0.3747344`, derived independently and matching; mod-7 mechanism table, with one finding — the solitary-7 verdict is resolution-dependent, mod 5 also crossing his significance bar at `N = 30,000`; reply material, his ledger/NOTE texts untouched); part B wiki homes (cycles.md Remarks 12.6.1.4 descent / 12.6.1.5 asymptote / 12.6.1.6 mod-7; aeh.md 13.6.7 two-equidistributions; itinerary.md Corollary 14.15.10.3 primitivity). **Key turns pushed, 2026-07-24:** mid-push the shared repo moved to `9f2dbcc` — **Merle seeded L-A5** (adelic content invariant `C(P) = log gcd(q, R_0)/log|q|` + a Lean kernel-verified separation lemma T1/T2, one key his, ours invited; our frame and letter constant credited with an anteriority sweep on record; pure-additive) — the three key-turn commits were re-seated on it (one add/add conflict at the L-A4/L-A5 boundary, resolved our-block-first; added lines verified identical to the reviewed versions) and pushed on the author's go-ahead: **shared HEAD now `740edb3`** (`cb765be` L-A4 two keys + two offers, `c40aa58` L3 accepted, `740edb3` L-A3 (B) pin + asymptote offer; archival patches at `briefs/merle-round8-coedit-patches/` regenerated against `9f2dbcc`). **Round-9 L-A5 verification: done** (branch `merle-la5-check`, `briefs/merle-la5-check-findings.md`; `experiments/merle_la5_check.py`, ~10,372 exact decisions, 0 failures). Invariant and separation lemma T1/T2 **confirmed** (clean-room derivation agrees with the Lean statements exactly; corollary and unit-seam isolation exact; artifacts `e297d9d`/`9932f3f` verified present, read-not-built — no toolchain; `#print axioms` output not committed, flat observation). **The gloss flag is CONFIRMED, now with the canary computed:** the `−17` cycle's primitive word has `C = 1` *exactly* (`gcd = |q| = 139`), is untuned (`K = 11`, `q < 0`), and is *totally isolated* — shared content `1` with every adjacent-transfer neighbour, forced by the corollary itself — so an aperiodic `C = 1` peak exists while being perfectly separated: T1/T2 prove adjacency separation only, and tuned-scoped the gloss restates the open condition `q | R₀`. Exact corrected co-edit language is in the findings (§(d)); `C` also undefined at `|q| = 1` (spent-stock words), minor co-edit item. **Our key must not turn on the gloss as written.** **L-A5 co-edit: PUSHED, 2026-07-25 (author go-ahead; shared HEAD `e53630f`)** (`briefs/merle-la5-coedit-findings.md` is the record): one commit `e53630f`, over `740edb3` (verified unmoved against the live remote; entry text verified unchanged), `LEDGER.md` only — the verification record (dated 2026-07-25, artifact pin `a87b94a` verified on public `main`), the `−17` exhibit stated flat, offer (a) the gloss restatement (findings' long form, acceptance his call), offer (b) the `|q| = 1` domain clause, and the honest key-status line (two keys upon his acceptance of a restatement, his own wording equally welcome; DRAFT until then); portable patch committed at `briefs/merle-la5-coedit-patches/0001-*.patch`. **Round-8 reply: DRAFTED, on file** (`briefs/merle-round8-reply-draft.md`, branch `merle-round8-reply`; every cited number and section verified at its named place, shared HEAD `e53630f` confirmed by read-only `ls-remote`) — business paragraphs only, the personal opening and closing the author's own; carries: the round-8 key turns and answers (L-A4 two keys with the two in-entry offers, L3 accepted into the two-key record, the door closure and the 13.6.7 disambiguation), the margin asymptote constants with the (B)-completion offer, the solitary-7 refinement, the L-A5 verification with the `−17` exhibit, offers (a)/(b), and the conditional key status, and the where-everything-lives map. **SENT** (established from Merle's round-9 letter of 2026-07-25, which answers its offers point by point: offers (a)/(b) accepted at `49351e5`, the (B) constants accepted, the solitary-7 correction acknowledged). **Round 9 (letter 2026-07-25) part 1 — L-A5 closure + kernel key: VERIFIED** (branch `merle-la5-closure`, `briefs/merle-la5-closure-findings.md`; `experiments/merle_contentdescent_check.py`, 4,541 exact checks, 0 failures): `49351e5` restatement **CLEAN — the two-keys marking is honest**; `905d75b` reconciled by the graph (axioms log added after our check, not missed); `08dc3d5`/`67c428a` ContentDescent statement-match exact; `6b9f2b1` accurate. L-A6/L-A7 checks done (below). **Round-10 L-A6 verification: done, reviewed and MERGED** (main merge `08b1547`, script re-run at review, output identical; `briefs/merle-la6-check-findings.md`; `experiments/merle_la6_check.py`, 424 recorded checks over 816,871 census words, 0 failures; shared HEAD re-verified `81431c7`, the expected pin). L-A6 (calibrated lottery / two-shore census, his commits `92a6edb` + `fb5e8fc`): census **exact-confirmed** (his 18 hits window-exact; completed our side to ALL `S` at `n ≤ 14` — 23 words, the qualitative claim unconditional), budgets and tail **model-replicated exact** (necklace λ 1.1175/2.6447 matching his 1.12/2.64; tranches identical at 4dp; residual `≈5·10⁻³` = the `n ≥ 61` cut, provenance not in the committed artifacts — flagged), realizability **upgraded to a proved lemma** (the ghost identity `3B(W) + q = 2^{σ₀}B(shift W)`, `B` always odd — formal hit IS a real cycle, exact), the calibration reading left at his own assessed grade. **Key recommendation: turn with offers** (domain/window clauses + all-`S` completion, the exact ghost lemma as the mechanism sentence, the residual cut pinned, minor wording) — the key turn itself is NOT made; main session reviews (re-runs the script), merges, then co-edit/key push gated on the author's go-ahead, then the reply draft. Repo state at this round's clone cross-checks consistent with the round-9 part-1 record above (L-A5 two keys via `49351e5`; ContentDescent kernel block `08dc3d5`; L-A7 seed at `81431c7`) — the L-A7 check (`briefs/merle-la7-mu-check-brief.md`) is the remaining in-flight item.

   **Round-9 L-A7 (torsion ruler) verification: done, reviewed and MERGED** (main merge `13ba557`, script re-run at review, output identical modulo a BOM; `briefs/merle-la7-mu-check-findings.md`; `experiments/merle_la7_check.py`, 0 failures). Algebra and numerics replicated digit-exact (`C₀ = −5.774` at `n = 2`, slack `14.483`, tail table all seven cuts, headline cut `N = 600` exact). **The μ source he flagged is adjudicated: misattributed** — `5.125` is Salikhov 2007 for `ln 3` (Dokl. Math. 76, 955–957; superseded even for `ln 3` by Wu–Wang 2014, `5.1163051`), not `log₂3`; the best citable effective ingredient for `log₂3` is Rhin 1987 (Prop. p. 160, exponent `13.3`; Simons–de Weger 2005 Lemma 12 the printed Collatz-side precedent), under which the same construction still closes with corrected headline "tail `< 5.2·10⁻⁴` beyond `n ≈ 2233`" (full sensitivity table in the findings; guaranteed two-log floor Gouillon 2006, crossing `~7.4·10⁸`). Key recommendation: **no turn as written; turn-with-offer** (re-source the ingredient, restate the headline scale, fix the `≈ 550` crossing sentence — not reproduced under either reading; two further ingredients flagged: the for-all-`n` margin inequality is verified-not-proved, and the best-cell→both-shore-mass step needs the south-side floor, unchecked in his artifact). Owed next per the standing pattern: main-session review (re-run the script), merge, then co-edit/key material gated on the author's go-ahead; reply material draws from the findings' §2 citations.

   **Round-9 co-edit: PREPARED LOCAL-ONLY, NOT PUSHED** (branch `merle-round9-coedit`; `briefs/merle-round9-coedit-findings.md` is the record, prepared blocks verbatim). One shared-repo commit **`641a530`** prepared over `81431c7` (HEAD verified unmoved: `ls-remote` + fresh clone) on a local branch of a scratchpad clone, `LEDGER.md` only, 26 insertions / 1 deletion (the sole deletion = the date-stamp appended to our own L-A5 Key-status line; his prose untouched everywhere, offers only): **L-A6** — Macindoe key turned, scoped (census exact-confirmed + the all-`S` completion to 23 words at `n ≤ 14`; the ghost identity stated as the proved lemma `3·B(W) + q = 2^{σ_0}·B(shift W)`; budgets/tail as replicated model computations `λ` 1.1175/2.6447; the calibration reading left at his assessed grade explicitly), offers (a)–(d) inside the entry (domain clauses + all-`S` closure, the exact lemma as the mechanism sentence, the residual `n ≥ 61` cut pinned, minor wording), Key status **two keys** scoped; **L-A7** — Macindoe verification record (replication digit-exact: `C₀ = −5.774`, slack `14.483`, all seven tail cuts, headline cut 600 exact; the μ re-check stated flat with the Salikhov 2007 / Wu–Wang 2014 / Rhin 1987 / Simons–de Weger 2005 citations and the corrected headline `n ≈ 2233`), the re-source offer with the two further ingredients named (for-all-`n` margin inequality; south floor `ε'_n`), Key status conditional — **two keys upon his acceptance of a re-sourcing**, DRAFT until then, his honest-scope paragraph untouched; **L-A5** — the satisfied DRAFT conditional date-stamped (`49351e5`, 2026-07-24) + the minor primitive-qualifier offer on the wall rider; **L-A4** — one statement-match line on the ContentDescent Lean key (`00f7bbc`, 4,541 checks). Patch at `briefs/merle-round9-coedit-patches/0001-*.patch`, verified applying clean on pristine `81431c7`, tree-identical to `641a530`. **PUSHED, 2026-07-25 (author go-ahead, given in-session): shared HEAD `641a530`** — remote verified unmoved at `81431c7` immediately before; wiki main pushed by the author first (public `main` = `3aa26a1`, all three artifact pins resolve). The reply draft's placeholder filled with the real SHA post-push. The round-9 reply draft is a parallel session's work, not claimed here.

   **Round-9 reply: DRAFTED, on file** (`briefs/merle-round9-reply-draft.md`, branch `merle-round9-reply`, base `69eebdd`; every number, SHA, and citation verified at its named place in the three round-9 findings files; the round-9 co-edit push SHA carried as an explicit `[PENDING: shared-repo push — SHA to be filled at send time]` placeholder — the co-edit is prepared in its own parallel session and its record is its own). Business paragraphs only, the personal opening/closing and anything answering his personal paragraphs the author's own. Carries: the thread closures acknowledged (`49351e5` claim-identical/CLEAN, `905d75b` reconciled by the graph with times attached), ContentDescent acknowledged as the kernel key on the structured half (read-not-built stated), the L-A6 key-turn report (census exact + all-`S` completion to 23 words, budgets/tail digit-exact, the ghost identity stated as the exact two-line lemma, calibration left at his own assessed grade, offers (a)–(d)), the L-A7 re-source finding with its remedy (`5.125` = Salikhov's `ln 3` measure, not `log₂3`; Rhin 1987 / Simons–de Weger chain; corrected headline `n ≈ 2233`, computed picture unchanged; replication digit-exact; the `≈ 550` crossing handed back as not reproduced (372/440); the for-all-`n` margin inequality and south floor named, with the margin-proof offer), the where-everything-lives map (wiki main pinned `69eebdd`), and a fenced `[OPTIONAL — the author's call, drop freely]` joint-note block. **Owed now:** sending only (the author's; personal paragraphs + the optional-block decision his); nothing else is owed either side once it is sent.

   **Round-10 Lean statement-match audit (DeficitLemma + T1Structure): done, NOT merged** (branch `merle-lean-r10-audit`, base `2374bfe`; `briefs/merle-lean-r10-audit-findings.md` is the record; `experiments/merle_lean_r10_audit.py`, 15,930 exact checks, 0 failures; read-not-built, trust boundary stated in the findings). Lean HEAD verified `5c9b663`, graph linear from `97b57d7`, no drift in the three previously audited files. **marginTarget: confirmed** — it encodes `margin(n) ≥ n/13` exactly at the tuned north cell (hypotheses pin `K = ⌈n·log₂3⌉` uniquely; `C(K−2,n−1)` is the L-A7 word count, Vandermonde-verified; gaps stated flat: constant `1/13` vs `c_gen`, tuned-cell coverage only). **convPairs: independently confirmed** = the 22 in-window convergent denominators of `log₂3` (CF at two precisions), criterion 5.17×/5.44× reproduced, fails at the next convergent; direction trace written out. **One substantive mismatch:** `ceiling_upper` proves the *upper half only* — the L-A8 block states both bounds; the lower `3^(p+1) < 2^K` is the unproved (elementary) `hceil` hypothesis threaded downstream — one-lemma repair, reply/co-edit material. Axiom logs: T1 exact (13/13, `discharge_all` `[propext]`, `convPairs_length` `[]`); DeficitLemma covers 8 of the stated 10 (`key_shifted`/`key15` unprobed); LegendreApprox absent from the log its header names. Retracted note: full at `7d46474`, superseded at `4856058` by a one-line reference; nothing at HEAD depends on the retracted theorem. Recommendation (no key turned): DeficitLemma statement-match key can turn as scoped; T1 turn-with-offer (`ceiling_lower` or restate). **Reviewed and MERGED** (main merge `30d0d89`; script re-run at review, output byte-identical).

   **Round 10 (Merle's triple reply, 2026-07-25/26) — received, all three verification checks done, reviewed and MERGED** (main merge `30d0d89`; each script re-run at review, output byte-identical to its committed one). Shared repo HEAD **`826970e`** (eleven commits over `641a530`, `LEDGER.md` only, +114/−1, all his); his Lean repo HEAD **`5c9b663`** (~20 commits over `97b57d7`). Letter contents: (i) **all four round-9 L-A7 offer clauses accepted** — the Rhin 1987 / Simons–de Weger 2005 re-sourcing, headline `n ≈ 2233`, `C₀ ≈ 2.06`, repair bits `< 1.94` — which satisfies our stated DRAFT condition, so **L-A7 stands at two keys**; the `≈ 550` crossing **withdrawn by him with its cause stated** (never computed — a head estimate written into the ledger as if machine-produced), re-derived to `372`/`440`, reproducing both our readings; (ii) a **volunteered** honesty item we did not raise (his `C₀` is exhibited, not proved for all `n`, so the entry is conditional on the margin inequality); (iii) both flagged ingredients discharged his side — south floor by the identity `ε + ε′ = 1` (no new citation), margin inequality **claimed PROVED at kernel** (`marginTarget`, 10 theorems, `DeficitLemma.lean`) via a Stirling-free rational-binomial route (`x = 12/7`, `19 = 12+7`), at proved constant `1/13` ≈ 3% below `c_gen`, with the entry's thresholds honestly recomputed (`1596→1655`, `1661→1722`); (iv) **L-A8 seeded, one key (his)** — T1, the no-hair theorem, 13 theorems ending in `discharge_all`, closing *no positive cycle with `x_min ≥ 2^71` and length `n ≤ 3.5032·10^10`*; (v) a **self-reported retraction** (`da2c8db`: stack overflow read as "0 errors", `sorryAx` at workable depth) withdrawn with cause and his check hardened to four tests; (vi) a **self-audit of his older Junction Theorem repo** (README overclaimed an unconditional no-cycles result; rewritten to match its own technical documents, `AUDIT_V9` in that repo — **recon done, see below: that repository is not publicly reachable, so the self-audit is neither confirmed nor denied**); (vii) **the joint note: yes**, framed as mapping the resistance and as honest about what it does not do, no schedule either side. Personal: he is a lycée electricity teacher, no doctorate, a year in — that letter is the author's to answer.

   **Round-10 L-A7 closure check: done, reviewed and MERGED** (branch `merle-la7-close-check`, base `2374bfe`; `briefs/merle-la7-close-check-findings.md`; `experiments/merle_la7_close_check.py`, 66 checks, 0 failures). **Every claim group MATCH**: crossings `372`/`440`; south floor `ε + ε′ = 1` with min `9.3·10⁻⁸` at `n = 190537` over `n ≤ 200,000`; margin slack `2.8414` at `n = 2`; entropy route min `1.6647` at `n = 16266`, max `2.10492` at `n = 190537`; `γ·log₂3 = c_gen` **EXACT with a symbolic derivation** (not merely 80 digits — an upgrade on his own "error 0.0 at fifty digits"); `c(12/7) = 0.0793165`, loss `2.07·10⁻⁶`; window `[5.727444, 5.747075]` with `s = 15, t = 86` the **unique** smallest admissible pair (`s = 1..14` exhausted in exact integers); atoms `0.088`/`0.327` bits; `key_core` exact on 1137 hub-admissible pairs; both his machine-caught errors confirmed; `n/13` slack `1.7003`; negative control 241 fails; thresholds `1596`/`1655`/`1661`/`1722` and the `2233` reconciliation reproduced. **L-A7 two-keys standing CONFIRMED.** Recommendation (no key turned): date-stamp the satisfied conditional, add a Macindoe verification record for the new blocks, three offers — the south floor and the `γ` identity **restated as theorems** (his numerics undersell them), the negative-control count pinned to its `x` (241 at `x = 1709/1000`, 256 at the entry's own `12/7`), and the "1.700 bits" pinned as the *route bound's* slack, not the true margin's; plus one flat line that `OUT_REQ-MATH-043` is committed without a generator script (all six numbers reproduce — an offer, not a doubt). Kernel-claim language gated on the Lean audit, as briefed.

   **Round-10 L-A8 / T1 clean-room check: done, reviewed and MERGED** (branch `merle-la8-t1-check`, base `2374bfe`; `briefs/merle-la8-t1-check-findings.md`; `experiments/merle_la8_t1_check.py`, 62 checks, 0 failures). **Every link derived in our own words and CONFIRMED**: product identity exact on all four real cycles both shores; survivor/ceiling equivalence; seam bound; log gap; `δ = 4.0734·10⁻²²` with his factor-2 correction confirmed exactly (the withdrawn `4.955·10^10` is precisely the missing-factor-2 artifact); 22 in-window convergents; discharge tightest `5.17×` (exact test `5.44×`, integer form conservative by design); non-vacuity canary failing at the next convergent; each real cycle exiting scope exactly where the hypotheses say. **Both pinned discrepancies resolved:** `3.5035e10` vs `3.5032e10` are the *exact* and *integral* windows (`35035491004` / `35031771147`) — both right, the definition shifted mid-entry without a naming clause; the `q₂₁` collision is an indexing slip (true labels `q₂₂ = 65470613321`, `q₂₃ = 137528045312`; `q₂₁ = 6586818670`), substance right in both stacks. **Hercher adjudicated with citation:** his bound is `K > 1.375·10^11` (Cor. 29, conditional on `X₀ ≥ 3·2^69`, met by Barina `2^71`), the underlying threshold exactly `q₂₃` — so the frame-prediction point (thresholds live on the convergent grid) **is genuinely supported**, but the scope comparison is *not* apples-to-apples and the wording needs restating. Also flagged flat: REQ-055/056 outputs committed without scripts, OUT-052/053 carry crashed-run tracebacks and two mutually shifted index conventions, and "exhaustively to `q₁₀ = 190537`" matches neither the committed sweep bound nor its own indexing (we re-verified the property to `n < 190537`, so the fact stands). Recommendation (no key turned): **turn-with-scope** — the mathematics of every link turns; all kernel claims deferred to the Lean audit; offers = the window clause, the indexing convention + two subscript corrections, the Hercher restatement, our clean-room derivations of both named glue facts (including the tightening that in-window `n` is a priori a *multiple* of a convergent denominator, closed by the same 22 checks).

   **Round-10 Junction repo recon: done, reviewed and MERGED** (main merge `ece0162`, units script re-run at review, output identical; branch `junction-repo-recon`, base `9d22dc8` verified against main before merge; `briefs/junction-repo-recon-findings.md` is the record; `experiments/junction_deficit_units_check.py` + its output). **The repository his letter describes is not publicly reachable**, so his self-audit could not be confirmed or denied: his handle has exactly three public repositories (`collatz-conditional-cycles` HEAD `c24633c`, `one-obstruction-three-faces-lean` HEAD `5c9b663`, `paper-trading-dashboard`), no organizations, and a public event stream touching only those plus our shared repo; **no `AUDIT_V9`, no `STATUS.md`, no `*Legendre*` file in any ref** (10 branches, 7 tags) of `collatz-conditional-cycles`; code search finds no `AUDIT_V9` under his ownership. Every described point is therefore recorded **NOT FOUND (repository inaccessible)** — nothing found contradicts his account and nothing confirms it. His only other public Collatz repo, `collatz-conditional-cycles`, is recorded flat as **not** the described one: headed "conditional non-existence" since its first commit, 0 `axiom` declarations, 0 `sorry`, Simons–de Weger as a *structure parameter* (`ProductBoundThreshold`) not an axiom, `native_decide`'s compiler-trust axioms already flagged in `expected_axioms.md`, and no `k = 200` / `k = 50000` / two-asymptotic-programs counterpart (its stated horizon is `k < q₁₄ ≈ 10,590,737`); the one described number with a visible counterpart there is the Simons–de Weger `m ≥ 69` ingredient. Deficit-lemma provenance recorded at second hand from his own committed `DeficitLemma.lean` header ("Junction Theorem preprint 2026, §3" — the header dates it 2026) and his REQ-MATH-037 script/output; **flag 6 of the L-A7 closure findings is SETTLED — the preprint's `S` is our `K`**, from the committed artifacts alone (the inequality does not discriminate, `S = K−n` being strictly weaker and also holding; the *units* do: `γ·log₂3 = c_gen` exactly vs `γ·(log₂3−1) = 0.02928`). **`LegendreApprox.lean`: home NOT CONFIRMED** (absent from every ref of `collatz-conditional-cycles`; provenance rests on his `da2c8db` commit message and the shared LEDGER, neither naming a URL), byte-identical since `da2c8db` in his Lean repo, 0 `sorry`/`native_decide`/`axiom` by read — **diff verdict: NOT PERFORMED**, no counterpart exists. No interaction of any kind with his repositories (no fork, issue, comment, star, watch). If confirming the self-audit matters to the reply, the repository's location is the thing to ask for — plainly and without implication: **absence of a public copy is not evidence against his account**, a set-aside preprint repo may simply be private or unpushed, and the reply must not imply otherwise. One merge-time correction on record (`5611699`): the findings' spring-2025-vs-2026 dating flag is **withdrawn as our error, not his** — his letter says only "last spring", which from 2026-07-26 is spring 2026 and matches his `DeficitLemma.lean` header; the "2025" was a main-session inference written into the brief. The delegate flagged and deferred it correctly, which is why it was caught before it could reach a reply.

   **Round-11 Junction public recon: done, reviewed and MERGED** (main merge `185d44b`) (branch `junction-public-recon`, base `e938040`; `briefs/junction-public-recon-findings.md` is the record; `experiments/junction_public_recon_deficit_check.py` + committed output, 465 checks, 0 failures). **This supersedes the round-10 recon paragraph above on reachability and on every NOT FOUND verdict in it** — its posture sentence (absence of a public copy is not evidence against his account) stands as written and is not revisited. He made four repositories public (his handle now shows 7): `Collatz-Junction-Theorem` HEAD **`a57d29e`** (2026-04-22, 232 commits, 3 branches, MIT + CC-BY-4.0 `LICENSE-PAPER`), `collatz-cycles-lean` HEAD **`1d77168`**, `collatz-nocycle-lean4` HEAD **`4ec239d`**, `collatz-audit-2026` HEAD **`eb237b3`**. **Licence account checks out in every particular**, including which repo had none — `collatz-audit-2026`, MIT added at the flip (`eb237b3`, 2026-07-26, *"Add MIT licence before making the repository public"*), the only commit anywhere in the four later than 2026-04-25; "last pushed 22 April" for the Junction repo is correct. **Most described points CONFIRMED, several verbatim, but distributed across two repositories rather than one**: the unconditional README overclaim and its rewrite are in `collatz-cycles-lean` (`93c64ee` *"For every k ≥ 1 … admits no nontrivial positive cycle"* → `1a56828` *"reduce claim to what is actually proved"*); the two asymptotic programs with named gaps beyond `k = 200` are `docs/PROOF_ASSEMBLY.md` §2; the `k ≥ 69` extra-hypothesis clause is the Junction preprint's `rem:junction-scope`; the scope banners go further than described; the plain-words "does not prove the Collatz conjecture" is in `collatz-nocycle-lean4` only; and **Simons–de Weger is a genuine `axiom` declaration** (the round-10 NOT VERIFIABLE, now quoted). **Three did not**: no `AUDIT_V9` in any ref of any of the four (the public series stops at V8, 2026-03-07, verdict *"l'abstract suraffirme"*); no `STATUS.md` (the rewritten status page is `VERIFICATION.md`, which marks its own headline module INVALID by name); and `k > 50000` is marked **PROVED**, not OPEN — `PROOF_ASSEMBLY.md` §10.6 still closes *"No gap remains. The proof is unconditional for all k ≥ 3"* about the Range Exclusion module the rest of the repo marks invalid, that one file having been missed by the rewrite. **Timing recorded flat and left unadjudicated**: every confirmed remediation is dated March–April 2026, so the work is there and older than the description; the April 2026 adversarial audit apparatus is fully committed in `collatz-nocycle-lean4/docs/BIBLE/`. `Projet_Collatz` untouched, unrequested, unreached. **`LegendreApprox.lean`: home CONFIRMED and the diff PERFORMED** (round 10: NOT PERFORMED) — the Junction copy at `lean/skeleton/` is upstream by five months (`09f481b`, 2026-02-26, blob `a4fae1f`, byte-identical to the `collatz-cycles-lean` copy and unchanged to HEAD); against the T1-chain copy (blob `b55095a`, unchanged at `da2c8db`/`5c9b663`/current `c991430`) it is **not byte-identical but the difference is immaterial** — same 3,175 bytes, the whole diff a two-line reordering of `open Real` relative to the namespace line, no statement/hypothesis/tactic differing, nothing in T1's kernel claims turning on it; 0 `sorry`/`native_decide`/`axiom` by read; build contexts differ (v4.29.0-rc2 vs v4.27.0) without bearing on T1, which compiles its own copy. **Flag 6 CONFIRMED at first hand and on the definition**: the preprint *defines* `S = S(k) = ⌈k log₂3⌉`, i.e. our `K`, so no correction to the round-10 units settlement; two flat differences newly visible — his printed statement carries `− ε(k)`, `ε = O(log k)` (dropped in his REQ-MATH-037 transcription; read without it the inequality fails at 8 sampled `k`, worst `−4.485` at `k = 306`, all at convergents), and his `C = binom(S−1,k−1)` against our `binom(K−2,n−1)`, larger by `→ 1.43803` bits. The preprint's own version is exhibited on `k ∈ [18,500]` plus an asymptotic argument with `c` and `ε(k)` never made explicit — the idea and the constant, not an effective inequality. **Prior art, for a joint note, no priority contest**: his Range Exclusion is our uniform-trim geometry in other coordinates (his `3^(−0.415k)` is **not** our `1.585^(−p)` — different constants, different counters); no counterpart anywhere to the staircase family (the word *escalier* occurs for a different object); and `PROOF_ASSEMBLY.md` §10.5, dated 17 March 2026, already confines the dangerous `k` to convergent denominators of `log₂3` — the L-A8/T1 frame-prediction point, four months earlier, on his side. **Nothing found would change a claim of ours.** No interaction of any kind occurred (read-only clones and public API reads only; no fork, issue, PR, comment, star, watch, follow or push). Owed: main-session review (re-run the script), merge; nothing here is a key turn, ledger text or reply paragraph.

   **The margin inequality — PROVED our side, at the true constant `c_gen`** (branch `margin-inequality-proof`, base `b860fe8`; record `briefs/margin-inequality-proof-findings.md`; `experiments/margin_inequality_proof_check.py` + committed output, written from scratch, every link of the chain checked separately with negative controls). This discharges the offer made in the round-9 reply — the ingredient round 9 flagged as *verified-not-proved*. **Theorem A:** for all `n ≥ 1`, at the tuned north cell `K = ⌈n·log₂3⌉`, `margin(n) = K − log₂C(K−2,n−1) > c_gen·n + 1 + log₂(log₂3)` — i.e. `≥ c_gen·n` with a uniform surplus above `1.664` bits, at `c_gen = β(1 − h(1/β))` **exactly**, not a rational `3%` below it. The proof is elementary and cites nothing: the textbook entropy bound `C(m,k) ≤ 2^{m·h(k/m)}`, plus the tangent-line bound from concavity of `h` taken **at `p₀ = 1/β`** — the point at which the linear-in-`n` term cancels *identically* against `c_gen` (that cancellation is the whole content; at any other tangent point the slope is strictly below `c_gen` and the bound drifts negative, verified as a negative control). Contrary to the brief's expectation, **the Stirling term is not needed**: the crude entropy route already closes with a positive constant, because concavity supplies the perturbation bound with no remainder term. Robbins is nonetheless carried out as the refinement (**Theorem B′**, `n ≥ 2`, Robbins cited): `margin(n) > c_gen·n + (1/2)log₂n + 2.1217…`, the Stirling factor confirmed to be a credit, not a debt. **Scope is wider than asked:** Theorem A′ covers every cell with `n+2 ≤ K ≤ nβ + 4.7998`, i.e. the tuned north cell, the **entire south shore**, and the next three or four north cells — closing the south-shore half of the coverage gap the round-10 audit recorded against his `marginTarget` (far-north cells beyond that window remain open; there the same chain gives only `margin > 2`). Cross-checks: min slack `2.841362774` at `n = 2` reproduced; **one flat correction offered** — the ledger's crude-route figures (`1.6647` at `n = 16266`, `2.10492` at `n = 190537`) come from taking `c_gen` as the 7-digit decimal `0.0793186`; under the exact constant the figures are `1.664453` at `n = 111202` and `2.102482` at `n = 190537`, and the true minimum is what matches the proved floor `1 + log₂β = 1.6644487`. Nothing here is a key turn or ledger text: **owed next** is main-session review (re-run the script), merge, then — gated on the author's go-ahead — the co-edit offering this to L-A7 as the second, entropic proof at the true constant, and the reply paragraph.

   **Round-10 reply: DRAFTED, on file** (`briefs/merle-round10-reply-draft.md`, branch `merle-round10-reply`, base `d9b2715`; the worktree was cut at the stale `b860fe8` and rebased onto `d9b2715` before drafting). Business paragraphs only across all three of his letters; the personal opening/closing and anything answering his personal paragraphs are the author's own, marked as placeholders. Every number, SHA, section reference and citation verified at its named place in the five round-10 findings files, with the load-bearing arithmetic independently recomputed in-session (constants `c_gen`, `1 + log₂β`, `2log₂β − Λ`, `γ`, `x*`, the `t/s` window, the CF of `log₂3` and `q₀…q₂₄`, both Legendre windows, the discharge criterion, the `−17` product identity, the crude-route extrema under both constants, the Corollary-B′ table) — all agreed. Carries: L-A7 closed at two keys with the four new blocks verified (66 checks, 0 failures) and the two theorem-not-numerics upgrades (`ε + ε′ = 1`; `γ·β = c_gen` symbolic) plus the two scope clauses and the OUT-043 script line; **our margin proof as the centrepiece** (Theorem A at the true `c_gen` for all `n ≥ 1`, B′, A′ closing the south-shore half of the `marginTarget` coverage gap) with **our own wrong Stirling premise stated plainly as ours** — a constant surplus is not a shortage, concavity needs no remainder, his rational-`x` route credited as a genuinely independent second proof built on the strength of our error — plus the closed-form `[1.66444871, 2.10248137]` upgrade and the 7-digit-decimal correction; L-A8 key turned on the mathematics with kernel claims scoped to the read-not-built audit, both discrepancies resolved without fault (exact vs integral window; the `q₂₁` indexing convention), the multiples tightening, and the Hercher restatement (label `q₂₃`, printed-vs-underlying threshold, Cor. 29's `X₀ ≥ 3·2^69`) — **note, against the brief: the findings hold the "3.9× further" comparison IS apples-to-apples, the asymmetry running in Hercher's favour; the draft follows the findings and flags the divergence in its header**; the `ceiling_upper` upper-half-only mismatch as a one-lemma repair or restatement, his choice, with the axiom-log hygiene named as hygiene; the Junction repository **asked for, plainly, with no implication** (recorded NOT FOUND, absence not evidence; the `S`-is-`K` settlement noted so the ask stays small; `LegendreApprox.lean`'s unconfirmed home as the one clause that bears on T1); the retraction acknowledged once, factually, no moral; the joint note agreed with his framing and no outline proposed; and the where-everything-lives map. Two bracketed fields: the co-edit push SHA as `[PENDING: shared-repo push — SHA to be filled at send time]` (a **parallel session's** work, not claimed here) and the wiki-`main` pin `d9b2715` marked CHECK AT SEND TIME (must be public first or the artifact pins do not resolve). **One correction to our own record, found while verifying:** `briefs/merle-lean-r10-audit-findings.md` item (iv) gives the integral window as `⌊√(2079·2^71/4000)⌋ = 35 031 770 966`; the correct value is **35 031 771 147** (the figure `briefs/merle-la8-t1-check-findings.md` §(d) carries, recomputed twice this session). Nothing depends on it — the 22-convergent count is identical under either figure and under the exact window — and the draft carries the correct one. **Owed now:** author review of the draft, then sending (his; personal paragraphs and the bracketed fields his).

   **Round-10 co-edit: PREPARED LOCAL-ONLY, NOT PUSHED** (branch `merle-round10-coedit`, base `d9b2715` — the worktree was cut at the stale `b860fe8` and was rebased onto current `main` first; `briefs/merle-round10-coedit-findings.md` is the record, with every prepared block verbatim). Shared HEAD verified **`826970e`** and unmoved at every check (`ls-remote` plus a fresh clone, three times — at drafting and after each of the two review changes). One shared-repo commit **`5481d2d`** prepared over `826970e` on a local branch of a scratchpad clone, `LEDGER.md` only, **49 insertions / 2 deletions** — both "deletions" are pure-append modifications of a single line: our own round-9 L-A7 Key-status paragraph (date-stamp appended) and the file's final line, which gains only its newline terminator (`826970e` ends without one); his prose is untouched everywhere, corrections offered inside the entries. **L-A7** — the round-9 DRAFT conditional date-stamped as met (2026-07-25, the re-sourcing accepted in full), a Macindoe verification record for the four new blocks replicated digit-exact (66/66: crossings 372/440; `ε + ε′ = 1` min `9.306e−8` at `n = 190537`; margin slack `2.84136` at `n = 2`; entropy route `1.66469`@16266 / `2.10492`@190537; `γ·log₂3 − c_gen = 3.95e−82`; window `[5.7274437, 5.7470751]` with `s = 15, t = 86` the unique smallest pair; atoms `0.0883`/`0.3267`; `key_core` on all 1,137 hub-admissible pairs; `n/13` route slack `1.7003` at `n = 12`; control 241; all six OUT-043 numbers; the 1596/1655/1661/1722-vs-2233 reconciliation pinned), four offers (south floor + `γ` identity restated as **theorems**, the control's `x` pinned at 241/256, the "1.700 bits" pinned as the route bound's, OUT-043's missing generator script), **our own margin proof offered as the second proof at the true `c_gen`** (Theorem A all `n ≥ 1` with uniform surplus `1 + log₂β`, elementary and citing nothing; Theorem B′ the Robbins refinement; Theorem A′ covering the tuned cell, the entire south shore and the next three or four north cells — with **our wrong Stirling premise stated plainly**, his route credited as genuinely independent and the rational-`x` device as his own, the `[1.66, 2.10]` interval given in closed form, and the one flat correction that the crude-route figures come from `c_gen` as the 7-digit decimal), a read-not-built `marginTarget` statement-match (encoding exact at the tuned cell, hypotheses load-bearing, axiom log 8 of 10, stale SCOPE header), and **Key status: two keys, scope stated** — the margin step now proved on both sides, kernel claims explicitly outside the key. **L-A8** — **Macindoe key turned, scoped: two keys** on the mathematics of every link (62/62 clean-room: product identity exact on all four real cycles both shores, survivor/ceiling, seam, log gap `δ = 4.07336e−22` with the factor-2 correction confirmed exactly, 22 in-window convergents, discharge `5.1713×`/`5.4433×`, the non-vacuity canary, each real cycle exiting scope where the hypotheses say), with **every kernel claim explicitly deferred** to the read-not-built T1 statement-match (thirteen declarations matched, `convPairs` independently confirmed as the first named glue fact, axiom log one-for-one, retraction where the block says); five offers (exact-vs-integral window `35035491004`/`35031771147`; the indexing convention with `q₂₂`/`q₂₃` corrected; the Hercher sentence restated with Cor. 29 and its `X₀ ≥ 3·2⁶⁹` condition — the frame-prediction point genuinely supported, and the comparison **fair on both axes** with the one asymmetry running in *Hercher's* favour on both hypothesis and conclusion, so "3.9× further, but on paper" understates his advantage; both named glue facts derived, including the multiple-of-a-convergent tightening; flat hygiene notes); and the one substantive mismatch, `ceiling_upper` proving the upper half only, offered with its one-lemma repair (`ceiling_lower`) or a restatement, his choice. Nothing about the Junction repository is in this commit (it is reply material). Patch at `briefs/merle-round10-coedit-patches/0001-*.patch`, verified applying clean on a pristine `826970e` and tree-identical to `5481d2d` (tree `bcae6b6`), re-verified after each review change. **Reviewed, with two changes applied at review and the commit regenerated** (`d050e2e` → `434de9a` → `0cb155b` → `5481d2d`, counts unchanged throughout; the last step corrected the **commit message**, which had paraphrased offer (c) with the same wrong clause and which enters the shared repo's history): (i) the L-A8 key-turn block now pins its own artifact `cde2e5b`, so every record in the commit pins one — main session's call, consistency inside one commit outweighing the one-artifact-per-record convention; (ii) offer (c)'s "the scope comparison is not apples-to-apples" clause **corrected** — the label was wrong and **came from the brief**, which had `briefs/merle-la8-t1-check-findings.md`'s Hercher adjudication item 3 backwards; that record is the authority and says the comparison IS apples-to-apples on both axes (his `K` counts odd members, the same convention as T1's `n = p+1`), with the one asymmetry in Hercher's favour on both hypothesis (`3·2⁶⁹` needed against `2⁷¹`) and conclusion (`1.375·10¹¹` against `3.5032·10¹⁰`, ratio `3.9258`). Recorded with its origin named, as the Junction recon's spring-2025 dating slip was (`5611699`): where a brief contradicts a findings file, the findings file wins. **PRE-PUSH CONDITION, the round-9 pattern exactly: all five artifact pins — `8e385b9`, `7cb47cb`, `bb9e5a7` (twice), `cde2e5b`, four distinct commits — fail to resolve on public wiki `main`, which is still at `b860fe8`; the author must push wiki main to `5c1faf4` or later (`d9b2715` current) before or with the shared-repo push.** Separately noted, not part of this commit: `briefs/merle-lean-r10-audit-findings.md` item (iv) gives the integral window as `35 031 770 966`; the correct value is `35 031 771 147` (carried by the la8 findings, this record and the prepared block), being repaired on `main` by the main session — **done, `0816878`**.

   **Round-10 co-edit: PUSHED, 2026-07-26 (author go-ahead) — shared HEAD now `c966875`.** Wiki `main` pushed publicly by the author first (`ae402b9`), and all four artifact commits (`8e385b9`, `7cb47cb`, `bb9e5a7`, `cde2e5b`) verified to resolve on public `main` *before* the push went out. The shared remote was verified unmoved at `826970e` immediately before pushing — he had made no further commits, so no conflict arose and nothing of his needed re-seating. Result confirmed by an independent fresh clone: HEAD `c966875`, tree `bcae6b6`, `LEDGER.md` only, 49 insertions / 2 deletions, his prose untouched. **Note on the SHA:** the prepared commit was `5481d2d` in the preparing session's clone; applying the patch fresh restamps it, so **`c966875` is the SHA to cite** — the tree is identical and was checked as such. The reply draft's two bracketed fields are filled with the real SHAs.

   **Owed next (round 11).** The three verification windows are **reviewed and MERGED** — the ceiling re-audit (`3aeecbc`), the Junction public recon (`185d44b`), the hygiene check and the four negatives (`185c622`); each script re-run at review with byte-identical output, each base verified `e938040`, each delegate having rebased off the stale `2225b68` worktree cut itself. One review correction applied on the hygiene branch and named there: the drift/`δ` correction factor was stated backwards in that findings file's closing recommendation (`D(x_min) = δ·(1 + 1/(27x_min²))`, not the reverse — `δ` sits strictly below the true drift), the body having it right in all three places; re-verified at merge in 140-digit arithmetic, and note that a 60-digit check is *not* enough to see it. Still to come, all gated on the author: **window D**, the joint-note premise pre-check (facts only — whether "Macindoe's counting dichotomy" names an already-published result of ours as his proposed contribution sentence assumes, and the `ccchallenge.org` claims); then the **co-edit** (his own proposed L-A8 wording, the L-A8 kernel key now keyable in the ContentDescent language, the one-sidedness sharpening, the artifact notes) and the **round-11 reply**. The joint note's one-sentence contribution is the author's own to write — Merle asked him directly, and asked it as the thing that decides whether the note should exist.

   **Round-11 reply: DRAFTED, on file** (`briefs/merle-round11-reply-draft.md`, branch `merle-round11-reply`, base `9fdaa0f` — the worktree was cut at the stale `2225b68` and was rebranched onto the brief commit before anything was written). Business paragraphs only across both of his 2026-07-27 letters; the personal opening and closing and anything answering his personal paragraphs (his training, the retraction, the "not a debt" symmetry, his own two false alarms in one week) are the author's own, marked as placeholders and **neither drafted nor glossed**. **Both absolute exclusions honoured:** no contribution sentence for the joint note and no wording proposed for it anywhere in any form — the draft carries the premise-check facts and stops at a marked placeholder, because he asked the author directly and framed it as the thing that decides whether the note should exist — and no outline, spine, section list or genre. Every number, SHA, section reference and citation verified at its named place in the five round-11 findings files, with the load-bearing arithmetic independently recomputed in-session at 120–200 digits (the CF of `log₂3` and `q₀…q₂₅` on the pinned convention, the sides of `q₂₂`/`q₂₃`, `δ`, both windows, the discharge at all 22 convergents, `θ₂₂/(q₂₂·δ_old)`, the seam identity on all four real cycles both shores, the `−17` drift sums, `D(x_min)/δ − 1` against `1/(27x_min²)`, `D(1)`/`D(3)` against `2 − log₂3`, `γ`, `c_gen`, `1 + log₂β`, `S(k) = ⌈k log₂3⌉` over `k = 1..400`, the deficit inequality read without `ε(k)`, and `704/1536/2048`) — all agreed. Carries: the ceiling repair **verified rather than taken** (four signatures verbatim before and after, `hceil` gone by every route including the hoisting trap, `ceiling_lower` unconditional in pure ℕ, axiom logs 13 → 15 exact, DeficitLemma 10 of 10, `sorryAx` absent, the RETRACTED block standalone, 190 checks / 0 failures, **no handbacks**); the **one-sidedness sharpening as ours** (`q₂₂` below `log₂3`, so the first north-shore scale is `q₂₃ = 137,528,045,312` — his frame-prediction point landing harder than he claimed it); the repositories thanked for in his own terms and the recon reported — what confirmed, then the three that did not, **flat and kind, with the round-10 posture carried over explicitly** (absence of a public copy was never evidence against his account and is not now) and the V8 audit's likeness to his description written **as a resemblance, never as an identification**; `LegendreApprox.lean`'s home confirmed and the diff performed; flag 6 upgraded to the definition with the dropped `− ε(k)` and the binomial index gap; **his prior art volunteered by us** (`PROOF_ASSEMBLY.md` §10.5, 17 March 2026, with no priority language in either direction, plus the two flat comparisons so Range Exclusion and `3^(−0.415k)` are never conflated with our trim and `1.585^(−p)`); the hygiene pass with **one sentence, no ceremony, that `5.17× at j = 21` is correct and our query was wrong**, the `053` precision as a compliment to his red team, and the `OUT-052` `(d-bis)` deletion as the one item with a live consequence; and the four negatives with the **theorem hand-back**. **Drift direction verified character by character:** `D(x_min) = δ·(1 + 1/(27·x_min²))`, `δ` strictly *below* the true drift, relative excess `6.643193·10⁻⁴⁵` at `2⁷¹` agreeing digit for digit with `1/(27x_min²)`. **One correction to our own record, flagged in the draft header and not sent:** `briefs/junction-public-recon-findings.md` §4.3(b) prints the limiting binomial gap as `1.43803` bits; `log₂(log₂3/(log₂3−1)) = 1.43803265928…` at 60 digits, and that file's own two finite checkpoints (`1.34792` at `k = 18`, `1.43752` at `k = 2000`) reproduce exactly and converge there — ours, named as ours, in the round-10 `35 031 770 966` pattern. Two bracketed fields: the co-edit push SHA as `[PENDING: shared-repo push — SHA to be filled at send time]` (a **parallel session's** work, not claimed here) and the wiki-`main` pin `9fdaa0f` marked **CHECK AT SEND TIME** (public `main` must lead or none of the co-edit's artifact pins resolve). Nothing sent, nothing pushed, no interaction with his repositories; `experiments/encoding_scan.py` clean. **Owed now:** main-session review and merge, then — the author's alone — the personal paragraphs, the contribution sentence, the two bracketed fields, and the sending.
   **Round-11 co-edit: PREPARED LOCAL-ONLY, NOT PUSHED** (branch `merle-round11-coedit`, base `9fdaa0f` — the worktree was cut at the stale `2225b68` and was moved onto current `main` first; `briefs/merle-round11-coedit-findings.md` is the record, with every prepared block verbatim). Shared HEAD verified **`c966875`** and unmoved at both checks (`ls-remote` plus a fresh clone at the start, `ls-remote` again immediately before finishing). One shared-repo commit **`2f724d1`** (tree **`5c0ccbf`**) prepared over `c966875` on a local branch of a scratchpad clone, `LEDGER.md` only, **44 insertions / 1 deletion** — the sole deletion is a pure-append modification of our **own** L-A3 (B) Key-status line (verified mechanically: the old line is an exact prefix of the new, 954 → 1,318 characters); his prose is untouched everywhere, corrections offered inside the entries. **L-A8** — **the ceiling-pinned entry written in Merle's own proposed wording** (his proposal after he deliberately left `LEDGER.md` untouched rather than restate his own claim; `ceiling_pinned` cited with `ceiling_lower`, the round-10 finding and the round-11 repair both named factually and without moral, and his "your key belongs wherever you judge it belongs" honoured by keeping ours in a separate block); **the Macindoe kernel key turned, scoped in the ContentDescent language with read-not-built in the same sentence** — statement match (fifteen declarations at `c991430`), dependency structure (the `hceil` removal **verified rather than taken**: not renamed, not weakened, not a structure field, **not hoisted**, the file declaring no `variable`/`include`/`omit`/`section` in 482 lines, re-derived at two located call sites), committed axiom logs (T1 **13 → 15 exact**, DeficitLemma 10 of 10, `sorryAx` absent), **190 exact checks / 0 failures**, and our own derivation of the repair — with the **two continued-fraction glue facts** (independently confirmed, *not* kernel-proved) and **`LegendreApprox.lean`'s absent axiom-log entry** explicitly outside the key, and **no claim of end-to-end machine checking made or implied** anywhere; the hygiene verification record (115 checks, 0 failures; all five scripts run and reproducing their committed outputs; **`5.17× at j = 21` confirmed CORRECT — our query was wrong and the record says so**; the `053` monotonicity argument exhibited as yielding `≥ 22` not `= 22`, the equality a separate computation at a margin of `2.0039×`, which he ran); the one-sidedness sharpening offered as a gift (the sentence it tightens is **ours**, not his — signed figures recomputed here at two precisions because they were brief-only: `p₂₂/q₂₂ − log₂3 = −1.016341·10⁻²²`, `p₂₃/q₂₃ − log₂3 = +9.427058·10⁻²⁴`, first north-shore scale `q₂₃ = 137528045312` = Hercher's underlying threshold); the **theorem hand-back** (`Σᵢ log₂(1 ± 1/(3xᵢ)) = K − n·log₂3` exactly on all four real cycles both shores, with the two flat corrections — "exactly `n·δ`" is a sharp bound not an identity, and `D(x_min) = δ·(1 + 1/(27·x_min²))` so **`δ` sits strictly BELOW the true per-step drift at the minimum**, the direction checked character by character against the hygiene findings' three correct places — plus the `x = 1` corollary handed back as his); and **seven offers**, all minor, all hygiene (axiom-log headers/raw output, probes for the two unprobed lemmas, a `LegendreApprox` probe, the `1.700 bits` pin still open from round 10, the two surviving `q₂₁` and the pre-054 `δ` in the Lean comments, Cor. 29's `X₀ ≥ 3·2⁶⁹` recorded as promised-not-landed, and — **marked as different in kind** — the deleted `OUT-052` `(d-bis)` section whose `median 15601` and `14936 = 22·665 + 306` this ledger's own L-A8 seed block cites, with the remedy he himself used for 043/055/056). **L-A3 (B)** — our own conditional date-stamped as met (2026-07-25, his round-9 letter), bookkeeping only. Patch at `briefs/merle-round11-coedit-patches/0001-*.patch`, verified applying clean on a **pristine** `c966875` and tree-identical (`5c0ccbf`, and the applied `LEDGER.md` byte-identical at 114,762 bytes; `git am` restamps the SHA to `b6aaa67`, so the **tree** is the object to check). **Deliberate abstention, recorded: no ledger entry was created for his δ8 impossibility** — Face I's Merle half, which has no entry at all and which `NOTE.md`'s own rule says must enter via the ledger; **that is his to seed**, and the observation is reply material, as are L1's `corrected` status word and the absence of L-A5…L-A8 from `NOTE.md`. **PRE-PUSH CONDITION, the round-9 and round-10 pattern exactly: both artifact pins — `dc8fde6` (ceiling audit) and `38ccc49` (hygiene check), four citations — fail to resolve on public wiki `main`, which is still at `2225b68`; the author must push wiki `main` to `185c622` or later (`9fdaa0f` current) before or with the shared-repo push, or the pins dangle.** Nothing about the Junction repositories or the joint note is in this commit. Owed next: main-session review (re-verify the patch and the counts), merge, then push gated on the author's go-ahead.

   *(Round 10, closed.)* **Owed next (round 10) — SENDING ONLY, and it is the author's.** Everything else is done: the three verification checks, our margin proof, the Junction recon, the co-edit (prepared, reviewed and now pushed) and the reply draft are all reviewed and merged. What remains is the author's alone — the reply's **personal opening and closing**, anything answering Merle's personal paragraphs, and the sending itself. Nothing is owed on either side once it is sent. The joint note remains the next arc, to be planned once L-A8 has settled; **his letter also leaves two things open for whenever he next writes** — where the Junction repository lives, and his choice between the `ceiling_lower` lemma and a restatement.

   **Round-11 Lean re-audit — the ceiling repair: done, reviewed and MERGED** (main merge `3aeecbc`) (branch `merle-r11-ceiling-audit`, base `e938040`; `briefs/merle-r11-ceiling-audit-findings.md` is the record; `experiments/merle_r11_ceiling_audit.py` + committed output, **190 exact checks, 0 failures**, fresh code importing nothing of his and nothing from the round-10 scripts; read-not-built, trust boundary stated in the findings). Lean HEAD verified **`c991430`** over `6c084c5` over `5c9b663`, linear, both commits his, 2026-07-26; **no drift** in `ContentDescent.lean`, `ContentSeparation.lean`, `TransportRecurrence.lean` or `LegendreApprox.lean` (the last recorded explicitly — it bears on the Junction recon). **The round-10 `ceiling_upper` mismatch is CLOSED, and closed further than we offered:** `ceiling_lower` is a kernel theorem in pure ℕ, **unconditional** — no `hpX`, no `2^71`, no window, no real-number hypothesis — with `∏x > 0` *proved* in-file from `0 < X ≤ xᵢ`, not assumed; `ceiling_pinned` conjoins exactly the L-A8 block's `3^(p+1) < 2^K < 2·3^(p+1)`, same direction and strictness. **The `hceil` removal is VERIFIED, not taken** — the load-bearing item: all four downstream signatures recorded verbatim at `5c9b663` and at HEAD, the hypothesis is not renamed, not weakened, not a structure/class field, and **not hoisted**, because `T1Structure.lean` declares no `variable`, `include`, `omit`, `section`, `structure`, `class`, `instance`, `attribute` or `local` anywhere in its 482 lines; it is re-derived by `have hceil := ceiling_lower …` at two located call sites and inherited transitively by the other two, with no unrelated drift riding along (window constants, `2000`/`2079`, `4000n² ≤ 2079X`, the `2^71` numerals and every conclusion character-identical). Axiom logs: T1 **13 → 15 exact** (only `ceiling_lower`/`ceiling_pinned` added, nothing removed or re-axiomatised; `discharge_all` `[propext]`, `convPairs_length` `[]`, the rest kernel-3); DeficitLemma **10 of 10** with `key_shifted`/`key15` carrying their own probes; **`sorryAx` absent** from the whole tree but for two prose lines in the RETRACTED block. SCOPE header corrected in place and now matching its file, finder named. **RETRACTED block present, standalone, DO NOT REMOVE, all five things stated**, recorded verbatim; nothing at HEAD depends on the retracted result. **Round-10 flags 1, 2, 4, 5 all closed;** flag 3 closed the other way — the log header naming `LegendreApprox` was deleted rather than entries added, so that file still has no probe and no log line. **Handbacks: none; no discrepancy of digits, hashes, statements or list contents anywhere.** New flat notes: both axiom logs are now curated lists rather than raw probe output (headers and compiler warnings deleted with the repair); `mul_pow_succ_le`/`pow_succ_lt_two_mul_pow` remain unprobed; the volunteered `sorryAx` error-recovery anecdote is not verifiable read-not-built; and one sharpening — because `ceiling_lower` makes the log gap's positivity a *theorem*, a positive cycle is confined to convergents *above* `log₂3`, moving the first north-shore admissible scale from `q₂₂` to `q₂₃ = 137528045312`, Hercher's own threshold (this tightens our own `briefs/merle-la8-t1-check-findings.md` §(f) sentence, not anything of his). **One correction to our own record, named as ours:** `briefs/merle-lean-r10-audit-findings.md` item 3 lists `pow_succ_lt_two_mul_pow` among "the thirteen probed theorems"; it was not probed at `5c9b663` and is not probed at HEAD — that file's axiom-log verdict was correct, only its verbatim list over-included one lemma. **Key recommendation (no key turned): the round-10 condition is met — L-A8's kernel claims can now be keyed, scoped in the ContentDescent language** (statement match + dependency structure + committed logs + 190-check instantiation + our own derivation, with **read-not-built** stated in the same sentence; the two continued-fraction glue facts and `LegendreApprox`'s missing log stay outside), with five hygiene offers (log headers/raw output, probes for the two unprobed lemmas, a `LegendreApprox` probe, the `1.700 bits` pin already offered at round 10, and the one-sidedness sharpening). Owed next per the standing pattern: main-session review (re-run the script), merge, then co-edit/reply material gated on the author's go-ahead.

   *(Superseded, kept for the shape of the round.)* **Windows 1 and 2 were DONE, reviewed and merged** — the three verification checks (main merge `30d0d89`), the Junction recon (`ece0162`), and our own margin proof (`5c1faf4`); every script re-run at review with byte-identical output. Everything still owed sits in **Window 3, main session:** co-edit (L-A7 date-stamp + verification record + three offers; L-A8 key turn with scope + the offers above, `ceiling_upper`'s upper-half-only mismatch carried as the one-lemma repair), then the reply draft; push and sending stay the author's. The reply must also **ask where the Junction repository lives** (see the recon paragraph — plainly, no implication) and carry our margin proof as the second, entropic proof at the true constant. The joint note is deliberately **not** in these windows — it is the next arc, to be planned once L-A8 is two-keyed.

   **Round-11 hygiene check + the four negative measurements: done, reviewed and MERGED** (main merge `185c622`) (branch `merle-r11-hygiene-check`, base `e938040`; `briefs/merle-r11-hygiene-check-findings.md`; `experiments/merle_r11_hygiene_check.py` + committed output, one script for both halves, **115 checks, 0 failures**). His Lean HEAD **`c991430`**; shared repo **unmoved at `c966875`** (our own round-10 push), so the whole hygiene pass is artifact-side and nothing has reached the shared LEDGER. **Half 1 — every claim verified, nothing failed to reconcile:** the three orphan outputs now have generator scripts, and all five relevant scripts (043/055/056 and the repaired 052/053) were **run** here and reproduce their committed outputs byte-identically; every number independently recomputed (six thresholds under both constants, both windows and the `0.01062 %` loss — the same number round 10 rounded to `0.011 %` — the 22 convergents, `5.1713×`/`5.4433×`, `δ = 4.073367·10⁻²²`, the exhaustive sweep over all 190,536 `n < q₁₃ = 190537`). **`5.17× at j = 21` is CORRECT** under the pinned convention (recomputed at all 22; the pre-check `q₁₃/q₂₁/q₂₂/q₂₃` re-derived independently). The **`053` monotonicity argument exhibited**: the direction is valid and needs no hypothesis, but it yields `≥ 22`, not `= 22`; the equality is a separate computation at a margin of only `2.0039×`, which he did run (his red team) and which checks out — so "no result changed" holds, with that precision. **Citation claim (v) CONFIRMED**: no altered `θ_j`/`δ` figure is cited in the shared LEDGER/NOTE/PROTOCOL or in any `briefs/merle-*` record. Three flat artifact notes: `T1Structure.lean` at `c991430` carries **two different `q₂₁`** (line 188 the shifted `6.547·10¹⁰`, line 433 the correct `6586818670`) plus the pre-054 `δ` in the same docstring — the sweep reached the Python and not the Lean comments; the same repair **deleted `OUT-052`'s `(d-bis)` Ostrowski section**, whose `median 15601 vs control 1` and `14936 = 22·665 + 306` the ledger's L-A8 seed block cites, and the committed 052 script does not produce it, so that sentence has no committed artifact at HEAD; and Cor. 29's `X₀ ≥ 3·2⁶⁹` is **promised, not landed** (absent from the whole Lean repo). **Half 2 — the four negatives:** the four lag-1 autocorrelations reproduce exactly **once read as the correlation of `log a_i`** (raw partial quotients give an order of magnitude less — infinite variance), confirming his own retraction; **the chi-squared `0.00103` is a normalised distance**, not a statistic (1.08–22.1) and not a p-value (which would mean rejection), with the normalisation and binning not recoverable — asked as a question, not disputed; the `1.02`–`55.6` ratio range and φ's Fibonacci constancy reproduced; item 3's structural drift claim `log₂a < 2 ⟺ a < 4` **verified exactly** with its averaging convention stated, his empirics recorded as his and **unreplicated — the stopping rule was applied, the search not extended, the want recorded**. **Item 4 verdict: one theorem to hand back, and not the one he names.** Exact and verified on all four real cycles both shores: `Σ log₂(1 ± 1/(3x_i)) = K − n·log₂3` — the seam identity written one step at a time, so "a third face of the same wall" is right and *stronger* than he states it. "Summed around a cycle exactly `n·δ`" is **not** an identity but a sharp bound with equality only at the extremal cycle (on `−17`: `0.188` against `0.396`); what is real is `D(x_min) = δ·(1 + 1/(27x_min²))`, i.e. **`δ` is the per-step north–south drift at the minimum element** (sitting strictly *below* it, by that relative `6.643·10⁻⁴⁵` at `2⁷¹`), its factor 2 being the two-shore symmetry (where the la8 derivation got that 2 from a crude two-bound). `x* = 7/3` is exact and unique but its carrying identity is a tautology; the corollary with teeth is ours — **`x = 1` is the only odd integer at which the sign information exceeds the drift**. Recommendation (no key turned, no ledger text): reply carries the verification, the `j = 21` confirmation, the `053` precision as a compliment to his red team, the three artifact notes, the two scope clauses as questions, and the theorem hand-back; **ledger: nothing yet**. Owed next: main-session review (re-run the script) and merge.

   **Window D, half A — the joint-note premise pre-check, what is ours and what is published: done, reviewed and MERGED** (main merge `836735b`) (branch `jointnote-premise-ours`, base `d945556` — the worktree was cut at the stale `2225b68` and was rebased first; `briefs/jointnote-premise-ours-findings.md` is the record; **no verification script — nothing needed computing**, and the findings say so rather than inventing one). Facts only: no sentence drafted, no wording proposed, no opinion on whether the note should be written. **The phrase is his, the mathematics and the word are ours.** "Counting dichotomy" has **zero** hits in the wiki, `paper/`, `sources/` or either PDF — but it contracts two phrases in our own *published* abstract: **"a sharp dichotomy for counting arguments"** and **"the counting-limit dichotomy developed here"**, under the title *"…and the limits of counting arguments for cycles"*. `cycles.md` itself never uses the word; every other "dichotomy" in the repo is a different object (ladder 15.1.1, reverse one-step 14.10.1, the `q`-dichotomy, and the *refuted* tower dichotomy). **Already published: SUPPORTED, verified in the frozen PDFs and not from `publication.md`** — both halves are paper 1 **v1**, DOI 10.5281/zenodo.21273548, §4, printed as **Theorem 4.5 (uniform trim)** and **Theorem 4.6 (sharpness: the staircase)**, unchanged in **v2**, DOI 10.5281/zenodo.21421120; the contiguous `p ∈ {2,…,23}` evidence is **v2 only**; **the mirror paper carries no part of it** (its `thm:dich` is the 3-adic one-step window, a different theorem). **No load-bearing half is wiki-only.** Four wiki-vs-published differences, none moving a half out of print: the wiki calls half B a **Remark 12.8.3** where the paper calls it a **Theorem**; 12.8.6's machinery is wiki-only while everything it *claims* is in the v2 note; the hedge is identical in both; and 12.8.2's `n₀(p)` table is wiki-only (the paper states only the shape). **Where the fit is loose, two places:** "counting closes every period" overstates 12.8.2, which gives *effective finiteness* — our published wording is careful and says "effective finiteness at every period", and 12.8.5/README forbid running the finite computation; and "provably cannot close them uniformly" is **exactly half proved** — the no-extension-of-the-small-period-constants sentence is a theorem by exhibited witness (`p = 7`, `n = 94`, `γ = 6.74`), the all-`p` `γ = O(log p)` claim carries the published hedge *"though not proved here for all `p`"*, unchanged in v2. **Already yours: SUPPORTED for the published pair** — 12.8 written 2026-07-08 (`b5f367d`), paper drafted 07-09, **correspondence begins 07-16**; two qualifications recorded, both from our own pages: 12.8.6 / the v2 note packages his pincer hypothesis and credits it *in print*, and our own published Related-work paragraph already names *"an impossibility lemma for measure-based uniform bounds kindred to our Theorem 4.6"* in his Zenodo preprint 10.5281/zenodo.19790406. **The obstruction clause: our record has no single "the obstruction"** — four statements at four grades, carried verbatim: the consumption identity is **proved** (11.8.7.7 / published Prop. 3.9), the conclusion drawn from it is **"the organizing heuristic, not a formalized theorem"**, the two-way split is **"the paper's organizing negative observation"**, and the only **theorem**-grade statement of what the obstruction *is* is Theorem 4.6's closing sentence (uniform exclusion requires the divisibility system / anchor-walk rigidity); 12.6.1.3 disclaims being a lever, and the `×2×3` gap is not ours (`aeh.md` 13.6.7, the shared naming of 2026-07-24). So the clause is honest under that last reading and **not** under README's "where the difficulty actually lives", which is heuristic-grade by our own words — the failure mode the brief names is live. **"Three independent directions": not supported from our record** — the three faces are *his* architecture (shared repo `one-obstruction-three-faces`; `NOTE.md` skeleton 2026-07-19: Face I size, Face II digits, Face III seam), and our record's own count of the faces of the difficulty is **two** ("cleanly in two, and only two, places"). **"Three" survives the round-11 hygiene finding** — the drift was never one of the three, so removing it as a candidate *fourth* does not disturb the count — but *independent* is established by nothing of ours, and our record's characteristic move runs the other way (12.8.4 "the same problem"; 13.6.7 "two faces of one missing genre of theorem"). Terminology discrepancy recorded, unadjudicated: his letter says "a third face of the same wall", the brief says "a fourth face". The prior-art item is recorded flat with dates and no priority adjudication, and pinned as bearing on the L-A8/T1 frame prediction and **not** on either half of the dichotomy. Two of our own numbers flagged, recorded and **not corrected**: README/12.8.5 give the `p = 92` bound as `n ~ 10^18` where 12.8.2's own table gives `n₀(92) ~ 4.78·10^21` (both wiki, neither published), and `publication.md`'s "dichotomy" wordings are not the published ones. `experiments/encoding_scan.py` clean. Owed next: main-session review and merge; the sentence itself remains the author's.
   **Window D, half B — the joint-note premise pre-check, the external facts: done, reviewed and MERGED** (main merge `b782e59`) (branch `jointnote-premise-external`, base `d945556`; `briefs/jointnote-premise-external-findings.md` is the record; no script — an external records check, the only arithmetic being three integer comparisons and one ratio, checked in-session). Read-only everywhere: GET requests only on `ccchallenge.org` (no account, nothing added, claimed, edited, wishlisted or submitted; its one `POST` route never called) and unauthenticated clone/`gh api` reads on GitHub (no push, fork, issue, comment, star, watch or follow). **`ccchallenge.org`, checked 2026-07-28: all four of his numbers are the site's current numbers — 371 entries, 1 formalised, 4 being formalised, 5 "Ready to be audited" — so there is no drift to record;** the `371`/`363` pair is database size vs formalisation goal (`353 + 4 + 5 + 0 + 1 = 363`). **The load-bearing fact is CONFIRMED: `Eliahou1993`, "New Lower Bounds on Nontrivial Cycle Lengths", carries one `lean4` formalisation at "Ready to be audited" (AI-assisted: aristotle)** — his self-correction from "the first machine-checked fragment of the cycle literature" to "a fragment" is supported, and supported twice over, since the register's **single accepted** formalisation, `BohmSontacchi1978`, is itself a cycle-existence paper and `Knight2026` ("Collatz high cycles do not exist") is being formalised. Steiner (three entries), Simons–de Weger and Hercher are all listed, all ☆ Wishlist, all **Formalisations (0)**. `Macindoe2026` is catalogued from our own Zenodo **v1** export (recorded verbatim in the findings; the standing decision pins hosting to v2), wishlist 1, **formalisation slot empty** ("No formalisations yet." / "+ Add Formalisation"); submitter attribution is not exposed on any public fragment, so whose entry it is could not be established read-only. **Hercher: `m >= 92` confirmed** (Theorem 23; the abstract defines `m` as the number of local minima — right figure, right symbol, his), with the flat note that `m` and `K` measure different things and only `K` is on the note's axis. **The clause "on a strictly weaker verification hypothesis than ours" is CONFIRMED in direction and agrees with our la8 adjudication — the asymmetry runs in *Hercher's* favour on hypothesis and conclusion alike — and it is now established for *both* his numbers, not only Cor. 29:** in units of `2^60`, Thm 23 needs **704** (his Definition 4, citing Barina), Cor. 29 needs **1536** = `3·2^69`, and the note's own exclusion instantiates **2048** = `2^71` (conclusion ratio `q₂₃/35031771147 = 3.9258`). Hercher is **formalised nowhere we can see** (ccchallenge 0; no counterpart in any of his repositories — his Lean stack uses Simons–de Weger). **Shared repo HEAD `c966875` — unmoved**, our own round-10 push; nothing of his has landed since round 10, so his round-11 `NOTE.md` proposals are proposals only. `NOTE.md` is **structurally the skeleton and textually not the 19 July text**: `git diff f496abe..HEAD` is 2 insertions / 2 deletions — one line of §4 and one of §6, both rewritten **by him on 2026-07-24**, absorbing our two pins; everything else byte-identical, including the header's own "(Merle, 2026-07-19)", against a skeleton commit timestamped 2026-07-18 23:48 +0200. Recorded flat. Three facts against his three proposals, no recommendation: **the word "dichotomy" does not occur in `NOTE.md`**; **there is no abstract** for a position paragraph to sit in (the opening is §0, the Gersonides porch); the mapping-and-instruments genre is already partly there (§7 "Method", and the shared README's "technical-comparison note"). **Three-faces status:** Face II (L-A1, L-A2) and Face III's two ledger components (L3, L-A4) are at **two keys**, both with kernel keys on top; Face III's realization-height component is ours and unledgered by design. **Face I is the exception** — its Merle half, the δ8 impossibility, has **no ledger entry at all** (`LEDGER.md` at `c966875` has zero occurrences of "Product", "δ8" or "scissors", though the claim is in the shared README and in `NOTE.md` §2/§6), against the note header's own rule that every numbered claim enters via the ledger first; and the entry §2 does cite, **L1, carries the status word `corrected`, not `two keys`** (the two-key entry on that side is L2). Two further flat facts: **L-A3 (B) still reads "two keys once Merle's acceptance of the asymptote lands"** in the shared file although our record has that acceptance in his round-9 letter (bookkeeping gap, not disagreement); and **L-A5…L-A8 are cited nowhere in `NOTE.md`, including L-A8/T1**, the very thing a machine-checked-fragment clause would be about. The embarrassment list (findings item 5) carries nine items, including the re-verified fact that `collatz-cycles-lean` at HEAD `1d77168` marks Range Exclusion **INVALID** in `VERIFICATION.md` while `docs/PROOF_ASSEMBLY.md` §10.6 closes *"No gap remains. The proof is unconditional for all `k ≥ 3`"* — material only if the note cites that repository — with the round-11 recon's posture sentence carried over unaltered (absence of a public copy is not evidence against his account). **Facts only: no contribution sentence, no note prose, no framing recommendation — the sentence is the author's, Merle having asked him directly.** Owed next: main-session review and merge.

   **Pointers.** cycles.md 12.6.1.1/12.6.1.2; aeh.md §13.4 (external-replication line); itinerary.md 14.15.9(a); `experiments/transport_recurrence_vectors.json`; the `briefs/merle-*` files are the round-by-round record.

2. **KL–LP residual** (low priority; CLOSED front, see State of the fronts). One well-defined sub-question survives in 14.13: whether a *size-threshold-coupled* version of the `(j,r)` DAG (coupling precision-loss to the renewal induction's accumulated-offset variable, rather than crediting exhaustion for free) recovers real gains from residues. Reopens only with an idea for that coupling, not with more computation.

3. **The wiki's own longer-horizon items.** The anchor-pinning framing thread (README's 2023 seed question) has run its course through the door/exit seam, block-map layer, and itinerary language: the Bridge's remaining content is exactly the stratum word at unbounded length, proved free at every finite level (full shift, itinerary.md 14.15.2), with a symbolic name (the diagonal compatibility locus, 14.15.3(c)) — paused until a further step can engage the word at unbounded length directly, which is the Bridge itself. The equidistribution question (AEH, aeh.md §13) stays long-range per the stopping rules: proof effort waits on an idea. Community outreach (r/Collatz, ccchallenge) is de-prioritized, not a standing item; a substantive response would be verified against the wiki and answered with one reply, no questions.

4. **All-`p` staircase, gap B (the correction algorithm) — DELIVERED on branch `staircase-allp-construction`, awaiting review; `cycles.md` untouched.** Base `e0c34a9`; record `briefs/staircase-allp-construction-findings.md`, fresh verifier `experiments/staircase_allp_construction.py` (+ committed `experiments/staircase_allp_construction_output.txt`, exact big-integer throughout, importing nothing from `staircase_allp.py`/`p22_passer.py`/`uniform_trim.py`). Result: **Algorithm `12.8.6.3` can be removed from the argument, not merely bounded.** The diagnosis first — `12.8.6.2`'s *pure* geometric climb runs a deficit of exactly one bit per block, so its shortfall is `Θ(p)`, and item 5.3 of `briefs/staircase-allp-findings.md` (hoping for an `O(1)`/`O(log p)` move bound) is **refuted as a target**. The fix is one additive constant: the correct staircase is geometric **plus `1/(L−1) = 1.70951` per block**, the standing depth whose interest at rate `log_2(3/2)` pays each block's single unit of exit valuation. Saturating the resulting condition in exact integers gives a construction whose `p` size conditions `q ≤ R_r` hold **by construction, with no correction step** (Theorem B). It consumes exactly one property of `n` — `⌈n log_2 3⌉ − n log_2 3 ≤ −log_2(1 − 2^{−Γ(p,n)})`, i.e. `γ ≥ Γ(p,n)`, with `Γ = 0.92714·n/1.585^p + 2.71 + o(1)` — so **`γ = O(1)`, no `p`-dependence at all**, strictly stronger than the published `O(log p)`. Two consequences for the sibling (`staircase-allp-diophantine`) and for review: the coverage lemma `12.8.6.1` should be **retargeted** from "a correctly-signed continued-fraction run with a bound on the gap between runs" to the far weaker "every interval `[N, 1.05N]` contains an `n` with `⌈nL⌉ − nL ≤ 0.117`" (a density-`0.117` condition, three-distance with no sign or convergent bookkeeping); and `thm:staircase`/`12.8.3`'s `O(log p)` could be strengthened to `O(1)` once that lands. Verified `p ∈ {2,…,32}` (past the record's `23`), certified `n` within `≤ 11` of `round(1.585^p)` at every `p` outside `{2,4}`, all four required negative controls bite (rounding rule: greedy `29/29` against `12.8.6.2`'s `6/29` at the same `n`; quality, crash depth and per-block saturation each fail exactly where the theorem says they may). Every constructed instance was checked against `q | R_r` at all rotations and **fails**, as `12.8.3`/`12.8.6.4` record for theirs — size-passers only. **`12.8.5` is unaffected at any grade and the cycle front stays parked.**
4. **All-`p` staircase, gap A — Diophantine availability: CLOSED, awaiting main-session review.** Branch `staircase-allp-diophantine` (base `e0c34a9`), `briefs/staircase-allp-diophantine-findings.md`, `experiments/staircase_allp_diophantine.py` + committed output. Two results, both delegate-side and unmerged. (i) *The γ budget is not `O(log p)`.* Refuting every polynomial-in-`p` trim `γ + log₂p > c₀n/p^A` — the literal content of `thm:staircase`'s first sentence — needs only `γ(p) = O(ρ^p)` for some `ρ < 1.585`, because the constant it must fall below scales like `1.585^p`; `O(log p)` is required only for the tightest reading (that `12.8.1`'s rate is sharp to within a log factor), which no published sentence asserts. (ii) *Availability is proved unconditionally.* `12.8.6.1`'s gap was routed through the convergent chain, where it is equivalent to `log₂3` being badly approximable; the chain is the wrong object. The construction needs an integer in an exponentially long window whose `‖nL‖` lands in a polynomially small interval — a counting statement. Proved: for every `p ≥ 24` the window `[0.68, 4]·1.585^p` contains `n` with correct sign and `γ(n) ≥ 4.143·log₂p` (exact multiples identity, `p ≤ 712`; Rhin's pinned bound giving `0.049959p − 1.06` beyond), and `γ` prescribable to within 1.28 bits anywhere in `[1.77, 3.358·log₂p]` (three distances), with no hypothesis on the partial quotients. The desert is real (`q₁₃ = 190537 → q₁₄ = 10590737`; correctly-signed semiconvergent run length there is `a₁₅ = 1`) and is blanketed additively by *multiples* of `q₁₃`, which the exhaustive scan shows are optimal there. (iii) *Retargeted mid-session, and closed again at the weaker target.* Once the sibling (`staircase-allp-construction`, merged `e42d785`) closed gap B by **removing** the correction algorithm, its Construction B turned out to consume exactly one property of `n` — `γ(n) ≥ Γ(p,n)` plus a scale side-condition — and nothing about the continued fraction. The lemma owed then reduces to *every interval `[N, 1.05N]` contains an `n` with `⌈nL⌉ − nL ≤ 0.116939`*, and that closes elementarily: `θ := 8 − 5L = 0.07518…`, so `n → n+5` moves `{nL}` by exactly `θ`, the 15 points `N, N+5, …, N+70` lie `θ` apart around a full turn, and `0.117 > θ` — hence **among any 71 consecutive integers there is a good `n`**, unconditionally, with no irrationality measure and no three-distance theorem. The window holds `0.05·1.585^p` integers, which passes 71 at `p = 16`; `p ≤ 15` is checked directly and every `p` except `2` and `4` (outside Construction B's own reach) has a witness. Brute force to `n = 3·10^6`: longest run of failures is **11**, matching the sibling's independently observed worst offset. End-to-end: Construction B, transcribed from the sibling's statement rather than its code, at the `n` supplied here, verified by this session's own transport-recurrence evaluator over `p = 3…26` — all rotations pass, divisibility fails everywhere. **The convergent desert is not an obstruction under the new interface at all.** So the Diophantine half is closed twice over, at the old (stronger) target and at the new one; the recommendation is that `12.8.6.1` be re-stated at the new interface with the convergent-run framing kept as superseded, since the published v2 note points at it. `12.8.5` is unaffected; the cycle front stays parked.
5. **All-`p` staircase — the record's status sweep: PROPOSALS DELIVERED on branch `staircase-status-audit`, awaiting review; nothing edited.** Base `82f0523`; record `briefs/staircase-status-audit-findings.md`. Both halves above being merged (`e42d785`, `da5f06d`), the wiki and the published record were swept for every statement the two theorems make wrong, stale or understated: **46 locations across 12 tracked files — 5 RETIRE, 17 UPDATE, 3 UPGRADE, 21 UNAFFECTED**, plus four published sentences analysed separately. Drafted replacements are carried in full for `cycles.md` §12.8.6 (heading, preamble, `12.8.6.1` restated as an availability theorem with the convergent-run framing kept as a *named superseded formulation* since the published note points at it, `12.8.6.2` as Construction B, `12.8.6.3` demoted with the `Θ(p)` result that killed it, `12.8.6.4`'s band recalibrated, and the "Achieved grade" paragraph replaced), plus `cycles.md` front matter and Current-state, `index.md`, `README.md` line 17, `TOUR.md` (four rows, including the "assessed" vocabulary entry), `publication.md`'s hedge-status entry, and an erratum draft. **The published record needs a different treatment from the wiki, and the asymmetry is sharp: exactly one published sentence is *wrong* — the v2 note's identification of the remaining gap, on both halves — and everything else in print is merely *understated*, the `thm:staircase` hedge's "not proved *here*" remaining a true statement about the paper.** Two mitigations are real: the note's repository pointer is pinned to a frozen commit, so the paper is self-consistent against its own citation. Recommendation: **wiki-only now; an erratum correcting only the gap sentence once §12.8.6 is settled; no v3 yet**, since a v3 has two prerequisites. Those prerequisites are the two gates: **(P1)** `γ = O(1)` is asserted in both findings files and proved in neither — Lemma D is one-sided (it bounds `γ` from *below*), and bounding the achieved `γ` above needs a two-sided target arc, which this session verified is available at no cost (`{nL} ∈ [1−0.116939, 1−0.04]`, length `0.076939 > θ`, giving `3.683 ≤ γ ≤ 5.193` uniformly in `p`; brute-forced to `n = 3·10⁶`, worst run 16 against the proved 70) but which is **new mathematics introduced at audit and must be re-derived before any page says `γ = O(1)`**; **(P2)** whether Construction B is `thm:staircase`'s own "staircase family" better specified or a different family — **the author's judgment, laid out five points each way and deliberately not adjudicated**; one structural fact lowers the stakes, in that it binds only the hedge clause, the theorem's first and closing sentences and the abstract's dichotomy being quantified over all size-passing configurations and now true at every period either way. Also caught: the two drafted replacements in the sibling findings are themselves stale where they name the correction algorithm's move count as what remains (§5(b)'s last sentence, §5(c)'s second clause), because Construction B removes that algorithm from the argument — the "wrong twice over" is a third turn. **Confirmed by reading, not assumed: 12.8.5's strategic conclusion, the PARKED cycle front, and all three README stopping rules are unaffected, and no proposal touches any of them**; `viz/` carries no status text about this claim (zero grep matches on two patterns). Application order, per-step risk grade and a reviewer checklist are in the findings §6; five items I am less than confident in are listed at §7. Nothing here is ledger, note or reply material.

5. **Two record defects found at review — REPAIRED on branch `record-defects-repair`, awaiting review.** Base `82f0523` (worktree cut at the stale `2225b68`, rebranched before starting); record `briefs/record-defects-repair-findings.md`; verifier `experiments/record_defects_check.py` + committed output, **40 checks, 0 failures**. Separate commits, content separate from structure. (i) *`σ` was never defined in Proposition `12.6.1`* — the convention `σ_j = s_j + m_(j+1)` sat 112 lines above in the §12 preamble (implicitly, as `m_(t+1) = σ_t − s_t`) and in closed form only inside `12.6.1.4`'s proof, 33 lines below; the natural local reading `σ_j = m_j + s_j` is a different object, wrong by up to 3.3 orders of magnitude (correct convention reproduces the committed `uniform_trim.R_rot` 300/300, the misreading 2/300). **The blindness is worse than flagged:** not only `12.6.1`'s trivial-cycle sanity identity and `K = Σs_t + n`, but *also* `12.6.1.1`'s transport recurrence (1685/1685 under both), the ghost identity, and `12.6.1.4`'s multiplicativity all pass under either reading — every one depends on `σ` only through the cyclic total `Σσ = K`, which the two readings share. Repaired with one clause in `12.6.1`; no renumbering, no mathematics restated, proof untouched. Canary added (`record_defects_check.py` part 3): three tiers on a profile with unequal `m` and `s`, discriminating via the recurrence with `σ_r` written out *and* via the recorded `gcd(q, R_r) = 7` at `12.8.3`'s `p = 7` seed. Sweep recorded in the findings: `itinerary.md` `14.15.9.2` is **clean** (no `σ` at all; its 24 `σ`s are the sector sign), `spine.md` and `stage*.md` clean or defined-at-use, all 73 frozen drafts carry no `σ`; the same gap **does** sit in `paper/collatz-reduced-v2.tex` line 178 and `sources/paper/collatz-reduced-v1.tex` line 175 (with a `Definition~\ref{def:reduced}` pointer in the proof that the wiki lacked) — **recorded, not edited**, as are `HANDOFF.md`'s own `σ₀` at lines 35/39 and `aeh.md` 109/127. (ii) *The `p = 92` bound.* `n_0(92)` recomputed in exact `Decimal` from Corollary `12.8.2`'s displayed equation: **`4.778·10^21`**, and the same solver reproduces all eight rows of `12.8.2`'s table within 0.4%. The table is right; `README.md` and `12.8.5` were wrong by a factor 4,778. Not a different question — `n`-vs-`K` (`7.6e21`), `p−1` (`2.99e21`) and `92·1.585^92` (`2.3e20`) all miss, while `1.585^92 = 2.53e18` is the bare trim rate without the `γ'/0.585 ≈ 1890` factor the corollary supplies. Both prose figures repaired; `12.8.2`'s table, `12.8.3` and `12.8.6` untouched. **`12.8.5`'s conclusion verified unchanged, not assumed** — at either figure a `p = 92` search must enumerate `≥ C(n−1, 91) ~ 10^1498` resp. `10^1833` profiles against a `~2^68` frontier, with no stopping-rule threshold between them; crossover plan withdrawn, cycle front parked. (Supersedes the "recorded and **not corrected**" note in item 1's Window-D half-A line for the `n_0(92)` figure; `publication.md`'s flagged number is untouched.)

6. **Gate P1 — the `γ` upper bound: CLOSED on branch `staircase-gamma-upper`, awaiting review; nothing edited outside `briefs/` and `experiments/`.** Base `cec521c`; record `briefs/staircase-gamma-upper-findings.md`; fresh verifier `experiments/staircase_gamma_upper.py` + committed output, **45 checks, 0 failures** (19 s; the optional argument `30` extends the end-to-end table, 173 s). **`γ = O(1)` is now proved**, with the absolute bracket **`3.683012 ≤ γ ≤ 5.140212`** at every `p ≥ 16`, the finite tail `3 ≤ p ≤ 15` by exhibition at an explicit `n`, and `p ∈ {2,4}` outside Construction B's reach — so item 5's gate (P1) is discharged and the pages may say `γ = O(1)` with an explicit constant, subject to review. Nothing already in the record needs correcting. **(i) Exact arithmetic does not eat the margin.** `sup Γ` is proved to be the `p → ∞` limit *algebraically* — `η` enters `Γ` with coefficient exactly `1`, and the correction bracket `B(p) = 1.05L(L−1) + 1/(L−1) − (L−1) − (p−2) − L/(L−1)` is negative for every `p ≥ 2` — so `Γ(p,n) < Γ* = 1.05·L(L−1) + 1/(L−1) + 1 ≤ 3.683012100721` for all `p ≥ 2`, `n ≤ 1.05L^p`; the induced `δ_hi = −log₂(1 − 2^{−Γ*}) ≥ 0.116939066509` sits fractionally *above* the record's rounded `0.116939`, which is therefore conservative, and Lemma D stands unaltered. All constants are exact rationals: `L` is enclosed to `10^{-135}` by an `atanh` series with an explicit remainder bound, cross-certified against exact `3^b` vs `2^a` at nine convergents and against `mpmath.iv`; every per-`n` decision is an exact integer comparison that raises rather than guesses. **(ii) (H0) holds unconditionally at every `p ≥ 16` and every `n` in the window**, from the scale alone (`S ≥ (L−1)L^p` and `γ ≥ Γ* > 3.68`) — margins `911.75` and `906.07` at `p = 16` against a `γ` cap worth `1.46` bits, so the interaction the audit could not rule out is three orders of magnitude from being real; the clause that does fail at tiny `p` is `(L−1)L^p ≥ p`, false at `p = 2`. **(iii) `p₀ = 16`, unchanged**, and the sweep criterion is sharpened: the governing condition is `maxgap{jθ mod 1 : j ≤ J} ≤ arc length`, an **iff**, not the span condition the record uses — it needs **66** consecutive integers for the two-sided arc and only **61** for the recorded one-sided one, so Lemma D's "71" is valid (`71 ≥ 66`) but neither minimal nor derived. `J = 12` genuinely fails two-sidedly (447 counterexample starts in `N ≤ 20000`). **(iv) The optimum of this sweep is `γ < 5.131618` (infimum, not attained)**; `δ_lo = 0.0415` is the recommended headline, giving `5.140212` (the audit's `0.04` gives `5.192576`, both certified). Using `Γ(p,n)` exactly rather than its supremum helps at **small** `p`, not large — `0.042` bits at `p = 16`, nothing asymptotically — and is not worth the extra clause; lengthening the same step-5 sweep refines the grid by three distances, so `γ` can be driven to `Γ* = 3.6831` itself at a larger `p₀` (ladder in the findings §8c). End-to-end at 23 periods `p ∈ {3,5,…,26}`: every witness builds, passes all `p` rotations `q ≤ R_r` in this session's own `12.6.1` evaluator (`σ_j = s_j + m_{j+1}`), has crash depth `1`, and **fails `q | R_r` at every rotation**. All four negative controls bite (the `p = 26` row is the exhibit: the one-sided first witness has `γ = 17.058`, the two-sided one `γ = 4.299`). Two of my own over-strong claims were caught by their own controls and are recorded in findings §10 rather than smoothed over. `12.8.5`, the parked cycle front and the README stopping rules are unaffected; `cycles.md`, `README.md`, `publication.md`, `paper/` and `sources/` untouched. Nothing here is ledger, note or reply material.

## The delegation pattern (proven)

One full case study exists: `briefs/mirror-queue-brief.md` → Sonnet session → branch `mirror-queue` → four theorems with per-commit discipline → independent re-verification in review (~50k checks re-run) → merge. The pattern: (i) written brief forcing AGENTS.md compliance and branch discipline; (ii) explicit "record obstructions, don't force analogies" instruction; (iii) reviewer re-runs all verification code before merging. Use it for the delegatable items above (Merle follow-up, KL–LP, the long-horizon items) — not author-only publication steps.

## Known infrastructure quirks

- The sandbox mount of the repo can serve stale reads mid-session (files appear truncated). The Windows-side files are authoritative; verify via PowerShell (`git status`, line counts) before "repairing" anything.
- **Never edit a tracked file with PowerShell 5.1 `Get-Content`/`Set-Content`.** `Get-Content` decodes UTF-8 as ANSI and `Set-Content -Encoding utf8` re-encodes the result as UTF-8, double-encoding every non-ASCII character and adding a BOM. These pages are full of `—`, `≤`, `⌈⌉`, `ε`, `β`, so the damage is total and silent — it survives commits, rebases and merges, and `git diff --stat` shows only the lines you meant to touch. Observed 2026-07-26 resolving a rebase conflict in `HANDOFF.md`: 125 mangled sequences, repaired by restoring the file from the last clean commit (`0378ee4`) and re-applying the wanted change from its own uncorrupted commit rather than attempting byte surgery. Use the Edit/Write tools for file content; if PowerShell is unavoidable, use `[System.IO.File]::ReadAllText/WriteAllLines` with an explicit `UTF8Encoding($false)`. **Check with `experiments/encoding_scan.py`** (run it over the tracked tree before committing anything PowerShell has touched): it reports files that are not valid UTF-8, files carrying a BOM, and files containing the double-encoding signatures of the characters these pages actually use. The same script found and fixed two older instances of the *inverse* fault — `merle_la6_check_output.txt` had been written in cp1252 (a `0x97` em-dash byte, from a round-9 delegate session) and five committed script outputs carried BOMs from PowerShell `>` redirection, which is what caused the "output identical modulo a BOM" friction at earlier reviews. Do not paste a literal mojibake sample into any tracked file — it makes the file itself a permanent false positive.
- The Gmail retrieval pipeline can silently garble numerals in correspondence (observed 2026-07-17: several two-digit groups rendered as single glyphs in a Merle email that displayed cleanly in the author's own client; a reconstructed guess then produced a spurious "disagreement," dissolved once the author read the true value — `briefs/merle-pincer-check-findings.md`, Correction). Before verifying or disputing any number quoted from email, have the author confirm the digits from his own client.
- LaTeX builds: the mount locks aux files; build in a sandbox temp dir and copy artifacts in.
- `\wp` and `\dp` are TeX primitives; the papers use `\wnext`, `\dnext`.
