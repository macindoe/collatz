# Handoff

Written 2026-07-11, ahead of the primary assistant's access ending. This document lets any capable model (or a future session of any model) continue the project without loss. It is a working document: update it whenever the project's state changes materially.

## Onboarding order for a new assistant

1. `README.md` — what this project is, the strategy, and the **binding stopping rules**.
2. `AGENTS.md` — the schema and verification protocol. Non-negotiable: nothing is labeled proved without independently written verification code; failures are recorded, not deleted; sources/ is immutable.
3. `index.md` — the resolver; then whichever pages the task touches.
4. This file — current state and open items.

**Register norm (author's explicit preference):** flat, calibrated prose. No excitement inflation — it degrades judgment and invites hallucination. Heuristics are labeled heuristics. Claims pass through verification before they pass into prose.

**The author's role:** Ben directs. Empirically, the project's load-bearing ideas — the coordinate system itself, the family diagram, the mirror front, the ladder, the dead-ends map — originated as his plain-language questions. Take his naive-sounding questions seriously and work them before defaulting to queued engineering.

## State of the fronts (2026-07-17)

- **Forward per-step theory** (spine.md §9; stage1.md–stage4.md): closed per step; terminal open object is the anchor increment at unbounded depth, consolidated as the Bridge (bridge.md §16).
- **Cycles** (cycles.md §12): periods 1–3 closed; uniform trim resolved — exponentially weak, sharp at the verified instances (12.8.1–12.8.3). All-`p` staircase sharpness held at floor grade: verified instances for the contiguous range `p ∈ {2,...,23}` (12.8.6), the Diophantine coverage bound the sole remaining gap. Front PARKED; reopens only with a divisibility-aware (anchor-rigidity) idea, per the stopping rules.
- **Statistics / AEH** (aeh.md §13): hypothesis formalized, calibration clean (four dissolved anomalies, methodology fixed). The single-sequence structural axis (anchors.md §17.7) is at its endpoint — breadth and depth batteries, a non-2-automaticity screen, further operation lenses, and a PractRand pass, all clean against the fair-coin null. Proof effort parked; equidistribution stays long-range per the stopping rules.
- **Reverse / mirror** (reverse.md §14): ACTIVE. Forward/backward duality complete (14.1–14.12; density bound `c* = 0.3304`, 14.6). KL–LP density refinement closed short of the 0.43 bar on a structural obstruction (14.6.5, 14.13) — reopens only with a size-threshold-coupled idea for the `(j,r)` DAG. The door/exit seam and its block-map corollary (14.14, 14.14.7–14.14.8), the itinerary language as a full shift (14.15), and the two-sided diagonal characterization — completed and signed-extended over the nonzero odd integers (14.15.4–14.15.6) — are all closed and merged; exact height laws (14.15.7) close the integer-fixed-point periodic instances, upgrading the two published escape tables and the period-7 cycle's word to corollaries of one CRT-rate law; a companion author-authorized empirical probe (`briefs/h-nonintegral-probe-findings.md`, findings-grade, no wiki content) confirms modulus-rate escape with purely periodic denominator-bounded constants on the whole non-integral single-letter shelf (n ≤ 25), pinning the rigidity content of height growth on aperiodic words — the Bridge itself; the Bridge's status is unchanged throughout.
- **Ladder** (ladder.md §15): closed as local law.
- **Papers**: BOTH PUBLISHED — paper 1 (DOI 10.5281/zenodo.21273548) and the mirror paper (DOI 10.5281/zenodo.21303918), linked via a Continues relation, two external referee cycles completed pre-publication. Paper 1's v2 — subtitle fix, staircase evidence extended through the `p = 22` resolution — is merged to main and awaits the author's Zenodo upload (publication.md). Sources: `sources/paper/collatz-reduced-v1.tex` (archived), `paper/collatz-reduced-v2.tex`, `paper/collatz-mirror-v1.tex`.

## Open work items, in priority order

1. **Eric Merle collaboration.** Trial accepted by the author (2026-07-16). First exchange closed: his reply verified and answered, sent by the author — hedge conceded exactly, the AEH question answered (no calibration anomaly was staircase-shaped, TOUR.md), the 12.8.6 floor-grade outcome reported (interval `[1.828, 3.643]`), a two-key/three-repo collaboration-protocol proposal accepted. His second reply proposed the "Diophantine pincer" hypothesis for `p = 22`; delegated, verified, merged (`briefs/merle-pincer-check-findings.md`): his cause was confirmed, his recovery-threshold claim was refuted at his own candidates, and the `p = 22` obstruction is dissolved — `cycles.md` 12.8.6 now records the full contiguous range `p ∈ {2,...,23}`. **Current facts:** the second reply is received and verified, the pincer check is merged, and the author's reply was **SENT 2026-07-17** (sending stays with the author, a standing convention): it reports the `p = 22` closure and the all-four-rows confirmation of his measurements, asks for his candidate-generation rule (his `n = 25217, 31202` are arithmetic on the best convergent, outside our lemma's step), endorses "one obstruction, three faces" as the note's title, and announces the shared repository — `github.com/macindoe/one-obstruction-three-faces` (private; seeded with `PROTOCOL.md` as an explicit draft-for-co-editing and `LEDGER.md` with seeds L1–L3, L1 recorded "corrected, both directions"); the GitHub collaborator invitation went to his email with the reply. Next: Merle's protocol edits and his generator-rule answer; then the note itself, with the `p = 22` resolution and the Diophantine coverage gap (`briefs/staircase-allp-findings.md`) as its first shared technical objects.
2. **Paper 1 v2 — Zenodo upload pending.** v2, including the `p = 22` update, is merged to main; the sole remaining step is the author's upload (publication.md).
3. **KL–LP residual** (low priority; CLOSED front, see State of the fronts). One well-defined sub-question survives in 14.13: whether a *size-threshold-coupled* version of the `(j,r)` DAG (coupling precision-loss to the renewal induction's accumulated-offset variable, rather than crediting exhaustion for free) recovers real gains from residues. Reopens only with an idea for that coupling, not with more computation.
4. **The wiki's own longer-horizon items.** The anchor-pinning framing thread (README's 2023 seed question) has run its course through the door/exit seam, block-map layer, and itinerary language: the Bridge's remaining content is exactly the stratum word at unbounded length, proved free at every finite level (full shift, reverse.md 14.15.2), with a symbolic name (the diagonal compatibility locus, 14.15.3(c)) — paused until a further step can engage the word at unbounded length directly, which is the Bridge itself. The equidistribution question (AEH, aeh.md §13) stays long-range per the stopping rules: proof effort waits on an idea. Community outreach (r/Collatz, ccchallenge) is de-prioritized, not a standing item; a substantive response would be verified against the wiki and answered with one reply, no questions.

## The delegation pattern (proven)

One full case study exists: `briefs/mirror-queue-brief.md` → Sonnet session → branch `mirror-queue` → four theorems with per-commit discipline → independent re-verification in review (~50k checks re-run) → merge. The pattern: (i) written brief forcing AGENTS.md compliance and branch discipline; (ii) explicit "record obstructions, don't force analogies" instruction; (iii) reviewer re-runs all verification code before merging. Use it for the delegatable items above (Merle follow-up, KL–LP, the long-horizon items) — not the author-only Zenodo upload.

## Known infrastructure quirks

- The sandbox mount of the repo can serve stale reads mid-session (files appear truncated). The Windows-side files are authoritative; verify via PowerShell (`git status`, line counts) before "repairing" anything.
- The Gmail retrieval pipeline can silently garble numerals in correspondence (observed 2026-07-17: several two-digit groups rendered as single glyphs in a Merle email that displayed cleanly in the author's own client; a reconstructed guess then produced a spurious "disagreement," dissolved once the author read the true value — `briefs/merle-pincer-check-findings.md`, Correction). Before verifying or disputing any number quoted from email, have the author confirm the digits from his own client.
- LaTeX builds: the mount locks aux files; build in a sandbox temp dir and copy artifacts in.
- `\wp` and `\dp` are TeX primitives; the papers use `\wnext`, `\dnext`.
